---
milestone: v1.0
audited: 2026-07-16T15:00:00Z
status: tech_debt
scores:
  requirements: 21/21
  phases: 3/3
  integration: 5/5
  flows: 1/1
gaps: {}
tech_debt:
  - phase: 01-xml-validation-hook
    items:
      - "WR-01 (non-blocking, code review): CRLF offset/line-col desync in XMLV-04 root-check — wrong column reported only under CRLF content. Real modelo/*.md files are all LF, so no current production impact."
      - "WR-03 (non-blocking, code review): validate-xml-casca.test.js has a process.cwd()-dependent real-file test — fragile if ever invoked from a different cwd, does not affect shipped hook behavior."
  - phase: 03-separar-claude-md-distribuido-cliente-via-npm-do-claude-md-i
    items:
      - "IN-01/IN-02/IN-03 (non-blocking, code review): stale GSD project-block prose, a JSON quote-escaping edge case, and an undocumented dual-missing-file fallback — all explicitly marked low-priority/non-blocking by the phase's own code review."
  - phase: integration (cross-phase, found by this audit)
    items:
      - "recepcionista-scaffolder.md (lines 3, 16, 18, 147) still names the retired client-project-scaffolder subagent instead of the new client-scaffold-structure/-collect/-fill chain. Phase 2's Truth #11 only checked ei-cria-cliente.md for stale references (confirmed 0 there); this file was out of the 3-step-split scope by Phase 2's own D-05 decision, but its prose now describes a sequencing dependency on a subagent that no longer exists. Ships to clients via manifest.json. Non-blocking (descriptive text, not executable wiring) but will confuse future maintainers."
      - "inject-ei-context.sh hardcodes $PROJECT_DIR/CLAUDE.md with no Phase-3 dual-context (client/CLAUDE.md-first) fallback. Currently disabled (no SessionStart/UserPromptSubmit/PreToolUse hook registers it per settings.json's _disabled_note) and still ships to clients via manifest.json, where root CLAUDE.md exists post-install so it would still resolve correctly for clients. Latent risk is repo-fonte-only: if ever re-enabled in this repo (where root CLAUDE.md was intentionally removed by Phase 3), it would silently warn 'CLAUDE.md não encontrado' instead of falling back to client/CLAUDE.md."
---

# Milestone v1.0 Audit — ei-prompt: Validação de XML + Scaffolding em 3 Passos

**Audited:** 2026-07-16T15:00:00Z
**Definition of done (ROADMAP.md Overview):** Two independent hardening tracks — (1) a deterministic XML-casca validation hook replacing the manual review checklist with real code checks, and (2) a refactor of client creation into three auditable steps with a hard confirmation gate — plus a structural Phase 3 splitting the npm-distributed client CLAUDE.md from this repo's internal maintainer CLAUDE.md, guarded by a deterministic regression hook.

## Requirements Coverage (3-Source Cross-Reference)

All 21 v1 requirements cross-checked across REQUIREMENTS.md traceability, each phase's VERIFICATION.md requirements table, and each plan's SUMMARY.md `requirements-completed` frontmatter. No orphans, no mismatches.

| Requirement | Phase | REQUIREMENTS.md | VERIFICATION.md | SUMMARY frontmatter | Final Status |
|---|---|---|---|---|---|
| XMLV-01 | 1 | [x] | ✓ SATISFIED | 01-01 | **satisfied** |
| XMLV-02 | 1 | [x] | ✓ SATISFIED | 01-01 | **satisfied** |
| XMLV-03 | 1 | [x] | ✓ SATISFIED | 01-01 | **satisfied** |
| XMLV-04 | 1 | [x] | ✓ SATISFIED | 01-01 | **satisfied** |
| XMLV-05 | 1 | [x] | ✓ SATISFIED (gap closed by 01-04) | 01-02,01-03,01-04 | **satisfied** |
| XMLV-06 | 1 | [x] | ✓ SATISFIED | 01-01,01-02,01-03 | **satisfied** |
| XMLV-07 | 1 | [x] | ✓ SATISFIED | 01-01,01-03 | **satisfied** |
| SCAF-01 | 2 | [x] | ✓ SATISFIED | 02-01,02-02,02-03,02-05 | **satisfied** |
| SCAF-02 | 2 | [x] | ✓ SATISFIED | 02-01,02-03,02-05 | **satisfied** |
| SCAF-03 | 2 | [x] | ✓ SATISFIED | 02-01,02-03,02-05 | **satisfied** |
| SCAF-04 | 2 | [x] | ✓ SATISFIED | 02-03,02-04,02-05 | **satisfied** |
| SCAF-05 | 2 | [x] | ✓ SATISFIED | 02-04,02-05 | **satisfied** |
| SCAF-06 | 2 | [x] | ⚠ NEEDS HUMAN → resolved by 02-UAT.md (pass, 2026-07-06T02:35Z) | 02-01,02-03,02-05 | **satisfied** |
| CLMD-01 | 3 | [x] | ✓ SATISFIED | 03-01 | **satisfied** |
| CLMD-02 | 3 | [x] | ✓ SATISFIED | 03-01 | **satisfied** |
| CLMD-03 | 3 | [x] | ✓ SATISFIED | 03-02 | **satisfied** |
| CLMD-04 | 3 | [x] | ✓ SATISFIED | 03-04 | **satisfied** |
| CLMD-05 | 3 | [x] | ✓ SATISFIED | 03-04 | **satisfied** |
| CLMD-06 | 3 | [x] | ✓ SATISFIED | 03-03 | **satisfied** |
| CLMD-07 | 3 | [x] | ✓ SATISFIED | 03-05 | **satisfied** |
| CLMD-08 | 3 | [x] | ✓ SATISFIED (human checkpoint approved) | 03-05 | **satisfied** |

**Score: 21/21 satisfied. 0 orphaned. 0 unsatisfied.**

Note on SCAF-06: Phase 2's 02-VERIFICATION.md (2026-07-06T02:28Z) flagged this requirement `human_needed` because the field→file routing contract (CR-02, commit `d92a025`) changed *after* the phase's only two live-session checkpoints (02-05-SUMMARY.md) had already been approved — the old approvals were evidence for a schema that no longer matched the code on disk. This was not left open: `02-UAT.md` (2026-07-06T02:35Z, 5 minutes after the verification) records a fresh live re-test of exactly the CR-02 contract with result `pass`. Treated as satisfied.

## Phase Verification Summary

| Phase | VERIFICATION.md Status | Score | Critical Gaps | Notes |
|---|---|---|---|---|
| 1. XML Validation Hook | passed | 12/12 | none | Re-verification after one gap (WR-02/D-06, scoping) closed by Plan 01-04. 2 non-blocking code-review warnings (WR-01, WR-03) carried forward as accepted limitations. |
| 2. 3-Step Gated Client Scaffolding | passed (body header said `human_needed`, resolved same day via 02-UAT.md) | 11/12 → 12/12 after UAT | none | Only open item was the SCAF-06 live re-verification, closed by 02-UAT.md within minutes of the verification report. |
| 3. Separar CLAUDE.md distribuído/interno | passed | 8/8 | none | 2 critical + 1 warning code-review findings fixed and confirmed live in the same phase (CR-01, CR-02, WR-02); 1 warning (WR-01) explicitly marked no-action-required by the reviewer; 3 low-priority info items (IN-01..03) left open, non-blocking. |

**Score: 3/3 phases passed, 0 unverified phases.**

## Cross-Phase Integration Check

Independent re-derivation performed by `gsd-integration-checker` against the current codebase (not the phases' own VERIFICATION.md claims). Full detail below; primary source files read: `.claude/settings.json`, `.claude/settings.local.json`, `manifest.json`, `bin/cli.js`, `.claude/hooks/*.sh`, `.claude/agents/*.md`, `.claude/commands/ei-cria-cliente.md`.

| # | Check | Result | Evidence |
|---|---|---|---|
| 1 | Phase 1 hook still fires correctly on files written by Phase 2's renamed subagents | ✓ PASS (1 non-blocking doc-drift warning) | `discoverTouchedFiles` in `validate-xml-casca.js` is subagent-agnostic (scans Edit/Write tool_use generically); `post-scaffolder-review.sh`'s case statement correctly updated to `client-scaffold-fill`. **Warning:** `recepcionista-scaffolder.md` still names the retired `client-project-scaffolder` in prose (lines 3,16,18,147) — descriptive drift, not executable wiring. |
| 2 | Phase 2 subagents remain plain-string manifest entries; unaffected by Phase 3's `{from,to}` normalization | ✓ PASS | `client-scaffold-{structure,collect,fill}.md` are plain strings in `manifest.json`; only the CLAUDE.md entry is `{from,to}`; `normalizeEntry()`/`formatManifestEntry()` (`bin/cli.js:23,29`) handle both shapes in both the install loop and the deprecated-files loop. |
| 3 | Phase 3's dual-context CLAUDE.md fallback correctly applied to Phase 2 artifacts; Phase 1 hooks don't break on root CLAUDE.md removal | ✓ PASS (1 non-blocking latent-risk warning) | `client-scaffold-fill.md`/`client-scaffold-collect.md`/`client-scaffold-structure.md` all declare `Glob` in `tools:` and contain the fallback text. Phase 1 hooks have no hardcoded root-CLAUDE.md dependency. **Warning:** `inject-ei-context.sh` hardcodes `$PROJECT_DIR/CLAUDE.md` with no fallback — currently disabled repo-wide, and harmless for client installs (which still get `CLAUDE.md` post-install), but would misbehave in this repo-fonte if ever re-enabled. |
| 4 | Full E2E flow: scaffold → collect → gate → fill → both SubagentStop hooks (docs-reviewer audit + XML validation) fire independently without clobbering | ✓ PASS | `.claude/settings.json` SubagentStop array has independent entries for `post-scaffolder-review.sh` and `validate-xml-casca.sh`; different output shapes (`additionalContext` vs `decision:block`), no shared state/files, no `stop_hook_active` race (validator deliberately re-checks every time). |
| 5 | manifest.json completeness across all 3 phases | ✓ PASS | 3 new scaffolding subagents present; XML hook `.sh`/`.js` pair present (no `.test.js`/fixtures); `{from,to}` CLAUDE.md entry present; `client-project-scaffolder.md` correctly in `deprecated_files`, not `files[]`; `check-claude-md-audience.sh` correctly absent from `manifest.json` (registered only in gitignored `.claude/settings.local.json` per CLMD-07). |

**Score: 5/5 integration checks pass. 0 blockers. 2 non-blocking warnings (both pre-existing doc/dead-code drift, not regressions introduced by wiring failures).**

## E2E User Flows

| Flow | Status | Notes |
|---|---|---|
| New client creation (single-agent): scaffold → collect → gate → fill → dual SubagentStop audit | ✓ PASS | Live-verified in 02-05-SUMMARY.md Task 2 (pre-CR-02) + 02-UAT.md (post-CR-02, 2026-07-06). |
| New client creation (multi-agent, incl. deliberate Cancel): same chain per specialty, non-destructive cancel | ✓ PASS | Live-verified in 02-05-SUMMARY.md Task 3 (approval + deliberate Cancel). |
| Client adjustment (`/ei-ajustes`) unaffected by XML hook and CLAUDE.md split | ✓ PASS | Phase 1's XML hook coexists with `post-ajustes-fanout.sh`'s sentinel protocol (both registered independently on Stop); Phase 3's fallback pattern applied to `ei-ajustes.md`'s 2 edit points. |
| npm distribution end-to-end (`bin/cli.js` against updated `manifest.json`) | ✓ PASS | Human checkpoint approved in 03-05-SUMMARY.md (`npm test` 43/43, `--help` sanity, content-diff/install check, live guard sanity trigger+revert). |

**Score: 4/4 flows pass (workflow's flows score field reported as 1/1 for the single explicit E2E trace requested; the table above additionally cross-checks the 3 other flows implied by the phases' own live checkpoints).**

## Tech Debt (Non-Blocking, Deferred)

See YAML frontmatter `tech_debt` for the structured list. Summary: 2 pre-existing code-review warnings from Phase 1 (CRLF column desync, cwd-dependent test — both accepted as non-material since real files are LF and tests run from repo root), 3 low-priority info items from Phase 3's code review (explicitly marked non-blocking by the reviewer), and 2 new findings from this audit's integration check (stale `client-project-scaffolder` naming in `recepcionista-scaffolder.md`, and `inject-ei-context.sh`'s hardcoded root-CLAUDE.md path with no Phase-3 fallback — both dead/disabled-path or out-of-scope-by-design issues, neither blocking milestone completion).

## Status Rationale

**tech_debt** — no unsatisfied or orphaned requirements, no broken cross-phase wiring, no broken E2E flows, all 3 phases independently verified `passed`. Status is `tech_debt` rather than a clean `passed` only because of the 7 accumulated non-blocking items listed above (2 from Phase 1, 3 from Phase 3, 2 newly found by this audit's integration check) — all explicitly non-blocking, several already accepted by their originating phase's own reviewer, none touching a requirement's satisfaction.

---

*Audited: 2026-07-16T15:00:00Z*
*Auditor: Claude (gsd-audit-milestone)*
