---
phase: quick-260716-on3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.claude/commands/ei-ajustes.md, .claude/agents/docs-analyzer.md]
autonomous: true
requirements: [EI-AJUSTES-FALLBACK-NOTE-REMOVE-01, DOCS-ANALYZER-CONDITIONAL-MULTI-READ-01]

must_haves:
  truths:
    - "Rodando `/ei-ajustes` em fallback puro (o caminho padrão/esperado — hook não disparou em nenhum turno), a Apresentação final estendida exibida ao usuário leigo NÃO contém mais nenhuma linha mencionando o hook, `.claude/settings.json`, ou o fato da rodada ter rodado sem trigger — esse é um detalhe de implementação interna, não sinal acionável pelo dono do negócio."
    - "A documentação interna para implementadores (HOOK-02/HOOK-02b: 'fallback é o ESTADO PADRÃO, não caminho de exceção') permanece byte-a-byte intacta — só a INSTRUÇÃO de surfacear a nota ao usuário final foi removida, não a explicação técnica do porquê fallback é normal."
    - "Nenhuma referência remanescente a D-17 ou D-09 em `.claude/commands/ei-ajustes.md` ficou dangling (apontando para texto removido) — as ocorrências restantes desses rótulos pertencem a regras não relacionadas (D-17: navegação local 'Voltar' no caminho [B.2]; D-09: bug de contrato de confiança do analyzer e parsing dos marcadores `<resultado>`) e continuam válidas e autocontidas."
    - "Um ajuste single-agent (o caso comum em produção) via `/ei-ajustes` não faz mais o `docs-analyzer` ler `docs/multi-agente-recepcionista.md` — arquivo cujas regras (personificação pós-transferência do Recepcionista) só se aplicam a clientes multi-agente."
    - "Um ajuste multi-agente via `/ei-ajustes` (`<modo>=multi`) continua fazendo o `docs-analyzer` ler `docs/multi-agente-recepcionista.md` normalmente — a regra de personificação pós-transferência continua disponível para decisões corretas nesse modo (Exemplo 3 do próprio arquivo depende dela)."
    - "As outras 3 leituras obrigatórias do Passo 0 (`client/CLAUDE.md`/`CLAUDE.md` fallback, `docs/regras-edicao.md`, `docs/proibido-fazer.md`) permanecem incondicionais em ambos os modos — só `docs/multi-agente-recepcionista.md` passou a ser condicional."
  artifacts:
    - path: ".claude/commands/ei-ajustes.md"
      provides: "Apresentação final estendida (Passo 6, ambas ocorrências: linha ~504 dentro de HOOK-02/HOOK-02b, e linhas ~745-749 dentro de 'Apresentação final estendida') sem a instrução de nota de fallback voltada ao usuário final; explicação técnica HOOK-02/HOOK-02b inalterada."
    - path: ".claude/agents/docs-analyzer.md"
      provides: "Passo 0 com a leitura de `docs/multi-agente-recepcionista.md` condicionada a `<modo>=multi`, fraseada como referência antecipada à entrada (mesmo padrão já usado por REGRA #0 para `<cliente_path>` antes de `<entrada_esperada>` ser formalmente definida mais abaixo no arquivo)."
  key_links:
    - from: "Passo 6 — Instrução de trigger do hook (HOOK-02, ~linha 492-502)"
      to: "Apresentação final estendida (~linha 729-757, incluindo bloco 'Resumo final do /ei-ajustes' e 'Mapeamento dos ícones')"
      via: "As duas ocorrências da nota de fallback removidas nesta tarefa vivem uma em cada seção; remover uma sem a outra deixaria o comportamento inconsistente entre o Passo 6 (trigger) e a Apresentação final (onde a nota seria de fato exibida) — ambas devem ser removidas juntas neste mesmo plano."
      pattern: "modo fallback"
    - from: "docs-analyzer.md PASSO 0 (leitura de regras, topo do arquivo)"
      to: "docs-analyzer.md <entrada_esperada> (definição formal de `<modo>`, mais abaixo no arquivo)"
      via: "Passo 0 roda ANTES de `<entrada_esperada>` ser formalmente introduzida; a condicional precisa referenciar '`<modo>` recebido na entrada' do mesmo jeito que REGRA #0 já referencia `<cliente_path>` recebido na entrada antes da seção formal — sem essa costura, o Passo 0 ficaria ambíguo sobre de onde `<modo>` vem."
      pattern: "modo"
---

<objective>
Aplicar 2 correções de UX pontuais e independentes no pipeline `/ei-ajustes`, ambas identificadas pelo dono do projeto após revisar um caso real de produção:

1. Remover a instrução (rotulada D-17/D-09, duas ocorrências) que faz o `/ei-ajustes` incluir uma nota técnica interna ("rodada em modo fallback — hook não acionou") na Apresentação final estendida mostrada ao usuário leigo — fallback é o comportamento PADRÃO/esperado (já documentado como tal no próprio arquivo), não um sinal de problema que o operador de negócio precise ver ou agir.
2. Tornar a leitura de `docs/multi-agente-recepcionista.md` no Passo 0 do `docs-analyzer` condicional a `<modo>=multi` — hoje é lida incondicionalmente mesmo em ajustes single-agent (o caso comum), desperdiçando contexto com regras de personificação pós-transferência que só se aplicam a clientes multi-agente com Recepcionista.

Purpose: reduzir ruído confuso na saída voltada ao usuário final e reduzir desperdício de contexto/relevância em análises single-agent, sem alterar nenhum comportamento funcional do pipeline (HOOK-02/HOOK-02b continuam fazendo exatamente o que faziam; `docs/multi-agente-recepcionista.md` continua sendo lido normalmente sempre que `<modo>=multi`).
Output: `.claude/commands/ei-ajustes.md` e `.claude/agents/docs-analyzer.md` editados; nenhuma mudança de versão, CHANGELOG ou manifest.json (o conteúdo shipado via manifest já cobre esses paths, que não mudam).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/commands/ei-ajustes.md
@.claude/agents/docs-analyzer.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover a nota de fallback voltada ao usuário final em ei-ajustes.md</name>
  <files>.claude/commands/ei-ajustes.md</files>
  <action>
    Ler o arquivo primeiro para confirmar os números de linha atuais (podem ter deslocado ligeiramente desde a leitura de planejamento). Fazer DUAS remoções cirúrgicas, deixando o resto do arquivo byte-a-byte intacto:

    **Remoção A** — dentro da seção `#### Instrução de trigger do hook (HOOK-02 — fallback é o ESTADO PADRÃO)`, existe um único parágrafo posicionado logo depois do parágrafo que começa com "**HOOK-02b — fallback puro num único turno (consumed inline):**" e logo antes do heading `#### ⚠️ REGRA INVIOLÁVEL DO PASSO 6 (HOOK-02 — consumed pós-trigger)`. Esse parágrafo instrui incluir, na Apresentação final estendida, uma nota literal citando os rótulos D-17 e D-09 sempre que a rodada inteira tiver rodado sem o hook `post-ajustes-fanout.sh` ter disparado nenhum `reason`. Remover esse parágrafo por completo (a linha inteira). NÃO tocar nos parágrafos HOOK-02/HOOK-02b acima dele (a explicação de "fallback é o ESTADO PADRÃO, não caminho de exceção" e o mecanismo de emissão de `consumed` inline em fallback puro) — essa é documentação técnica válida para implementadores, não a nota voltada ao usuário. Depois de remover, deixar exatamente UMA linha em branco entre o fim do parágrafo HOOK-02b e o heading `#### ⚠️ REGRA INVIOLÁVEL DO PASSO 6`, igual ao espaçamento já usado entre parágrafos e headings em outros pontos deste mesmo arquivo.

    **Remoção B** — dentro da seção `#### Apresentação final estendida (D-16 estilo, 6 estados)`, logo depois do bloco de código cercado por ``` ``` ``` (o exemplo "Resumo final do /ei-ajustes...") e logo antes do parágrafo "Mapeamento dos ícones:", existem 3 linhas de conteúdo (com 2 linhas em branco intercaladas) formando um bloco: um parágrafo em negrito "**Nota opcional (D-17 — fallback):**..." explicando a condição, uma citação em blockquote (`>`) com o texto literal da nota entre aspas (citando os mesmos rótulos D-17/D-09 e mencionando `.claude/settings.json`), e um parágrafo final dizendo que a nota NÃO deve ser incluída se pelo menos uma rodada recebeu o trigger do hook. Remover o bloco inteiro (as 3 linhas de conteúdo + as 2 linhas em branco entre elas). Depois de remover, deixar exatamente UMA linha em branco entre o fechamento ``` ``` ``` do bloco de código "Resumo final" e o parágrafo "Mapeamento dos ícones:" que vem logo em seguida — igual ao padrão de espaçamento já usado no resto do arquivo entre um bloco de código e o parágrafo seguinte.

    **Checagem de referências penduradas:** depois das duas remoções, buscar (via Grep ou leitura) TODAS as ocorrências restantes de "D-17" e "D-09" no arquivo inteiro. Confirmar que cada ocorrência remanescente pertence a uma regra diferente e autocontida (não depende do texto removido): D-17 deve sobrar apenas nas 3 ocorrências relacionadas à navegação local "Voltar" do caminho [B.2] (armazenamento mental das opções originais + reabertura do AskUserQuestion sem re-invocar o analyzer); D-09 deve sobrar apenas nas 6 ocorrências relacionadas ao bug de contrato de confiança do `docs-analyzer` (roteamento quando `confianca` != `alta`, incluindo o gate de reformulação que reutiliza esse mesmo rótulo) e ao parsing dos marcadores `<resultado>OK|ERRO</resultado>` dos editores/reviewers. Se alguma ocorrência remanescente parecer fazer referência cruzada explícita ao texto que acabou de ser removido (por exemplo, dizendo algo como "ver nota de fallback acima" ou citando a mesma frase), ajustar essa ocorrência também — mas isso não é esperado dado o levantamento feito durante o planejamento.

    Depois de confirmar as duas remoções e a checagem de referências, commitar ESTA tarefa isoladamente: `git add .claude/commands/ei-ajustes.md` (SOMENTE este arquivo — NUNCA `git add -A` nem `git add .`, pois há uma alteração pré-existente e não relacionada já staged em `client/CLAUDE.md` no working tree que NÃO deve entrar neste commit) seguido de `git commit -m "docs: remove internal fallback-mode note from ei-ajustes end-user summary"` (mensagem exatamente como especificada, sem nenhum trailer/assinatura adicional como "Generated with Claude Code" ou "Co-Authored-By" — regra do projeto em `docs/proibido-fazer.md`).
  </action>
  <verify>
    <automated>grep -c "modo fallback" .claude/commands/ei-ajustes.md | grep -qx 0 && [ "$(grep -c 'D-17' .claude/commands/ei-ajustes.md)" = 3 ] && [ "$(grep -c 'D-09' .claude/commands/ei-ajustes.md)" = 6 ] && grep -q "fallback é o ESTADO PADRÃO" .claude/commands/ei-ajustes.md && grep -q "HOOK-02b — fallback puro" .claude/commands/ei-ajustes.md && grep -q "Mapeamento dos ícones" .claude/commands/ei-ajustes.md && grep -q "REGRA INVIOLÁVEL DO PASSO 6" .claude/commands/ei-ajustes.md && echo PASS</automated>
  </verify>
  <done>`.claude/commands/ei-ajustes.md` não contém mais nenhuma menção à nota de fallback voltada ao usuário final (0 ocorrências); a explicação técnica HOOK-02/HOOK-02b ("fallback é o ESTADO PADRÃO") e o bloco "Apresentação final estendida"/"Mapeamento dos ícones" permanecem presentes e intactos; contagem de D-17 caiu de 5 para 3 ocorrências e de D-09 caiu de 8 para 6 ocorrências, todas as remanescentes pertencendo a regras não relacionadas e autocontidas; mudança commitada isoladamente (`.claude/commands/ei-ajustes.md` apenas, sem tocar `client/CLAUDE.md`).</done>
</task>

<task type="auto">
  <name>Task 2: Condicionar a leitura de multi-agente-recepcionista.md ao modo=multi em docs-analyzer.md</name>
  <files>.claude/agents/docs-analyzer.md</files>
  <action>
    Ler o arquivo primeiro para confirmar o texto atual da seção `## ⚠️ PASSO 0 — CARREGAR REGRAS (OBRIGATÓRIO)` (topo do arquivo, logo após o frontmatter). Hoje essa seção lista 4 alvos de `Read` incondicionais em bullets, sendo o último `docs/multi-agente-recepcionista.md` (regra de personificação pós-transferência).

    Reescrever a lista de bullets para que os 3 primeiros alvos (`client/CLAUDE.md` se existir via Glob, senão `CLAUDE.md`; `docs/regras-edicao.md`; `docs/proibido-fazer.md`) permaneçam EXATAMENTE como estão hoje, incondicionais, sem nenhuma mudança de texto. Substituir apenas o 4º bullet por uma condicional explícita: algo como "SE `<modo>` (recebido na entrada — ver `<entrada_esperada>` abaixo) for `multi`: leia também `docs/multi-agente-recepcionista.md` (regra de personificação pós-transferência) — aplica-se apenas a clientes multi-agente com Recepcionista; em `<modo>=single`, pule este arquivo." Usar a mesma fraseologia de referência antecipada já usada pela `REGRA #0 — CAMINHO LITERAL` logo abaixo do Passo 0, que já menciona "`<cliente_path>` recebido na entrada" antes de `<entrada_esperada>` ser formalmente definida mais adiante no arquivo — não é necessário reordenar seções, só fraseiar a condicional dessa forma consistente. Espelhar também o estilo de condicional single/multi já usado em `<fluxo_de_analise>` item 6 ("**Multi-agente:** se `<modo>` = `multi`, valide que...") para manter consistência de tom dentro do mesmo arquivo.

    Não alterar o parágrafo final da seção ("Essas regras são fonte da verdade sobre arquitetura de agentes...") nem nenhuma outra parte do arquivo — não tocar em `<entrada_esperada>`, `<fluxo_de_analise>`, `<conhecimento_dos_papeis>`, `<formato_resposta>`, `<exemplos>` ou `<regras>`, que já tratam `<modo>` corretamente e não precisam de mudança.

    Depois de confirmar a edição, commitar ESTA tarefa isoladamente: `git add .claude/agents/docs-analyzer.md` (SOMENTE este arquivo — NUNCA `git add -A` nem `git add .`, pelo mesmo motivo da Task 1: preservar a alteração pré-existente staged em `client/CLAUDE.md` fora deste commit) seguido de `git commit -m "docs: make multi-agente-recepcionista.md read conditional on modo=multi in docs-analyzer"` (mensagem exatamente como especificada, sem trailer/assinatura adicional).
  </action>
  <verify>
    <automated>P0=$(grep -A7 "PASSO 0 — CARREGAR REGRAS" .claude/agents/docs-analyzer.md); echo "$P0" | grep -q "modo" && echo "$P0" | grep -qE "^- \`client/CLAUDE.md\` se existir" && echo "$P0" | grep -qE "^- \`docs/regras-edicao.md\`$" && echo "$P0" | grep -qE "^- \`docs/proibido-fazer.md\`$" && echo "$P0" | grep -q "docs/multi-agente-recepcionista.md" && echo PASS</automated>
  </verify>
  <done>`.claude/agents/docs-analyzer.md` Passo 0 lê incondicionalmente as 3 primeiras regras (`client/CLAUDE.md`/`CLAUDE.md` fallback, `docs/regras-edicao.md`, `docs/proibido-fazer.md`, texto idêntico ao original) e lê `docs/multi-agente-recepcionista.md` apenas quando `<modo>=multi`, fraseado como referência antecipada à entrada; nenhuma outra seção do arquivo foi tocada; mudança commitada isoladamente (`.claude/agents/docs-analyzer.md` apenas, sem tocar `client/CLAUDE.md`).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A — mudança estática de prompt | Este plano edita apenas texto instrucional em dois arquivos `.md` interpretados pelo Claude Code (slash command e subagente). Não introduz nem altera código executável, endpoint, entrada de usuário não confiável, ou lógica de hook em shell. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-quick260716on3-01 | Tampering | `.claude/commands/ei-ajustes.md` (remoção da nota de fallback) | low | accept | Remoção cirúrgica de um bloco textual bem delimitado, revisada via diff antes do commit e confirmada por `<verify>` automatizado (contagem exata de D-17/D-09 remanescentes + presença dos headings HOOK-02/HOOK-02b/Mapeamento dos ícones intactos); nenhuma lógica de hook shell (`post-ajustes-fanout.sh`) é tocada, então o protocolo sentinela `<ei-ajustes-round>`/`<ei-ajustes-round-consumed>` e o anti-loop `stop_hook_active` permanecem funcionalmente inalterados. |
| T-quick260716on3-02 | Information Disclosure (inverso — perda de sinal) | `.claude/commands/ei-ajustes.md` (nota de fallback removida) | low | accept | A nota removida era voltada a um detalhe de implementação interna (hook não disparou), não a um erro de negócio; a documentação técnica HOOK-02/HOOK-02b (explicando que fallback é o estado padrão) permanece intacta para quem precisar depurar o pipeline via leitura direta do arquivo-fonte — só a superfície exposta ao usuário leigo via Apresentação final muda. |
| T-quick260716on3-03 | Tampering | `.claude/agents/docs-analyzer.md` (condicional de leitura em Passo 0) | low | accept | A condicional é fraseada seguindo o padrão já existente no arquivo (REGRA #0 e `<fluxo_de_analise>` item 6) e verificada automaticamente (bullets incondicionais intactos + referência a `<modo>` presente no bloco); em `<modo>=multi` a leitura de `docs/multi-agente-recepcionista.md` continua ocorrendo normalmente, preservando a correção do bug-âncora de personificação pós-transferência documentado no próprio arquivo (Exemplo 3). |

Nenhuma task de instalação via npm/pip/cargo faz parte deste plano — o Package Legitimacy Gate (T-{phase}-SC) não se aplica.
</threat_model>

<verification>
- `grep -c "modo fallback" .claude/commands/ei-ajustes.md` retorna `0` — a nota de fallback voltada ao usuário final desapareceu de ambas as ocorrências (Passo 6 / Apresentação final estendida).
- Contagem de `D-17` em `.claude/commands/ei-ajustes.md` é `3` (só as ocorrências da regra de navegação "Voltar"); contagem de `D-09` é `6` (só as ocorrências do bug de contrato de confiança do analyzer, incluindo o gate de reformulação, e do parsing de `<resultado>`) — nenhuma referência dangling.
- A explicação técnica HOOK-02/HOOK-02b ("fallback é o ESTADO PADRÃO") e o bloco "Apresentação final estendida" / "Mapeamento dos ícones" permanecem presentes e funcionalmente inalterados em `.claude/commands/ei-ajustes.md`.
- `.claude/agents/docs-analyzer.md` Passo 0 lê `docs/multi-agente-recepcionista.md` apenas sob uma condicional textual referenciando `<modo>=multi`; os outros 3 alvos de leitura (`client/CLAUDE.md`/`CLAUDE.md`, `docs/regras-edicao.md`, `docs/proibido-fazer.md`) permanecem incondicionais e com texto idêntico ao original.
- `git log` mostra 2 commits atômicos, um por arquivo, com as mensagens exatas especificadas nas tasks, sem trailer "Generated with Claude Code" / "Co-Authored-By".
- `git status` confirma que `client/CLAUDE.md` permanece no mesmo estado staged pré-existente (não tocado, não commitado, não revertido por este plano).
- `git diff` fica restrito a: as duas remoções em `.claude/commands/ei-ajustes.md` e a reescrita do 4º bullet do Passo 0 em `.claude/agents/docs-analyzer.md`. Nenhuma mudança em `package.json`, `manifest.json`, `CHANGELOG.md` ou qualquer outro arquivo.
</verification>

<success_criteria>
- Um operador de negócio leigo rodando `/ei-ajustes` em fallback puro (o caso comum) vê uma Apresentação final estendida limpa, sem menção a hooks, `.claude/settings.json` ou detalhes de implementação interna — só o resultado do ajuste em si.
- Implementadores que leem `.claude/commands/ei-ajustes.md` diretamente continuam tendo acesso completo à explicação técnica de por que fallback é o estado padrão (HOOK-02/HOOK-02b), sem nenhuma perda de documentação interna.
- Um ajuste single-agent via `/ei-ajustes` não desperdiça mais contexto do `docs-analyzer` lendo `docs/multi-agente-recepcionista.md`; um ajuste multi-agente continua lendo esse arquivo normalmente, sem regressão no bug-âncora de personificação pós-transferência já corrigido.
- Este plano não requer bump de versão, entrada de CHANGELOG, mudança em `manifest.json`, push para `origin`, ou PR — apenas 2 commits atômicos locais na branch `dev`, deixados para o usuário decidir quando incluir em um release futuro.
</success_criteria>

<output>
Criar `.planning/quick/260716-on3-two-ei-ajustes-ux-fixes-1-remove-the-int/260716-on3-SUMMARY.md` ao concluir, listando os 2 commits criados (hash + mensagem) e confirmando explicitamente que `client/CLAUDE.md` não foi tocado.
</output>