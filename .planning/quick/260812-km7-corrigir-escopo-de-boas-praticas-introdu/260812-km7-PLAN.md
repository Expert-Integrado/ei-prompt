---
phase: quick-260812-km7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/regras-edicao.md
  - docs/regras-validacao.md
  - .claude/agents/docs-analyzer.md
  - .claude/agents/docs-editor-conciso.md
  - .claude/agents/docs-reviewer.md
autonomous: true
requirements: [QUICK-260812-km7]

must_haves:
  truths:
    - "docs/regras-edicao.md's `### \\`<boas_praticas>\\` — seção obrigatória` subsection states the requirement applies SOMENTE a Scheduler.md — not to Orquestrador, Qualifier, Protractor, or Recepcionista."
    - "docs/regras-validacao.md's checkbox 2 in '## Validação de Seções Obrigatórias por Tipo de Agente' audits `<boas_praticas>` presence only for Scheduler.md files."
    - "docs-analyzer.md's `<secoes_faltantes>` detection (fluxo step 6, 'Regras do schema' bullet, `<regras>` bullet) applies the boas_praticas check ONLY when the file's agent type is Scheduler; Exemplo 3 (Recepcionista/Orquestrador.md, NOT Scheduler) now emits `<secoes_faltantes></secoes_faltantes>` empty instead of flagging `<boas_praticas>` as missing."
    - "docs-editor-conciso.md's 'SEÇÕES OBRIGATÓRIAS POR TIPO' section (a)/(b) and its final checklist bullet only require/add `<boas_praticas>` when the target file is Scheduler.md."
    - "docs-reviewer.md's checkbox 2 in '### 6. Seções Obrigatórias por Tipo de Agente' only audits `<boas_praticas>` in Scheduler.md files."
    - "The response-format tag-per-type map/logic (Orquestrador/Protractor/Recepcionista=`<response_format>`, Qualifier=`<formato_resposta>`, Scheduler=`<contrato_resposta>`, Follow-Up excluded) remains byte-identical across all 5 files — untouched by this correction."
    - "No file among the 5 claims `<boas_praticas>` is mandatory for Orquestrador, Qualifier, Protractor, or Recepcionista specifically."
    - "Nenhuma edição toca modelo/*.md nem .claude/agents/client-scaffold-fill.md."
  artifacts:
    - docs/regras-edicao.md
    - docs/regras-validacao.md
    - .claude/agents/docs-analyzer.md
    - .claude/agents/docs-editor-conciso.md
    - .claude/agents/docs-reviewer.md
  key_links:
    - "docs/regras-edicao.md é a fonte da verdade lida no Passo 0 de docs-analyzer.md e docs-editor-conciso.md — a regra corrigida de `<boas_praticas>` (Scheduler-only) precisa estar consistente lá e apenas REFERENCIADA (não reafirmada mais ampla) nos 2 arquivos de agente."
    - "docs/regras-validacao.md é a fonte da verdade lida no Passo 0 de docs-reviewer.md — o checkbox corrigido vive lá."
    - "O mapa tag-por-tipo de formato de resposta (decisão 1 de 260812-k2h) é uma decisão INDEPENDENTE que não muda nesta correção — nenhuma edição desta plan deve tocar essas linhas."
---

<objective>
Corrigir o escopo de `<boas_praticas>` introduzido pela quick task 260812-k2h: a regra hoje diz que `<boas_praticas>` é obrigatória em TODOS os 5 tipos de agente (Orquestrador, Qualifier, Scheduler, Protractor, Recepcionista). Isso está errado — `<boas_praticas>` só existe de fato em `modelo/Scheduler.md` hoje, então a obrigatoriedade deve valer SOMENTE para arquivos do tipo Scheduler. Este plano estreita essa regra nos mesmos 5 arquivos tocados por 260812-k2h, sem alterar em nada o mapa de tag de formato de resposta por tipo (decisão 1 de 260812-k2h, que permanece correta e intocada).

Purpose: um `docs-analyzer`/`docs-editor-conciso`/`docs-reviewer` que hoje (erroneamente) exige/adiciona/audita `<boas_praticas>` em Orquestrador, Qualifier, Protractor e Recepcionista vai gerar diffs incorretos em clientes reais — inventando ou reclamando de uma seção que nunca existiu nesses tipos. Corrigir a fonte da verdade e os 3 agentes fecha esse falso positivo antes que ele produza edição real.

Output: `<boas_praticas>` — seção obrigatória` escopo corrigido para Scheduler-only em `docs/regras-edicao.md`, `docs/regras-validacao.md`, `.claude/agents/docs-analyzer.md` (incluindo reversão do Exemplo 3), `.claude/agents/docs-editor-conciso.md`, `.claude/agents/docs-reviewer.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md

Fatos já levantados nesta sessão — NÃO re-descobrir, NÃO reabrir a decisão:

1. 260812-k2h (commits 7ee80af, 4a3d91e, 0c352e4) introduziu DUAS mudanças nos mesmos 5 arquivos: (i) mapa de tag de formato de resposta por tipo — CORRETO, INTOCADO por este plano; (ii) regra de `<boas_praticas>` obrigatória nos 5 tipos — ERRADA, é isso que este plano corrige.
2. `<boas_praticas>` só existe de fato em `modelo/Scheduler.md` (~linhas 298-306) — nenhum outro template tem essa seção. A obrigatoriedade deve valer SOMENTE para arquivos do tipo Scheduler (templates `Scheduler.md` E cópias de cliente desse tipo).
3. O texto EXATO atualmente em cada arquivo (lido nesta sessão, não de memória) está listado nas actions de cada task abaixo — usar esse texto literal como `old_string`, não paráfrase.
4. `modelo/*.md` e `.claude/agents/client-scaffold-fill.md` continuam fora de escopo — nenhuma task toca neles.
5. `.claude/commands/ei-ajustes.md` também fora de escopo (não foi tocado por 260812-k2h nem por este plano).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estreitar a regra de boas_praticas para Scheduler-only na fonte da verdade e no docs-analyzer</name>
  <files>docs/regras-edicao.md, docs/regras-validacao.md, .claude/agents/docs-analyzer.md</files>
  <action>
Corrigir 6 pontos de edição, todos ESTREITANDO (nunca ampliando) o escopo de `<boas_praticas>` para Scheduler-only. O mapa de tag de formato de resposta por tipo (response_format/formato_resposta/contrato_resposta) NÃO muda em nenhum destes pontos — editar SOMENTE o texto relativo a `<boas_praticas>`.

PARTE A — `docs/regras-edicao.md`: dentro da seção "## Seções Obrigatórias por Tipo de Agente", substituir o bloco completo do subtítulo "### `<boas_praticas>` — seção obrigatória" (o parágrafo que começa "`<boas_praticas>` passa a ser OBRIGATÓRIA em Orquestrador, Qualifier, Scheduler, Protractor e Recepcionista..." e o parágrafo seguinte "**Conteúdo mínimo ao adicionar:**...") pelo texto corrigido:

- Primeiro parágrafo: `<boas_praticas>` é obrigatória SOMENTE em `Scheduler.md` — o único tipo cujo `modelo/` realmente tem essa seção hoje (`modelo/Scheduler.md`). Os demais 4 tipos (Orquestrador, Qualifier, Protractor, Recepcionista) NÃO são obrigados a ter `<boas_praticas>` — não aplicar self-healing/adição preguiçosa neles. Quando `/ei-ajustes` tocar um `Scheduler.md` de cliente que não tenha a seção, adicioná-la de forma preguiçosa (self-healing) no mesmo despacho, nunca retroativa em `modelo/*.md`.
- Segundo parágrafo (conteúdo mínimo, agora explicitamente escopado a Scheduler): manter a regra de sintetizar 2 a 4 bullets diretos a partir de regras JÁ declaradas em outras seções do MESMO arquivo (`regras_gerais`, `regras_protractor`, `contrato_resposta` etc.), nunca inventar regra nova — mas trocar a proibição de "copiar o conteúdo específico de agenda do Scheduler para outro tipo de agente" (não faz mais sentido, já que só há um tipo) por: nunca copiar literalmente o `<boas_praticas>` de `modelo/Scheduler.md` — o conteúdo deve ser sintetizado a partir do `Scheduler.md` daquele cliente específico.

Não tocar em mais nada dessa seção (o subtítulo "### Mapa de tag de formato de resposta por tipo de agente" com a tabela de 6 linhas, o subtítulo "### Tag de formato de resposta totalmente ausente", e a frase de escopo inicial da seção ficam byte-idênticos).

PARTE B — `docs/regras-validacao.md`: na seção "## Validação de Seções Obrigatórias por Tipo de Agente", substituir SOMENTE o checkbox 2 — o texto exato hoje é:
"- [ ] `<boas_praticas>` está presente no arquivo? (obrigatória em Orquestrador, Qualifier, Scheduler, Protractor, Recepcionista; Follow-Up fora desta checagem — ver `docs/regras-edicao.md`)"
Trocar por:
"- [ ] `<boas_praticas>` está presente no arquivo? (obrigatória SOMENTE em Scheduler.md — o único tipo cujo `modelo/` tem essa seção hoje; demais tipos fora desta checagem — ver `docs/regras-edicao.md`)"
NÃO tocar o checkbox 1 (mapa de tag de formato de resposta) nem qualquer outra linha do arquivo.

PARTE C — `.claude/agents/docs-analyzer.md` (4 pontos de edição, todos estreitando o escopo de boas_praticas — a checagem de tag de formato de resposta continua igual para todos os tipos):

1. No step 6 de `<fluxo_de_analise>` ("**Checar seções obrigatórias:**..."), inserir a condição "SOMENTE quando o arquivo for do tipo Scheduler" junto ao item (b) `<boas_praticas>`, mantendo o item (a) (tag de formato de resposta) sem essa condição — ele continua valendo para todos os tipos (exceto Follow-Up, já excluído).

2. No bullet de "Regras do schema" que começa com "- `<secoes_faltantes>` fica DENTRO de cada `<arquivo>`...", inserir a mesma condição "SOMENTE quando o arquivo for do tipo Scheduler" imediatamente antes de `<boas_praticas>` na enumeração das duas tags candidatas a faltante.

3. No bullet de `<regras>` que começa com "- SEMPRE preencher `<secoes_faltantes>`...", inserir a mesma condição "SOMENTE quando o arquivo for do tipo Scheduler" imediatamente antes de `<boas_praticas>`.

4. **Exemplo 3** (multi-agente, personificação pós-transferência, target = `Brunno Brandi/Recepcionista/Orquestrador.md`): reverter a linha `<secoes_faltantes><boas_praticas></secoes_faltantes>` (populada) para `<secoes_faltantes></secoes_faltantes>` (vazia) — Recepcionista NÃO é Scheduler, então sob a regra corrigida nada está faltando nesse exemplo (idêntico ao padrão já usado no Exemplo 1).

Não alterar o mapa de tag de formato de resposta em nenhum dos 4 pontos, não alterar `<conhecimento_dos_papeis>`, não alterar Exemplo 1 nem Exemplo 2, não alterar mais nada no arquivo além do listado.
  </action>
  <verify>
    <automated>E="docs/regras-edicao.md"; V="docs/regras-validacao.md"; A=".claude/agents/docs-analyzer.md"; test "$(grep -Fc 'obrigatória SOMENTE em' "$E")" -ge 1 && ! grep -Fq 'OBRIGATÓRIA em Orquestrador, Qualifier, Scheduler, Protractor e Recepcionista' "$E" && test "$(grep -Fc 'obrigatória SOMENTE em Scheduler.md' "$V")" = "1" && ! grep -Fq 'obrigatória em Orquestrador, Qualifier, Scheduler, Protractor, Recepcionista' "$V" && test "$(grep -Fc 'SOMENTE quando o arquivo for do tipo Scheduler' "$A")" = "3" && test "$(grep -Fc '<secoes_faltantes><boas_praticas></secoes_faltantes>' "$A")" = "0" && test "$(grep -Fc '<secoes_faltantes></secoes_faltantes>' "$A")" = "2" && test "$(grep -Fc '<response_format>' "$E")" -ge 3 && test "$(grep -Fc '<contrato_resposta>' "$V")" -ge 1 && echo TASK1-OK</automated>
  </verify>
  <done>docs/regras-edicao.md e docs/regras-validacao.md declaram `<boas_praticas>` obrigatória SOMENTE em Scheduler.md (não mais nos 5 tipos); docs-analyzer.md aplica a checagem de boas_praticas apenas quando o tipo do arquivo é Scheduler nos 3 pontos (fluxo step 6, Regras do schema, `<regras>`), e o Exemplo 3 volta a ter `<secoes_faltantes></secoes_faltantes>` vazia; o mapa de tag de formato de resposta permanece intocado nos 3 arquivos.</done>
</task>

<task type="auto">
  <name>Task 2: Estreitar a regra de boas_praticas para Scheduler-only no docs-editor-conciso e no docs-reviewer</name>
  <files>.claude/agents/docs-editor-conciso.md, .claude/agents/docs-reviewer.md</files>
  <action>
Consome a mesma fonte da verdade corrigida na Task 1. Corrigir 3 pontos de edição, todos estreitando o escopo de `<boas_praticas>` — a checagem de tag de formato de resposta continua igual para todos os tipos em ambos os arquivos.

PARTE A — `.claude/agents/docs-editor-conciso.md` (2 pontos de edição, dentro de "## SEÇÕES OBRIGATÓRIAS POR TIPO (detectar e completar)" e no checklist final):

1. No item (a) dessa seção ("Antes de aplicar o ajuste pedido, verificar... contém a tag de formato de resposta correta para o tipo do agente (...) e `<boas_praticas>`."), inserir a condição "SOMENTE quando o arquivo for Scheduler.md" imediatamente antes de `<boas_praticas>` — a checagem da tag de formato de resposta (Orquestrador/Protractor/Recepcionista/Qualifier/Scheduler) permanece sem essa condição, vale para todos os tipos.

2. No item (b) da mesma seção ("Se alguma das duas estiver ausente, ADICIONAR..."), trocar "alguma das duas" por "alguma das seções aplicáveis" — já que para arquivos que não são Scheduler.md só há UMA checagem aplicável (a tag de formato de resposta), não duas.

Não alterar os itens (c) e (d) dessa seção, não alterar a REGRA INVIOLÁVEL (`<response_format>` intocável) logo acima, não alterar "## FLUXO DE TRABALHO".

3. No bullet do "## CHECKLIST ANTES DE ENTREGAR" que hoje diz "- [ ] Seção de formato de resposta (tag correta do tipo) e `<boas_praticas>` estão presentes (adicionadas se faltavam)?", trocar por "- [ ] Seção de formato de resposta (tag correta do tipo) e, se o arquivo for Scheduler.md, `<boas_praticas>` estão presentes (adicionadas se faltavam)?".

PARTE B — `.claude/agents/docs-reviewer.md`: na subseção "### 6. Seções Obrigatórias por Tipo de Agente", substituir SOMENTE o checkbox 2 — o texto exato hoje é:
"- [ ] `<boas_praticas>` está presente no arquivo? (obrigatória em Orquestrador, Qualifier, Scheduler, Protractor, Recepcionista — ver `docs/regras-edicao.md`; Follow-Up fora desta checagem)"
Trocar por:
"- [ ] `<boas_praticas>` está presente no arquivo? (obrigatória SOMENTE em Scheduler.md — ver `docs/regras-edicao.md`; demais tipos fora desta checagem)"
NÃO tocar o checkbox 1 dessa subseção (mapa de tag de formato de resposta), nem o item 1 existente do checklist geral ("Campos do `<formato_resposta>` inalterados (sem novos campos)?"), nem a linha de "## Regras" que já cita "(e) ausência da tag de formato de resposta do tipo ou de `<boas_praticas>`" — essa linha é genérica (não afirma escopo específico) e fica como está.

Não alterar mais nada nesses 2 arquivos além do listado acima. Não tocar `modelo/*.md` nem `.claude/agents/client-scaffold-fill.md`.
  </action>
  <verify>
    <automated>ED=".claude/agents/docs-editor-conciso.md"; R=".claude/agents/docs-reviewer.md"; test "$(grep -Fc 'SOMENTE quando o arquivo for Scheduler.md' "$ED")" = "1" && test "$(grep -Fc 'se o arquivo for Scheduler.md' "$ED")" = "1" && ! grep -Fq 'obrigatória em Orquestrador, Qualifier, Scheduler, Protractor, Recepcionista' "$ED" && test "$(grep -Fc '**NUNCA modificar `<response_format>`** — Esta seção é intocável. Preserve-a exatamente como está, caractere por caractere.' "$ED")" = "1" && test "$(grep -Fc '<contrato_resposta>' "$ED")" -ge 1 && test "$(grep -Fc 'obrigatória SOMENTE em Scheduler.md' "$R")" = "1" && ! grep -Fq 'obrigatória em Orquestrador, Qualifier, Scheduler, Protractor, Recepcionista' "$R" && test "$(grep -Fc 'Campos do `<formato_resposta>` inalterados (sem novos campos)?' "$R")" = "1" && test "$(grep -Fc '<contrato_resposta>' "$R")" -ge 1 && echo TASK2-OK</automated>
  </verify>
  <done>docs-editor-conciso.md exige/adiciona `<boas_praticas>` somente quando o arquivo é Scheduler.md (item (a)/(b) da seção "SEÇÕES OBRIGATÓRIAS POR TIPO" e checklist final corrigidos), com a REGRA INVIOLÁVEL e a checagem de tag de formato de resposta intocadas; docs-reviewer.md audita `<boas_praticas>` somente em Scheduler.md no checkbox 2 da subseção 6, com o checkbox 1 e o item 1 do checklist geral intocados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `docs/regras-edicao.md` + `docs/regras-validacao.md` → 3 agentes (`docs-analyzer`, `docs-editor-conciso`, `docs-reviewer`) | Fonte da verdade lida no Passo 0 de cada agente; se o escopo de `<boas_praticas>` ficar inconsistente entre a fonte e os agentes, o falso positivo (exigir/reclamar boas_praticas fora de Scheduler) volta a se propagar para arquivos de cliente. |
| `docs-analyzer` (Exemplo 3, `<secoes_faltantes>`) → humano leigo que aprova ajuste via `AskUserQuestion` | Um `<secoes_faltantes>` incorreto (apontando boas_praticas faltando em Recepcionista) poderia levar a uma correção indevida sendo proposta/aplicada num cliente real. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick260812km7-01 | Tampering | `.claude/agents/docs-analyzer.md` (Exemplo 3, `<secoes_faltantes>`) | medium | mitigate | Task 1 reverte o Exemplo 3 para `<secoes_faltantes></secoes_faltantes>` vazia e escopa as 3 checagens de fluxo/schema/regras a "SOMENTE quando o arquivo for do tipo Scheduler"; gate de verificação da Task 1 confirma zero ocorrências da forma populada e exatamente 2 ocorrências da forma vazia (Exemplo 1 + Exemplo 3). |
| T-quick260812km7-02 | Tampering | `docs/regras-edicao.md` + `docs/regras-validacao.md` (fonte da verdade) | medium | mitigate | Task 1 corrige o texto da regra de `<boas_praticas>` para Scheduler-only nos 2 docs; gates de verificação confirmam ausência da frase antiga ("obrigatória em Orquestrador, Qualifier, Scheduler, Protractor, Recepcionista") em ambos. |
| T-quick260812km7-03 | Tampering | `.claude/agents/docs-editor-conciso.md` (self-healing de `<boas_praticas>`) | low | mitigate | Sem a correção, o editor adicionaria `<boas_praticas>` sintética em arquivos que nunca tiveram essa seção (Orquestrador/Qualifier/Protractor/Recepcionista) na primeira vez que `/ei-ajustes` tocasse esses arquivos. Task 2 escopa o item (a)/(b) e o checklist a Scheduler.md apenas. |
| T-quick260812km7-04 | Tampering | Mapa de tag de formato de resposta por tipo (decisão 1 de 260812-k2h) | low | accept | Risco de regressão acidental do escopo de `<boas_praticas>` vazando para o mapa de formato de resposta é mitigado por old_string cirúrgicos que isolam só o texto de boas_praticas; gates de verificação de ambas as tasks confirmam presença de `<contrato_resposta>`/`<response_format>`/`<formato_resposta>` intactos nos 5 arquivos. |
| T-quick260812km7-SC | Tampering | npm/pip/cargo installs | low | accept | Nenhuma task instala pacote — plano só edita arquivos `.md` (2 docs + 3 agentes). Projeto permanece zero-dependency; sem superfície de supply chain nesta correção. |
</threat_model>

<verification>
Rodar na raiz do repo depois das 2 tasks:

1. Nenhum dos 5 arquivos ainda afirma `<boas_praticas>` obrigatória para os 4 tipos não-Scheduler:
   `for f in docs/regras-edicao.md docs/regras-validacao.md .claude/agents/docs-analyzer.md .claude/agents/docs-editor-conciso.md .claude/agents/docs-reviewer.md; do grep -Fq 'obrigatória em Orquestrador, Qualifier, Scheduler, Protractor, Recepcionista' "$f" && { echo "AINDA ERRADO em $f"; exit 1; }; grep -Fq 'OBRIGATÓRIA em Orquestrador, Qualifier, Scheduler, Protractor e Recepcionista' "$f" && { echo "AINDA ERRADO em $f"; exit 1; }; done; echo SCOPE-OK`

2. Scheduler-only presente nos 5 arquivos (uma variante ou outra da frase de escopo):
   `for f in docs/regras-edicao.md docs/regras-validacao.md .claude/agents/docs-analyzer.md .claude/agents/docs-editor-conciso.md .claude/agents/docs-reviewer.md; do grep -Fqi 'scheduler' "$f" || { echo "FALTA em $f"; exit 1; }; done; echo SCHEDULER-PRESENTE-OK`

3. Exemplo 3 do docs-analyzer.md revertido para vazio, Exemplo 1 continua vazio:
   `test "$(grep -Fc '<secoes_faltantes><boas_praticas></secoes_faltantes>' .claude/agents/docs-analyzer.md)" = "0" && test "$(grep -Fc '<secoes_faltantes></secoes_faltantes>' .claude/agents/docs-analyzer.md)" = "2" && echo EXEMPLO3-OK`

4. Mapa de tag de formato de resposta (decisão 1, intocada) presente onde já estava: `docs/regras-edicao.md`, `docs/regras-validacao.md`, `docs-editor-conciso.md` e `docs-reviewer.md` repetem a tag literal `<contrato_resposta>`; `docs-analyzer.md` NUNCA repetiu essa tag — ele só referencia o mapa por ponteiro a `docs/regras-edicao.md` (isso não muda neste plano):
   `for f in docs/regras-edicao.md docs/regras-validacao.md .claude/agents/docs-editor-conciso.md .claude/agents/docs-reviewer.md; do grep -Fq '<contrato_resposta>' "$f" || { echo "MAPA QUEBRADO em $f"; exit 1; }; done; grep -Fq 'docs/regras-edicao.md' .claude/agents/docs-analyzer.md || { echo "PONTEIRO QUEBRADO em docs-analyzer.md"; exit 1; }; echo MAPA-INTACTO-OK`

5. Zonas fora de escopo intocadas:
   `test -z "$(git diff --name-only -- modelo/ .claude/agents/client-scaffold-fill.md .claude/commands/ei-ajustes.md)" && echo ESCOPO-OK`

6. Diff total confinado aos 5 arquivos esperados:
   `git diff --name-only | grep -v '^\.planning/' | sort` deve conter exatamente `docs/regras-edicao.md`, `docs/regras-validacao.md`, `.claude/agents/docs-analyzer.md`, `.claude/agents/docs-editor-conciso.md`, `.claude/agents/docs-reviewer.md`.
</verification>

<success_criteria>
- [ ] `<boas_praticas>` obrigatória SOMENTE para Scheduler.md em `docs/regras-edicao.md` e `docs/regras-validacao.md` (checkbox 2 corrigido).
- [ ] `docs-analyzer.md` aplica a checagem de boas_praticas apenas a arquivos do tipo Scheduler (fluxo step 6, Regras do schema, `<regras>`); Exemplo 3 revertido para `<secoes_faltantes></secoes_faltantes>` vazia.
- [ ] `docs-editor-conciso.md` só exige/adiciona `<boas_praticas>` quando o arquivo é Scheduler.md; REGRA INVIOLÁVEL intocada.
- [ ] `docs-reviewer.md` só audita `<boas_praticas>` em Scheduler.md no checkbox 2 da subseção 6; checkbox 1 e checklist geral intocados.
- [ ] Mapa de tag de formato de resposta por tipo (decisão 1 de 260812-k2h) byte-idêntico nos 5 arquivos.
- [ ] `modelo/*.md`, `.claude/agents/client-scaffold-fill.md` e `.claude/commands/ei-ajustes.md` com diff vazio (fora de escopo).
- [ ] Diff confinado aos 5 arquivos esperados.
</success_criteria>

<output>
Create `.planning/quick/260812-km7-corrigir-escopo-de-boas-praticas-introdu/260812-km7-SUMMARY.md` when done
</output>