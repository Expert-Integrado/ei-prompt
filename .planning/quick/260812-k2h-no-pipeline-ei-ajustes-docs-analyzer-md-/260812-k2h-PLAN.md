---
phase: quick-260812-k2h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/regras-edicao.md
  - docs/regras-validacao.md
  - .claude/agents/docs-analyzer.md
  - .claude/agents/docs-editor-conciso.md
  - .claude/agents/docs-reviewer.md
autonomous: true
requirements: [QUICK-260812-k2h]

must_haves:
  truths:
    - "Para os 5 tipos de agente cobertos (Orquestrador, Qualifier, Scheduler, Protractor, Recepcionista), docs/regras-edicao.md e docs/regras-validacao.md declaram a MESMA tag XML esperada para a seção de formato de resposta daquele tipo (Orquestrador/Protractor/Recepcionista=<response_format>, Qualifier=<formato_resposta>, Scheduler=<contrato_resposta>); Follow-Up.md fica explicitamente fora desta checagem em ambos os docs."
    - "`<boas_praticas>` é documentada como seção obrigatória para os mesmos 5 tipos em docs/regras-edicao.md, com regra de conteúdo mínimo para quando precisar ser adicionada (sintetizado do próprio arquivo, nunca copiado do Scheduler, nunca inventado)."
    - "docs-analyzer.md preenche `<secoes_faltantes>` dentro de cada `<arquivo>` de sua saída estruturada (vazio se nada faltar), usando apenas tags do mapa de docs/regras-edicao.md — nunca uma tag inventada — e nunca aplica a checagem a Follow-Up.md."
    - "docs-editor-conciso.md detecta e adiciona a(s) seção(ões) mínima(s) faltante(s) no MESMO despacho do ajuste pedido pelo usuário — não como aviso isolado nem como um passe separado — e essa checagem funciona mesmo que o campo `<secoes_faltantes>` do analyzer não tenha sido propagado até ele, porque ele verifica de forma independente ao ler o arquivo completo (Passo 2 do próprio fluxo)."
    - "A REGRA INVIOLÁVEL pré-existente de docs-editor-conciso.md ('NUNCA modificar `<response_format>`') permanece byte-idêntica — a nova checagem é aditiva."
    - "docs-reviewer.md tem um item de checklist novo e DISTINTO (não misturado com o checklist existente 'Campos do `<formato_resposta>` inalterados (sem novos campos)?') que audita a presença das duas seções, e essa checagem existente permanece byte-idêntica."
    - "Nenhuma edição toca modelo/*.md nem .claude/agents/client-scaffold-fill.md."
  artifacts:
    - docs/regras-edicao.md
    - docs/regras-validacao.md
    - .claude/agents/docs-analyzer.md
    - .claude/agents/docs-editor-conciso.md
    - .claude/agents/docs-reviewer.md
  key_links:
    - "docs/regras-edicao.md é a fonte da verdade lida no Passo 0 de docs-analyzer.md e docs-editor-conciso.md — a tabela de mapeamento tag-por-tipo e a regra de `<boas_praticas>` obrigatória vivem SÓ lá, referenciadas (não duplicadas) nos 3 arquivos de agente."
    - "docs/regras-validacao.md é a fonte da verdade lida no Passo 0 de docs-reviewer.md — o novo item de checklist vive lá."
    - "docs-editor-conciso.md NÃO depende do comando /ei-ajustes propagar o campo `<secoes_faltantes>` do docs-analyzer (esse comando está fora de escopo deste plano) — a detecção é autocontida, porque o editor já lê o arquivo alvo completo antes de editar."
---

<objective>
Fechar o gap de auditoria automática do pipeline `/ei-ajustes`: hoje a presença da seção de formato de resposta (nome de tag correto varia por tipo de agente) e da seção `<boas_praticas>` (obrigatória a partir de agora, hoje só existe ad hoc em `Scheduler.md`) só é checada se um humano lembrar de olhar a checklist manual. Este plano codifica o mapeamento tag-por-tipo e a regra de `<boas_praticas>` em `docs/regras-edicao.md` e `docs/regras-validacao.md` (fonte da verdade já lida pelos 3 agentes no Passo 0), e ensina `docs-analyzer.md` a detectar/sinalizar, `docs-editor-conciso.md` a detectar/completar no mesmo despacho, e `docs-reviewer.md` a auditar — sem duplicar prosa nos 3 arquivos de agente.

Purpose: fecha um ponto cego real encontrado em pesquisa: `docs/regras-edicao.md`, `docs/regras-validacao.md`, `docs/proibido-fazer.md`, `docs-analyzer.md` e `docs-reviewer.md` citam genericamente `<formato_resposta>`, mas só `Qualifier.md` usa esse nome literal — `Orquestrador.md`/`Protractor.md`/Recepcionista usam `<response_format>` e `Scheduler.md` usa `<contrato_resposta>`. Sem uma checagem por tipo, um ajuste malfeito pode deixar um arquivo de cliente sem seção de formato de resposta (ou sem `<boas_praticas>`) sem que ninguém perceba.

Output: mapeamento tag-por-tipo + regra de `<boas_praticas>` codificados em 2 docs; `<secoes_faltantes>` novo em `docs-analyzer.md`; detecção+correção aditiva em `docs-editor-conciso.md`; item de checklist novo e distinto em `docs-reviewer.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/proibido-fazer.md

Fatos já levantados nesta sessão — NÃO re-descobrir, NÃO reabrir a decisão:

1. Mapa real de tag de formato de resposta por arquivo de template (confirmado por leitura de `modelo/*.md`): `Orquestrador.md`→`<response_format>`, `Qualifier.md`→`<formato_resposta>`, `Scheduler.md`→`<contrato_resposta>`, `Protractor.md`→`<response_format>`, `Recepcionista/Orquestrador.md` (multi-agente, mesmo arquivo `Orquestrador.md` dentro da pasta `Recepcionista/`)→`<response_format>`. `Follow-Up.md` NÃO tem tag de formato de resposta — está fora desta checagem por completo, não inventar uma para ele.
2. `<boas_praticas>` hoje só existe em `modelo/Scheduler.md` (bloco real, ~linhas 298-306). Vira seção obrigatória nos mesmos 5 tipos acima a partir de agora, aplicada de forma preguiçosa (self-healing) quando `/ei-ajustes` tocar um arquivo de cliente — NÃO é edição retroativa em `modelo/*.md` (fora de escopo).
3. `modelo/*.md` e `.claude/agents/client-scaffold-fill.md` estão EXPLICITAMENTE fora de escopo — nenhuma task deste plano os toca.
4. `.claude/commands/ei-ajustes.md` (o comando que despacha o `docs-editor-conciso`) também está fora de escopo deste plano — por isso a detecção do editor precisa ser AUTOCONTIDA (não pode depender de um campo novo do analyzer chegar através do template de dispatch do comando, que este plano não edita).
5. O bullet existente `docs/regras-validacao.md` linha 10 ("`<formato_resposta>` mantém apenas os campos originais (nenhum campo novo)?") e a REGRA INVIOLÁVEL de `docs-editor-conciso.md` ("NUNCA modificar `<response_format>`") ficam BYTE-IDÊNTICOS — este plano só ADICIONA checagens novas e distintas, nunca substitui ou afrouxa as existentes.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Codificar o mapa tag-por-tipo + regra de boas_praticas na fonte da verdade e ligar a detecção no docs-analyzer</name>
  <files>docs/regras-edicao.md, docs/regras-validacao.md, .claude/agents/docs-analyzer.md</files>
  <action>
Esta é a fatia tracer: cria a fonte da verdade (os 2 docs) e prova a detecção ponta-a-ponta no primeiro agente do pipeline (`docs-analyzer`, que roda ANTES de qualquer edição). As Tasks seguintes (Task 2) só consomem essa mesma fonte da verdade nos 2 agentes restantes.

PARTE A — `docs/regras-edicao.md`: adicionar uma seção nova IMEDIATAMENTE depois do bullet "- **Sempre incluir `<exemplos_resposta>`** com exemplos para cada cenário possível" (fim da seção "## Formato de Resposta de Agentes") e antes da linha em branco que precede "## Padrão de Ações no Campo `resume`". Inserir, como novo conteúdo (heading `##`, não alterar nada que já existe acima):

Título da seção nova: "## Seções Obrigatórias por Tipo de Agente (formato de resposta + `boas_praticas`)"

Corpo (nesta ordem):
1. Uma frase de escopo: aplica-se aos arquivos de CLIENTE ajustados via `/ei-ajustes` — não retroage sobre `modelo/*.md` (templates já nascem certos; esta é a política aplicada no momento do ajuste, não uma edição retroativa).
2. Um subtítulo "### Mapa de tag de formato de resposta por tipo de agente" com uma frase dizendo que cada tipo usa um NOME DIFERENTE de tag — não existe "o" nome universal correto — seguida de uma tabela markdown com colunas "Arquivo do cliente" e "Tag XML da seção de formato de resposta", com estas 6 linhas: `Orquestrador.md` → `<response_format>`; `Qualifier.md` → `<formato_resposta>`; `Scheduler.md` → `<contrato_resposta>`; `Protractor.md` → `<response_format>`; `Recepcionista/Orquestrador.md` (multi-agente) → `<response_format>`; `Follow-Up.md` → texto "Fora desta checagem — nunca teve tag de formato de resposta; não inventar uma". Depois da tabela, uma frase reforçando: ao verificar a seção de formato de resposta de um arquivo, usar SEMPRE a tag correta desta tabela para o tipo daquele arquivo — nunca uma lista genérica de "qualquer um destes 3 nomes".
3. Um subtítulo "### `<boas_praticas>` — seção obrigatória" com: (a) a regra de que `<boas_praticas>` passa a ser OBRIGATÓRIA em Orquestrador, Qualifier, Scheduler, Protractor e Recepcionista (os mesmos 5 tipos da tabela); hoje só existe de fato em `modelo/Scheduler.md`; para os demais é adicionada de forma preguiçosa (self-healing) no momento em que `/ei-ajustes` tocar o arquivo, nunca retroativa em `modelo/*.md`; (b) a regra de conteúdo mínimo ao adicionar: 2 a 4 bullets diretos (sem justificativa, no mesmo estilo econômico do `<boas_praticas>` de `modelo/Scheduler.md`), SINTETIZADOS a partir de regras JÁ declaradas em outras seções do MESMO arquivo (`regras_gerais`, `regras_protractor`, `contrato_resposta` etc.) — nunca inventar regra nova, nunca copiar o conteúdo específico de agenda do Scheduler para outro tipo de agente.
4. Um subtítulo "### Tag de formato de resposta totalmente ausente" com a regra: se a tag esperada (mapa acima) estiver de fato ausente do arquivo do cliente — não com nome diferente, mas ausente por completo — restaurar o bloco inteiro copiando o conteúdo correspondente de `modelo/<Tipo>.md` (leitura apenas; `modelo/` continua read-only, nunca editado).

PARTE B — `docs/regras-validacao.md`: adicionar uma seção nova IMEDIATAMENTE depois do bullet "- [ ] `<exemplos_resposta>` cobre todos os cenários possíveis do `result`?" (fim de "## Checklist Pré-Commit de Prompt") e antes da linha em branco que precede "## Validação de Ações no Campo `resume`". NÃO tocar nenhum bullet existente dessa seção (em especial a linha "`<formato_resposta>` mantém apenas os campos originais (nenhum campo novo)?" fica intocada — esta seção nova é DISTINTA daquela checagem, não uma substituição).

Título da seção nova: "## Validação de Seções Obrigatórias por Tipo de Agente" com 2 checkboxes:
- Checkbox 1: a tag de formato de resposta correta para o tipo do agente está presente, citando o mapa (Orquestrador/Protractor/Recepcionista=`<response_format>`, Qualifier=`<formato_resposta>`, Scheduler=`<contrato_resposta>`; Follow-Up fora desta checagem) e referenciando `docs/regras-edicao.md` para o mapa completo.
- Checkbox 2: `<boas_praticas>` está presente no arquivo, citando que é obrigatória em Orquestrador/Qualifier/Scheduler/Protractor/Recepcionista (Follow-Up fora) e referenciando `docs/regras-edicao.md`.

Também adicionar, ao final da seção "## Auditoria Automática" (depois do bullet "- Conformidade com esta checklist"), um bullet novo: "- Presença das seções obrigatórias por tipo de agente (formato de resposta + `<boas_praticas>`) — ver "## Validação de Seções Obrigatórias por Tipo de Agente"".

PARTE C — `.claude/agents/docs-analyzer.md` (7 pontos de edição, todos aditivos):

1. Em `<conhecimento_dos_papeis>`, adicionar uma linha nova IMEDIATAMENTE antes da tag de fechamento `</conhecimento_dos_papeis>` (depois do último bullet, o que termina em "ajustar a mensagem inicial do agente Y após a transferência"): "- **Seções obrigatórias por tipo (formato de resposta + `boas_praticas`):** mapa completo em `docs/regras-edicao.md` (lido no Passo 0) — não duplicar aqui."

2. Em `<fluxo_de_analise>`, substituir o step numerado 6 inteiro (o que hoje começa com "6. **Multi-agente:** se `<modo>` = `multi`, valide que TODOS os paths...") por DOIS steps: um novo step 6 "**Checar seções obrigatórias:** para cada arquivo que for compor `<arquivos>` (`<decisao>edit</decisao>`), verificar se contém (a) a tag de formato de resposta correta para o tipo daquele arquivo e (b) `<boas_praticas>` — mapa completo em `docs/regras-edicao.md` (Passo 0). Preencher `<secoes_faltantes>` daquele `<arquivo>` com a(s) tag(s) ausente(s) (vazio se nada faltar). NUNCA aplicar esta checagem a `Follow-Up.md` (nunca teve tag de formato de resposta)." seguido do step 7, que é o texto ORIGINAL do antigo step 6 renumerado para 7 sem nenhuma outra alteração de conteúdo.

3. No bloco de schema dentro de `<formato_resposta>` (o bloco cercado por crases triplas), dentro de CADA `<arquivo>` de exemplo do schema (o bloco que tem `<path>`, `<secao_tag>`, `<secao_descricao>`, `<justificativa>`), adicionar uma linha nova IMEDIATAMENTE depois da linha `<justificativa>1-2 linhas em PT-BR explicando por que esse arquivo + essa seção</justificativa>` e antes de `</arquivo>`: uma linha `<secoes_faltantes>` com um comentário inline explicando que fica vazia se nada faltar, e senão lista uma tag ausente por linha no MESMO formato de `<secao_tag>` (valor = a tag literal ausente).

4. Na seção "Regras do schema" (a lista de bullets logo depois do bloco de schema), adicionar um bullet novo IMEDIATAMENTE depois do bullet que começa com "- `<path>` é o caminho ABSOLUTO e LITERAL devolvido pelo Glob": "- `<secoes_faltantes>` fica DENTRO de cada `<arquivo>` (mesmo com `<decisao>edit</decisao>` + confiança alta) e lista, no formato de `<secao_tag>`, cada tag ausente entre: a tag de formato de resposta esperada para o tipo do arquivo (mapa em `docs/regras-edicao.md`) e `<boas_praticas>`. Vazia (ou ausente) quando nada faltar. NUNCA aplicar esta checagem a `Follow-Up.md`."

5. No Exemplo 1 (bug-âncora), dentro do bloco `<arquivo>` da "Saída esperada", adicionar `<secoes_faltantes></secoes_faltantes>` (vazia) IMEDIATAMENTE depois da linha `<justificativa>` e antes de `</arquivo>` — demonstra o caso feliz (nada faltando).

6. No Exemplo 3 (multi-agente, personificação pós-transferência), dentro do bloco `<arquivo>` da "Saída esperada", adicionar `<secoes_faltantes><boas_praticas></secoes_faltantes>` (preenchida) IMEDIATAMENTE depois da linha `<justificativa>` e antes de `</arquivo>` — demonstra o caso realista (Recepcionista/Orquestrador.md hoje não tem `<boas_praticas>` em `modelo/`, então qualquer cliente atual também não tem).

7. Na seção `<regras>` (perto do fim do arquivo), adicionar um bullet novo IMEDIATAMENTE depois do bullet "- NUNCA invente tag XML que não existe no arquivo do cliente — leia o conteúdo e use a tag literal.": "- SEMPRE preencher `<secoes_faltantes>` em cada `<arquivo>` da saída, mesmo vazia — verificar a tag de formato de resposta correta do tipo e `<boas_praticas>` (mapa em `docs/regras-edicao.md`). Exceto `Follow-Up.md`, que fica fora desta checagem."

Não altere mais nada nesses 3 arquivos além do listado acima. Não toque `modelo/*.md` nem `.claude/agents/client-scaffold-fill.md`.
  </action>
  <verify>
    <automated>E="docs/regras-edicao.md"; V="docs/regras-validacao.md"; A=".claude/agents/docs-analyzer.md"; test "$(grep -c '^## Seções Obrigatórias por Tipo de Agente' "$E")" = "1" && test "$(grep -Fc '<response_format>' "$E")" -ge 3 && test "$(grep -Fc '<formato_resposta>' "$E")" -ge 1 && test "$(grep -Fc '<contrato_resposta>' "$E")" -ge 1 && test "$(grep -Fc 'Follow-Up' "$E")" -ge 1 && test "$(grep -Fc '<boas_praticas>' "$E")" -ge 1 && test "$(grep -c '^## Validação de Seções Obrigatórias por Tipo de Agente' "$V")" = "1" && test "$(grep -Fc '<contrato_resposta>' "$V")" -ge 1 && test "$(grep -Fc '<formato_resposta>` mantém apenas os campos originais (nenhum campo novo)?' "$V")" = "1" && test "$(grep -Fc 'secoes_faltantes' "$A")" -ge 5 && test "$(grep -Fc 'Follow-Up' "$A")" -ge 1 && test "$(grep -Fc 'Checar seções obrigatórias' "$A")" = "1" && test "$(grep -c '^7\. \*\*Multi-agente' "$A")" = "1" && echo TASK1-OK</automated>
  </verify>
  <done>docs/regras-edicao.md e docs/regras-validacao.md têm a nova seção com o mapa tag-por-tipo completo (5 tipos + exclusão de Follow-Up) e a regra de `<boas_praticas>` obrigatória; docs-analyzer.md tem `<secoes_faltantes>` no schema, no step de fluxo (novo step 6, Multi-agente renumerado para 7), nas Regras do schema, em `<regras>`, e populado (vazio ou com conteúdo) nos Exemplos 1 e 3.</done>
</task>

<task type="auto">
  <name>Task 2: Ligar detecção+correção aditiva no docs-editor-conciso e checklist novo no docs-reviewer</name>
  <files>.claude/agents/docs-editor-conciso.md, .claude/agents/docs-reviewer.md</files>
  <action>
Consome a mesma fonte da verdade criada na Task 1 (`docs/regras-edicao.md` e `docs/regras-validacao.md`, já lidos no Passo 0 destes 2 agentes — nenhuma mudança de leitura necessária). NÃO depende do campo `<secoes_faltantes>` do `docs-analyzer` chegar via `/ei-ajustes` (fora de escopo) — a detecção é autocontida em cada agente.

PARTE A — `.claude/agents/docs-editor-conciso.md` (3 pontos de edição, todos aditivos):

1. Adicionar uma seção `##` nova IMEDIATAMENTE depois da linha "**NUNCA modificar `<response_format>`** — Esta seção é intocável. Preserve-a exatamente como está, caractere por caractere." (fim de "## REGRA INVIOLÁVEL") e antes da linha em branco que precede "## PRINCÍPIOS DE EDIÇÃO". NÃO altere a linha da REGRA INVIOLÁVEL — ela fica byte-idêntica.

Título da seção nova: "## SEÇÕES OBRIGATÓRIAS POR TIPO (detectar e completar)"

Corpo: (a) antes de aplicar o ajuste pedido, verificar (usando o mapa de `docs/regras-edicao.md`, já carregado no PASSO 0) se o arquivo alvo contém a tag de formato de resposta correta para o tipo do agente (Orquestrador/Protractor/Recepcionista=`<response_format>`, Qualifier=`<formato_resposta>`, Scheduler=`<contrato_resposta>`) e `<boas_praticas>`; (b) se alguma das duas estiver ausente, ADICIONAR a seção mínima faltante (conteúdo mínimo conforme `docs/regras-edicao.md`) NO MESMO despacho, junto com o ajuste pedido pelo usuário — nunca como aviso isolado, nunca como um passe separado; (c) esta checagem é ADITIVA — não substitui nem afrouxa a REGRA INVIOLÁVEL acima (a tag de formato de resposta, quando já presente, continua intocável); aplica-se somente quando a tag/seção estiver COMPLETAMENTE ausente do arquivo; (d) fora de escopo: `modelo/*.md` (read-only, nunca tocar) e `Follow-Up.md` (nunca teve tag de formato de resposta nem `<boas_praticas>` obrigatória — pular esta checagem para ele).

2. Na seção "## FLUXO DE TRABALHO", substituir a linha "4. Verificar se segue a arquitetura padrão de agentes" por "4. Verificar se segue a arquitetura padrão de agentes e a presença das seções obrigatórias (ver "SEÇÕES OBRIGATÓRIAS POR TIPO" acima)". Não altere as outras linhas numeradas dessa lista.

3. Na seção "## CHECKLIST ANTES DE ENTREGAR", adicionar um bullet novo IMEDIATAMENTE depois de "- [ ] `<response_format>` está IDÊNTICO ao original?" e antes da linha em branco que precede "## VERIFICAÇÃO DE ESCOPO": "- [ ] Seção de formato de resposta (tag correta do tipo) e `<boas_praticas>` estão presentes (adicionadas se faltavam)?"

PARTE B — `.claude/agents/docs-reviewer.md` (2 pontos de edição, ambos aditivos):

1. Adicionar uma subseção `###` nova IMEDIATAMENTE depois do bullet "- Fluxo permanece coerente?" (fim de "### 5. Lógica e Coerência") e antes da linha em branco que precede "## Formato de Resposta". NÃO renumere nem altere nenhuma subseção existente (1 a 5) — esta é aditiva, no final da lista.

Título da subseção nova: "### 6. Seções Obrigatórias por Tipo de Agente" com 2 checkboxes:
- Checkbox 1: a tag de formato de resposta correta para o tipo do agente está presente (mapa em `docs/regras-edicao.md` — Orquestrador/Protractor/Recepcionista=`<response_format>`, Qualifier=`<formato_resposta>`, Scheduler=`<contrato_resposta>`; Follow-Up fora desta checagem).
- Checkbox 2: `<boas_praticas>` está presente no arquivo (obrigatória em Orquestrador, Qualifier, Scheduler, Protractor, Recepcionista — ver `docs/regras-edicao.md`; Follow-Up fora desta checagem).

Esta subseção é DISTINTA do item 1 existente "Campos do `<formato_resposta>` inalterados (sem novos campos)?" — NÃO altere aquele bullet, ele fica byte-idêntico.

2. Na seção "## Regras", na linha "- PRIORIZAR: (a) alteração indevida em `modelo/`, (b) campos novos no `<formato_resposta>`, (c) duplicação de regras, (d) base de conhecimento inteira dentro do prompt.", adicionar ao final da mesma linha, antes do ponto final: "(e) ausência da tag de formato de resposta do tipo ou de `<boas_praticas>` (ver `docs/regras-validacao.md`)".

Não altere mais nada nesses 2 arquivos além do listado acima. Não toque `modelo/*.md` nem `.claude/agents/client-scaffold-fill.md`.
  </action>
  <verify>
    <automated>ED=".claude/agents/docs-editor-conciso.md"; R=".claude/agents/docs-reviewer.md"; test "$(grep -Fc '## SEÇÕES OBRIGATÓRIAS POR TIPO' "$ED")" = "1" && test "$(grep -Fc 'Follow-Up' "$ED")" -ge 1 && test "$(grep -Fc '<contrato_resposta>' "$ED")" -ge 1 && test "$(grep -Fc '**NUNCA modificar `<response_format>`** — Esta seção é intocável. Preserve-a exatamente como está, caractere por caractere.' "$ED")" = "1" && test "$(grep -Fc '### 6. Seções Obrigatórias por Tipo de Agente' "$R")" = "1" && test "$(grep -Fc '<contrato_resposta>' "$R")" -ge 1 && test "$(grep -Fc 'Campos do `<formato_resposta>` inalterados (sem novos campos)?' "$R")" = "1" && echo TASK2-OK</automated>
  </verify>
  <done>docs-editor-conciso.md tem a seção "SEÇÕES OBRIGATÓRIAS POR TIPO" nova (aditiva, REGRA INVIOLÁVEL intocada), o passo 4 do fluxo referencia essa seção, e o checklist final tem o bullet novo; docs-reviewer.md tem a subseção "### 6. Seções Obrigatórias por Tipo de Agente" nova (item 1 existente intocado) e a lista de prioridades em "## Regras" ganhou o item (e).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `docs/regras-edicao.md` + `docs/regras-validacao.md` → 3 agentes (`docs-analyzer`, `docs-editor-conciso`, `docs-reviewer`) | Fonte da verdade lida no Passo 0 de cada agente; qualquer inconsistência entre o mapa documentado e o mapa realmente aplicado pelos agentes propaga silenciosamente para arquivos de cliente. |
| `docs-analyzer` (sinalização) → `docs-editor-conciso` (correção) no mesmo despacho | `docs-editor-conciso` NÃO depende de receber `<secoes_faltantes>` do analyzer via o template de dispatch do comando `/ei-ajustes` (fora de escopo) — ele verifica de forma independente ao ler o arquivo completo (Passo 2 do próprio fluxo), evitando dependência frágil de um campo que pode não ser propagado. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick260812k2h-01 | Tampering | `.claude/agents/docs-editor-conciso.md` (nova seção "SEÇÕES OBRIGATÓRIAS POR TIPO") | medium | mitigate | A checagem é aditiva e autocontida na leitura completa do arquivo (Passo 2 do fluxo já existente) — não depende do comando `/ei-ajustes` (fora de escopo) propagar o campo novo do analyzer. Gate de verificação da Task 2 confirma a seção nova e a REGRA INVIOLÁVEL pré-existente intocada. |
| T-quick260812k2h-02 | Tampering | `.claude/agents/docs-editor-conciso.md` REGRA INVIOLÁVEL (`<response_format>` hardcoded, não por tipo) | low | accept | Esse hardcode já existia antes deste plano e não é agravado por ele (constraint explícita: preservar regras existentes byte-idênticas). A nova seção aditiva já orienta a checagem por tipo via `docs/regras-edicao.md`; corrigir a redação hardcoded da regra pré-existente fica fora de escopo. |
| T-quick260812k2h-03 | Repudiation | `.claude/agents/docs-analyzer.md` `<secoes_faltantes>` (novo campo de saída estruturada) | low | accept | Campo é somente informativo, consumido internamente pelo próprio pipeline; não há trilha de auditoria externa exigida para esta feature de detecção. |
| T-quick260812k2h-SC | Tampering | npm/pip/cargo installs | low | accept | Nenhuma task instala pacote — plano só edita arquivos `.md` (2 docs + 3 agentes). Projeto permanece zero-dependency; sem superfície de supply chain nesta mudança. |
</threat_model>

<verification>
Rodar na raiz do repo depois das 2 tasks:

1. Mapa completo dos 5 tipos + exclusão de Follow-Up presente em AMBOS os docs de fonte da verdade:
   `for f in docs/regras-edicao.md docs/regras-validacao.md; do grep -Fq '<response_format>' "$f" && grep -Fq '<formato_resposta>' "$f" && grep -Fq '<contrato_resposta>' "$f" && grep -Fq 'Follow-Up' "$f" || { echo "FALTA em $f"; exit 1; }; done; echo MAPA-OK`

2. `<secoes_faltantes>` presente e usado corretamente em `docs-analyzer.md` (schema + Regras do schema + `<regras>` + 2 exemplos):
   `test "$(grep -Fc 'secoes_faltantes' .claude/agents/docs-analyzer.md)" -ge 5 && echo ANALYZER-OK`

3. Checagens preservadas byte-idênticas (nenhuma regra existente foi afrouxada):
   `test "$(grep -Fc '<formato_resposta>` mantém apenas os campos originais (nenhum campo novo)?' docs/regras-validacao.md)" = "1" && test "$(grep -Fc '**NUNCA modificar `<response_format>`** — Esta seção é intocável. Preserve-a exatamente como está, caractere por caractere.' .claude/agents/docs-editor-conciso.md)" = "1" && test "$(grep -Fc 'Campos do `<formato_resposta>` inalterados (sem novos campos)?' .claude/agents/docs-reviewer.md)" = "1" && echo PRESERVACAO-OK`

4. Zonas fora de escopo intocadas:
   `test -z "$(git diff --name-only -- modelo/ .claude/agents/client-scaffold-fill.md .claude/commands/ei-ajustes.md)" && echo ESCOPO-OK`

5. Diff total confinado aos 5 arquivos esperados:
   `git diff --name-only | grep -v '^\.planning/' | sort` deve conter exatamente `docs/regras-edicao.md`, `docs/regras-validacao.md`, `.claude/agents/docs-analyzer.md`, `.claude/agents/docs-editor-conciso.md`, `.claude/agents/docs-reviewer.md`.
</verification>

<success_criteria>
- [ ] Mapa tag-por-tipo (5 tipos + exclusão de Follow-Up) e regra de `<boas_praticas>` obrigatória codificados em `docs/regras-edicao.md`; item de checklist correspondente (distinto do existente) em `docs/regras-validacao.md`.
- [ ] `docs-analyzer.md` preenche `<secoes_faltantes>` em cada `<arquivo>` da saída estruturada (vazio se nada faltar), referenciando o mapa em `docs/regras-edicao.md`, sem inventar tags e sem aplicar a Follow-Up.md.
- [ ] `docs-editor-conciso.md` detecta e adiciona a(s) seção(ões) mínima(s) faltante(s) no mesmo despacho do ajuste pedido, de forma autocontida (não depende de `/ei-ajustes` propagar o campo do analyzer); REGRA INVIOLÁVEL pré-existente intocada.
- [ ] `docs-reviewer.md` tem item de checklist novo e distinto (não misturado ao checklist existente de "sem campos novos") auditando as duas seções; checklist existente intocado.
- [ ] `modelo/*.md`, `.claude/agents/client-scaffold-fill.md` e `.claude/commands/ei-ajustes.md` com diff vazio (fora de escopo).
- [ ] Diff confinado aos 5 arquivos esperados.
</success_criteria>

<output>
Create `.planning/quick/260812-k2h-no-pipeline-ei-ajustes-docs-analyzer-md-/260812-k2h-SUMMARY.md` when done
</output>