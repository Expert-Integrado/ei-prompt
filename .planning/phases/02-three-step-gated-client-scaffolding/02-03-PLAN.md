---
phase: 02-three-step-gated-client-scaffolding
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - .claude/commands/ei-cria-cliente.md
autonomous: true
requirements: [SCAF-01, SCAF-02, SCAF-03, SCAF-04, SCAF-06]

must_haves:
  truths:
    - "A single-agent /ei-cria-cliente run chains structure→collect→gate→fill with zero silent skips"
    - "Any gate response other than the exact approval label is treated as Cancelar, and client-scaffold-fill is never dispatched in that case"
    - "The full <dados_coletados> block collected in Passo 2 is embedded verbatim (not summarized) into client-scaffold-fill's invocation prompt on approval"
  artifacts:
    - .claude/commands/ei-cria-cliente.md (new "Gate de Confirmação" subsection + rewritten Passo 4A)
  key_links:
    - "Passo 4A's dispatch of client-scaffold-fill must include the complete dados_coletados XML block from client-scaffold-collect's response, or Passo 3 fabricates or re-asks for values it should already have (Pitfall 2)"
---

## Phase Goal

**As a** developer creating a client project via `/ei-cria-cliente`, **I want to** go through three auditable, gated steps (scaffold folders → collect all required fields incl. media → fill templates) with a hard confirmation gate before filling, **so that** no client is ever left with silently incomplete required fields.

<objective>
Rewrite `/ei-cria-cliente.md`'s Passo 4A (single-agent mode) to chain the three subagents from Plan 02-01 — `client-scaffold-structure` → `client-scaffold-collect` → **hard confirmation gate** → `client-scaffold-fill` — and add the reusable "Gate de Confirmação" subsection that implements the hard gate itself (SCAF-04), mirroring `/ei-ajustes.md`'s Passo 3.5 caminho [A] exactly per D-06/D-07. This is the first complete vertical slice of the phase: after this plan, a developer running `/ei-cria-cliente` in single-agent mode gets the full three-step, gated experience end-to-end.

Purpose: this is the smallest unit of end-to-end value the phase can deliver — the 3 subagents alone (Plan 02-01) are inert until something dispatches them in the right order with a real human checkpoint in between.
Output: `.claude/commands/ei-cria-cliente.md` gains a new "Gate de Confirmação (Passo 2→3)" subsection and a rewritten Passo 4A.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-three-step-gated-client-scaffolding/02-CONTEXT.md
@.planning/phases/02-three-step-gated-client-scaffolding/02-RESEARCH.md
@.planning/phases/02-three-step-gated-client-scaffolding/02-PATTERNS.md
@.claude/commands/ei-cria-cliente.md
@.claude/commands/ei-ajustes.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the reusable "Gate de Confirmação (Passo 2→3)" subsection</name>
  <files>.claude/commands/ei-cria-cliente.md</files>
  <read_first>
    .claude/commands/ei-ajustes.md (Passo 3.5 caminho [A] — the literal `AskUserQuestion` JSON shape, the runtime/no-TTY note, the fail-closed interpretation rules, and the "REGRA INVIOLÁVEL" callout style, roughly lines 95-141 and 250-258 of the current file)
    .claude/commands/ei-cria-cliente.md (current Passo 3, Passo 4A, Passo 5 — to find the correct insertion point right after Passo 3 and before Passo 4A)
    .planning/phases/02-three-step-gated-client-scaffolding/02-CONTEXT.md (D-06, D-07)
    .planning/phases/02-three-step-gated-client-scaffolding/02-RESEARCH.md (Pattern 2, Pitfall 5, Open Question 1's A3 recommendation)
  </read_first>
  <action>
    Edit `.claude/commands/ei-cria-cliente.md`: insert a new subsection titled `### Gate de Confirmação (Passo 2→3)` positioned after Passo 3 (Modo) and before Passo 4A, written to be referenced (not duplicated) by both Passo 4A (this plan) and Passo 4B.1(b) (Plan 02-04). Content, adapted from `/ei-ajustes.md` Passo 3.5 caminho [A] per D-06/D-07:
    - Document that this subsection is invoked with the `<dados_coletados>` block just returned by `client-scaffold-collect` for the current client (or current especialidade, in multi mode) as input.
    - Require the tool that renders a single structured multi-choice question be used, with the `question` field built as a numbered list — one line per `<campo>`/`<midia>` from the input block, showing the collected value or the pending marker — ending with a line asking whether to proceed with filling.
    - Exactly two options, worded per D-06: `"Aprovar e preencher"` (dispatches the fill step with everything listed above) and `"Cancelar"` (ends without filling; the folder/files already created remain on disk, unfilled).
    - Copy the runtime/no-TTY note from `/ei-ajustes.md` near-verbatim, adapted to this gate (headless/no-TTY environments auto-resolve to the empty-response case, which the fail-closed rule below treats as Cancelar).
    - Fail-closed interpretation rules per D-07: response = the exact approval label → proceed to dispatch `client-scaffold-fill` with the complete `<dados_coletados>` block embedded verbatim in its prompt (never summarized or shortened — see Pitfall 2); response = "Cancelar", empty, `answers={}`, "Outro", or anything else → treat as Cancelar; on Cancelar, do NOT dispatch `client-scaffold-fill` — per RESEARCH.md's Open Question 1 / Pitfall 5 recommendation, leave the structure `client-scaffold-structure` already created on disk exactly as-is (non-destructive; `{{variavel}}` and pending markers remain unfilled), inform the caller this cycle ended without filling, and let the caller (Passo 4A or Passo 4B.1(b)) decide what happens next.
    - Add a "REGRA INVIOLÁVEL" callout, mirroring `/ei-ajustes.md`'s style: never invoke `client-scaffold-fill` without this gate having produced the exact approval-label response first — no alternate path, no exception for errors.
  </action>
  <acceptance_criteria>
    A `### Gate de Confirmação` heading exists in the file, positioned between the existing Passo 3 and Passo 4A sections.
    The subsection contains both exact option labels `"Aprovar e preencher"` and `"Cancelar"`.
    The subsection contains a `REGRA INVIOLÁVEL` callout about never dispatching the fill subagent without approval.
    The subsection documents the non-destructive Cancel behavior (structure stays on disk unfilled).
  </acceptance_criteria>
  <verify>
    <automated>grep -q '### Gate de Confirmação' .claude/commands/ei-cria-cliente.md && grep -q 'Aprovar e preencher' .claude/commands/ei-cria-cliente.md && grep -q 'REGRA INVIOLÁVEL' .claude/commands/ei-cria-cliente.md && echo PASS</automated>
  </verify>
  <done>The reusable Gate de Confirmação subsection exists with the exact D-06 option labels, D-07 fail-closed rules, and the non-destructive Cancel behavior documented.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Passo 4A to chain structure→collect→gate→fill for single-agent mode</name>
  <files>.claude/commands/ei-cria-cliente.md</files>
  <read_first>
    .claude/commands/ei-cria-cliente.md (current Passo 4A and Passo 5 — the file as it stands after Task 1's edit, including the new Gate de Confirmação subsection just added)
    .claude/commands/ei-ajustes.md (Passo 3's "Invocar via Agent tool com subagent_type: docs-analyzer" phrasing — the pattern for naming which subagent an Agent tool call dispatches)
  </read_first>
  <action>
    Edit `.claude/commands/ei-cria-cliente.md`'s Passo 4A: replace the single dispatch to the old monolithic scaffolder with four sequential steps: (1) dispatch `client-scaffold-structure` via the Agent tool with `modo: single-agent` and `nome_cliente: <valor>`, wait for it to return the created path; (2) dispatch `client-scaffold-collect` via the Agent tool with `<cliente_path>` set to that path, wait for its `<dados_coletados>` response; (3) invoke the "Gate de Confirmação (Passo 2→3)" subsection (Task 1) using that `<dados_coletados>` block; (4) on approval, dispatch `client-scaffold-fill` via the Agent tool with `<cliente_path>` and the complete `<dados_coletados>` block embedded literally in the prompt text; on Cancelar, end Passo 4A per the gate's documented Cancel behavior. After either outcome, proceed to Passo 5 (resumo final) as today — do not skip it even on Cancelar, since the resumo should still report the created-but-possibly-unfilled structure.
  </action>
  <acceptance_criteria>
    Passo 4A's text names all three new subagents (`client-scaffold-structure`, `client-scaffold-collect`, `client-scaffold-fill`) in dispatch order.
    Passo 4A references the "Gate de Confirmação" subsection rather than duplicating its content.
    Passo 4A documents embedding the complete `<dados_coletados>` block (not a summary) into the fill dispatch.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'client-scaffold-structure' .claude/commands/ei-cria-cliente.md && grep -q 'client-scaffold-collect' .claude/commands/ei-cria-cliente.md && grep -q 'client-scaffold-fill' .claude/commands/ei-cria-cliente.md && grep -q 'dados_coletados' .claude/commands/ei-cria-cliente.md && echo PASS</automated>
  </verify>
  <done>Passo 4A dispatches the 3 new subagents in order with the hard gate in between; approval embeds the full dados_coletados block verbatim into the fill dispatch; Cancel is fail-closed and non-destructive; Passo 5 still runs afterward either way.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human ↔ main command thread | The confirmation gate is the only point where an actual human decision authorizes writing client-specific content to disk; every other step in the chain is fully automated |
| Main command thread ↔ subagent context | `client-scaffold-fill` has zero memory of the Passo 2 conversation — the command thread is solely responsible for carrying the collected data across that boundary intact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Tampering (bypassing the hard gate) | Gate de Confirmação subsection, Passo 4A | critical | mitigate | Exact fail-closed response mapping mirroring `/ei-ajustes.md` Passo 3.5 caminho [A] (Task 1), with a REGRA INVIOLÁVEL callout; any response other than the literal approval label — empty, ambiguous, "Outro", no-TTY auto-resolution — is treated as Cancelar and `client-scaffold-fill` is never dispatched. Verified live in Plan 05's checkpoint (deliberate Cancel attempt). This is the phase's Core Value: no client is ever left with silently incomplete required fields written to disk without explicit approval. |
| T-2-04 | Repudiation (ambiguous half-created state) | Passo 4A Cancel path | low | mitigate | Cancel behavior is explicitly documented as non-destructive (structure stays on disk unfilled) rather than left as an accidental default, and Passo 5's summary still runs afterward so the state is visible to the user, not silently hidden |
</threat_model>

<verification>
Run both tasks' `<automated>` verify commands. Full behavioral confirmation of the fail-closed gate (Cancelar / empty / "Outro" all blocking the fill dispatch) happens live in Plan 05's checkpoint, since this requires an interactive session with the actual confirmation tool.
</verification>

<success_criteria>
- [ ] Single-agent `/ei-cria-cliente` dispatches structure→collect→gate→fill in order
- [ ] Gate uses the exact D-06 option labels and D-07 fail-closed rules
- [ ] Approval embeds the full dados_coletados block verbatim into the fill dispatch
- [ ] Cancel leaves the created structure on disk, unfilled, and still reaches Passo 5
</success_criteria>

## Artifacts This Phase Produces

| Artifact | Created By | Type |
|----------|-----------|------|
| `.claude/agents/client-scaffold-structure.md` | Plan 02-01 | New subagent file |
| `.claude/agents/client-scaffold-collect.md` | Plan 02-01 | New subagent file |
| `.claude/agents/client-scaffold-fill.md` | Plan 02-01 | New subagent file |
| `manifest.json` `files[]`/`deprecated_files[]` entries | Plan 02-02 | Config update |
| `.claude/hooks/post-scaffolder-review.sh` case branch retarget | Plan 02-02 | Renamed hook branch |
| New "Gate de Confirmação" subsection + rewritten Passo 4A in `.claude/commands/ei-cria-cliente.md` | This plan (02-03) | New command section |
| Rewritten Passo 4B.1(b) + enriched Passo 5 in `.claude/commands/ei-cria-cliente.md` | Plan 02-04 | Modified command section |

<output>
Create `.planning/phases/02-three-step-gated-client-scaffolding/02-03-SUMMARY.md` when done
</output>
