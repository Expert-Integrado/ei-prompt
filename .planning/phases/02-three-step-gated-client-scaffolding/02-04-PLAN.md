---
phase: 02-three-step-gated-client-scaffolding
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - .claude/commands/ei-cria-cliente.md
autonomous: true
requirements: [SCAF-04, SCAF-05]

must_haves:
  truths:
    - "A multi-agent /ei-cria-cliente run applies the identical structure→collect→gate→fill cycle per especialidade, one at a time, before the next especialidade begins"
    - "Cancelling one especialidade's gate does not abort the whole multi-agent run — the loop continues to the next especialidade and the cancelled one's status is recorded, not silently dropped"
    - "No dispatch anywhere in ei-cria-cliente.md still names the retired single-step scaffolder agent"
  artifacts:
    - .claude/commands/ei-cria-cliente.md (rewritten Passo 4B.1(b) + enriched Passo 5)
  key_links:
    - "Passo 4B.1(b) reuses the exact same Gate de Confirmação subsection Plan 02-03 added — it must not duplicate a second copy of the gate logic"
---

## Phase Goal

**As a** developer creating a client project via `/ei-cria-cliente`, **I want to** go through three auditable, gated steps (scaffold folders → collect all required fields incl. media → fill templates) with a hard confirmation gate before filling, **so that** no client is ever left with silently incomplete required fields.

<objective>
Rewrite `/ei-cria-cliente.md`'s Passo 4B.1(b) (the per-especialidade loop in multi-agent mode) to apply the identical structure→collect→gate→fill chain that Plan 02-03 wired for single-agent mode, preserving the existing sequential (non-parallel) loop and the "ask everything from scratch per especialidade" rule (D-03), and keeping the consolidated final summary at the end of the whole run (D-04). This is the phase's second and final vertical slice: after this plan, the three-step gated flow behaves identically in both modes (SCAF-05), completing the requirement set.

Purpose: SCAF-05 explicitly requires the split to apply to both existing modes — Plan 02-03 alone only delivers the single-agent half.
Output: `.claude/commands/ei-cria-cliente.md`'s Passo 4B.1(b) rewritten, Passo 5 enriched with per-especialidade gate outcomes, and every remaining reference to the retired agent removed from the file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-three-step-gated-client-scaffolding/02-CONTEXT.md
@.planning/phases/02-three-step-gated-client-scaffolding/02-RESEARCH.md
@.planning/phases/02-three-step-gated-client-scaffolding/02-PATTERNS.md
@.claude/commands/ei-cria-cliente.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Passo 4B.1(b)'s per-especialidade loop to chain structure→collect→gate→fill</name>
  <files>.claude/commands/ei-cria-cliente.md</files>
  <read_first>
    .claude/commands/ei-cria-cliente.md (the file as it stands after Plan 02-03's edits — current Passo 4B.1(b) loop, its "REGRA DO LOOP" note near the bottom of the Regras section, and Passo 4B.1(c) which dispatches the unrelated Recepcionista agent, plus the already-added "Gate de Confirmação" subsection and rewritten Passo 4A from Plan 02-03)
    .planning/phases/02-three-step-gated-client-scaffolding/02-CONTEXT.md (D-03, D-04)
    .planning/phases/02-three-step-gated-client-scaffolding/02-RESEARCH.md (Pitfall 5, Open Question 1's A3 recommendation, System Architecture Diagram's multi-agent loop note)
  </read_first>
  <action>
    Edit `.claude/commands/ei-cria-cliente.md`'s Passo 4B.1(b): for each especialidade name in the loop, replace the single per-specialty dispatch with the same four-step chain used in Passo 4A: (1) dispatch `client-scaffold-structure` with `modo: multi-agente-especialidade-unica`, `nome_cliente`, and `especialidade` for this iteration; (2) dispatch `client-scaffold-collect` scoped to that especialidade's created path, collecting only that especialidade's fields from scratch (preserve the existing "não assuma contexto de chamadas anteriores" rule verbatim — every iteration asks everything fresh); (3) invoke the same "Gate de Confirmação (Passo 2→3)" subsection Plan 02-03 added, using that especialidade's `<dados_coletados>`; (4) on approval, dispatch `client-scaffold-fill` for that especialidade with the complete collected block embedded verbatim. On Cancelar for a given especialidade (per the gate's documented non-destructive behavior), record that especialidade's status as cancelled-and-unfilled and CONTINUE the loop to the next especialidade in the list — do not abort the whole `/ei-cria-cliente` run over one especialidade's cancellation; this is a deliberate, documented default (not a silent guess), consistent with the gate's non-destructive Cancel behavior and with keeping each especialidade's cycle independent per D-03. Update the "REGRA DO LOOP" wording at the bottom of the Regras section to describe the new four-step-per-especialidade cycle instead of the old single dispatch, keeping the sequential (non-parallel), ask-everything-from-scratch guarantee intact. Update Passo 5 (resumo final) to aggregate, per especialidade, whether that cycle ended filled or cancelled-and-unfilled, alongside the existing pending-fields aggregation — still one consolidated summary at the end of the whole run (D-04), not a summary per especialidade. Passo 4B.1(c) (Recepcionista dispatch) and Passo 4B.2 (bypass) are unrelated to this split and stay as they are unless they reference the agent this phase is retiring, in which case update that reference to point at the new fill-step subagent's name instead. Finally, sweep the rest of the file (the top-level "Regras" section in particular) for any remaining reference to the agent this phase's Plan 02-01/02-02 retired, and replace each one with the equivalent rule phrased around the new three-subagent chain — by the end of this task, the file must contain zero mentions of the retired agent's name anywhere.
  </action>
  <acceptance_criteria>
    Passo 4B.1(b)'s loop text names all three new subagents in dispatch order for each especialidade.
    The loop text documents the Cancel-and-continue-to-next-especialidade behavior explicitly (not a silent abort).
    The "REGRA DO LOOP" note and Passo 5 no longer describe a single per-specialty dispatch.
    Nowhere in the file does the retired single-step scaffolder subagent's name still appear.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'client-scaffold-structure' .claude/commands/ei-cria-cliente.md && grep -q 'client-scaffold-collect' .claude/commands/ei-cria-cliente.md && grep -q 'client-scaffold-fill' .claude/commands/ei-cria-cliente.md && grep -q 'multi-agente-especialidade-unica' .claude/commands/ei-cria-cliente.md && grep -qi 'cancel' .claude/commands/ei-cria-cliente.md && [ "$(grep -c 'client-project-scaffolder' .claude/commands/ei-cria-cliente.md)" = "0" ] && echo PASS</automated>
  </verify>
  <done>Passo 4B.1(b) chains the 3 subagents + gate per especialidade, preserves the sequential non-parallel ask-from-scratch loop, Cancel-at-gate continues to the next especialidade with recorded status rather than aborting, Passo 5 aggregates per-especialidade outcomes, and zero references to the retired subagent remain anywhere in the command file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Per-especialidade loop iteration boundary | Each especialidade's structure→collect→gate→fill cycle must complete (or be explicitly cancelled) independently before the next begins — no especialidade's collected data or approval carries over to another |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Tampering (bypassing the hard gate) | Gate de Confirmação subsection reused inside Passo 4B.1(b) | critical | mitigate | Same gate implementation as Plan 02-03 (not a second, possibly-drifted copy) — fail-closed mapping applies identically per especialidade; verified live in Plan 05's checkpoint including a deliberate Cancel on one especialidade mid-loop |
| T-2-04 | Repudiation (ambiguous per-especialidade cancel state in a multi-step loop) | Passo 4B.1(b) loop, Passo 5 summary | medium | mitigate | Cancel-then-continue is a conscious, documented default (this task's action), and each especialidade's final status (filled vs. cancelled-and-unfilled) is surfaced in the one consolidated Passo 5 summary rather than silently lost when the loop moves on |
</threat_model>

<verification>
Run the task's `<automated>` verify command. Full behavioral confirmation of the multi-agent loop (sequential per-especialidade cycles, ask-from-scratch, mid-loop Cancel continuing to the next especialidade, consolidated summary) happens live in Plan 05's checkpoint.
</verification>

<success_criteria>
- [ ] Multi-agent `/ei-cria-cliente` dispatches structure→collect→gate→fill per especialidade, sequentially
- [ ] Each especialidade's collection asks everything from scratch (no reuse across iterations)
- [ ] Cancel on one especialidade continues the loop to the next, recording the cancelled status
- [ ] Passo 5 aggregates per-especialidade outcomes in one consolidated summary
- [ ] Zero remaining references to the retired subagent anywhere in ei-cria-cliente.md
</success_criteria>

## Artifacts This Phase Produces

| Artifact | Created By | Type |
|----------|-----------|------|
| `.claude/agents/client-scaffold-structure.md` | Plan 02-01 | New subagent file |
| `.claude/agents/client-scaffold-collect.md` | Plan 02-01 | New subagent file |
| `.claude/agents/client-scaffold-fill.md` | Plan 02-01 | New subagent file |
| `manifest.json` `files[]`/`deprecated_files[]` entries | Plan 02-02 | Config update |
| `.claude/hooks/post-scaffolder-review.sh` case branch retarget | Plan 02-02 | Renamed hook branch |
| New "Gate de Confirmação" subsection + rewritten Passo 4A in `.claude/commands/ei-cria-cliente.md` | Plan 02-03 | New command section |
| Rewritten Passo 4B.1(b) + enriched Passo 5 in `.claude/commands/ei-cria-cliente.md` | This plan (02-04) | Modified command section |

<output>
Create `.planning/phases/02-three-step-gated-client-scaffolding/02-04-SUMMARY.md` when done
</output>
