---
phase: 03.1-corrigir-refer-ncias-obsoletas-a-client-project-scaffolder-e
verified: 2026-07-16T16:20:00Z
status: passed
score: 7/7 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 6/7
  gaps_closed:
    - "Toda referência à arquitetura de scaffolding retirada (`client-project-scaffolder`) está corrigida para os 3 subagents atuais em toda a documentação e prompts do repositório (current-state docs, not historical records)"
  gaps_remaining: []
  regressions: []
---

# Phase 03.1: Corrigir referências obsoletas a client-project-scaffolder e fallback CLAUDE.md em inject-ei-context.sh Verification Report

**Phase Goal:** Como mantenedor do ei-prompt, quero que toda referência à arquitetura de scaffolding retirada (`client-project-scaffolder`) seja corrigida para os 3 subagents atuais (`client-scaffold-structure` / `client-scaffold-collect` / `client-scaffold-fill`) em toda a documentação e prompts do repositório, e que `inject-ei-context.sh` tenha o mesmo fallback dual-context CLAUDE.md dos demais consumidores, para que nenhum agente (humano ou LLM) seja guiado por instruções que descrevem um fluxo que não existe mais.
**Verified:** 2026-07-16T16:20:00Z
**Status:** passed
**Re-verification:** Yes — after gap closure (plan 03.1-03)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `recepcionista-scaffolder.md` has zero occurrences of the retired subagent name; frontmatter description + body (lines 3, 16, 18) reference `client-scaffold-structure` | ✓ VERIFIED (regression check) | `grep -c 'client-project-scaffolder' .claude/agents/recepcionista-scaffolder.md` → 0 |
| 2 | `recepcionista-scaffolder.md` Fase 5 confirmation text no longer claims a scaffolding subagent "still needs to run" | ✓ VERIFIED (regression check) | Line 147 confirmed present, reads "...não há nenhum passo de scaffolding pendente." Note: a code-review finding (CR-01 in `03.1-REVIEW.md`) flags this same sentence as making an unconditional completeness claim that is factually false in two scenarios (specialty cancellation, bypass mode) — see "Code Review Cross-Reference" below; this is a correctness concern, not a stale-name-reference regression, so the truth as scoped (name updated, no longer implies a retired subagent "still needs to run") remains verified. |
| 3 | `inject-ei-context.sh` prefers `client/CLAUDE.md` when present, falls back to root `CLAUDE.md` | ✓ VERIFIED (regression check) | `CLIENT_CLAUDE_MD="$PROJECT_DIR/client/CLAUDE.md"` + `[ -f "$CLIENT_CLAUDE_MD" ]` fallback logic confirmed present (lines 13, 15-16, 46) |
| 4 | `inject-ei-context.sh` remains unregistered in `.claude/settings.json` (latent-risk-only fix) | ✓ VERIFIED (regression check) | `grep` for `"SessionStart"`/`"UserPromptSubmit"`/`"PreToolUse"` in `.claude/settings.json` returns no match |
| 5 | `.claude/CLAUDE.md` has zero occurrences of the retired subagent name | ✓ VERIFIED (regression check) | `grep -c 'client-project-scaffolder' .claude/CLAUDE.md` → 0 |
| 6 | `COMANDOS.md` has zero occurrences of the retired subagent name | ✓ VERIFIED (regression check) | `grep -c 'client-project-scaffolder' COMANDOS.md` → 0 |
| 7 | Toda referência à arquitetura retirada está corrigida em toda a documentação e prompts do repositório (roadmap goal, verbatim) — **the previously-FAILED truth** | ✓ VERIFIED | Gap-closure plan 03.1-03 fixed all 7 files named in the prior gap: `client/CLAUDE.md` line 76 now names `client-scaffold-fill` (0 remaining hits); `.planning/PROJECT.md` line 9 (Core Value) and `.planning/STATE.md` line 26 (Core value bullet) both now name `client-scaffold-fill`, matching `.claude/CLAUDE.md`'s wording; all 4 `.planning/codebase/{ARCHITECTURE,STRUCTURE,CONVENTIONS,INTEGRATIONS}.md` files have 0 remaining hits and correctly cite the 3-subagent chain (verified substantively — ARCHITECTURE.md's Component Responsibilities table has 3 real rows with correct file paths/responsibilities; STRUCTURE.md's directory tree lists the 3 correct filenames in place of the deleted one; INTEGRATIONS.md's hook description and model-frontmatter list both cite current subagents). A broadest-possible repo-wide grep (`grep -rln "client-project-scaffolder" . --exclude-dir=.git`, no file-type filter) confirms every remaining hit is a legitimate historical/self-referential mention: `manifest.json`'s `deprecated_files` entry (the correct mechanism removing the dead file from client installs), `CHANGELOG.md` (append-only historical log), `.planning/REQUIREMENTS.md`/`.planning/ROADMAP.md` (past-tense requirement text and this phase's own title/goal naming the defect it fixes), `.planning/codebase/CONCERNS.md` (historical bug narration), `.planning/v1.0-v1.0-MILESTONE-AUDIT.md` (point-in-time audit snapshot), `.planning/phases/{01,02,03.1}-*` (this and prior phases' own planning artifacts narrating the retirement/fix), and the specifically carved-out historical/slug lines within `.planning/PROJECT.md` (lines 19, 53, 54) and `.planning/STATE.md` (lines 6, 27, 31, 84, 106) — all spot-checked verbatim and confirmed unmodified/legitimate. |

**Score:** 7/7 truths verified (0 present, behavior-unverified)

### Required Artifacts

None declared in any of the 3 plans' `must_haves.artifacts` — all 3 plans modify existing files only (documentation-only phase, no new symbols/files).

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `.claude/agents/recepcionista-scaffolder.md` | `.claude/commands/ei-cria-cliente.md` | Fase 5 confirmation text describes the actual Passo 4B.1(b)/(c) sequencing | ✓ WIRED (regression check) | Line 147 text matches the real Passo 4B.1(b)/(c) dispatch order |
| `.claude/hooks/inject-ei-context.sh` | `client/CLAUDE.md` | file-existence check (dual-context fallback) | ✓ WIRED (regression check) | `[ -f "$CLIENT_CLAUDE_MD" ]` check present |
| `client/CLAUDE.md` | `.claude/hooks/post-scaffolder-review.sh` | hook-trigger name in prose matches the hook's actual case-statement match | ✓ WIRED | `client/CLAUDE.md` line 76 now says `client-scaffold-fill`; `post-scaffolder-review.sh`'s case statement matches on `client-scaffold-fill` — confirmed consistent |
| `.planning/PROJECT.md` | `.claude/CLAUDE.md` | Core Value wording kept in sync | ✓ WIRED | Both files' Core Value lines now read identically (only the retired-name token differs from the original stale text, both now say `client-scaffold-fill`) |
| `.planning/codebase/ARCHITECTURE.md` | `.claude/agents/client-scaffold-{structure,collect,fill}.md` | Component Responsibilities table rows cite correct files | ✓ WIRED | 3-row split confirmed with correct file paths and responsibility text matching each subagent's actual role |

### Requirements Coverage

No requirement IDs are mapped to Phase 03.1 in `.planning/REQUIREMENTS.md` (`grep -n "03\.1" .planning/REQUIREMENTS.md` returns no matches). All 3 plans (`03.1-01`, `03.1-02`, `03.1-03`) declare `requirements: []` in frontmatter. This is expected and documented: `03.1-CONTEXT.md`'s Deferred section explicitly states requirement IDs are "TBD (post-hoc tech-debt phase — left unmapped rather than retrofitting REQ-IDs into the closed v1.0 traceability table)". No orphaned requirements found for this phase.

### Anti-Patterns Found

None. Scanned all 7 files touched by gap-closure plan 03.1-03 (`client/CLAUDE.md`, `.planning/PROJECT.md`, `.planning/STATE.md`, `.planning/codebase/{ARCHITECTURE,STRUCTURE,CONVENTIONS,INTEGRATIONS}.md`) for TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER markers — zero hits. Combined with the 4 files from plans 03.1-01/03.1-02 (already clean per the prior verification pass), all 11 files touched across this phase are free of debt markers.

### Code Review Cross-Reference (informational — not a phase-goal gap)

`03.1-REVIEW.md` (code review, standard depth, 4 files) found 1 critical finding (CR-01), 2 warnings, 2 info items — all in files touched by plans 03.1-01/03.1-02, none in the gap-closure plan 03.1-03's 7 files. CR-01 is a real correctness concern: `recepcionista-scaffolder.md`'s Fase 5 confirmation text (rewritten by plan 03.1-01) makes an unconditional claim ("não há nenhum passo de scaffolding pendente") that is factually false when a specialty was cancelled mid-loop or when bypass mode is used — in both cases `client-scaffold-fill` never ran for some/all specialties, yet the text presents completion unconditionally. This is a quality/correctness issue distinct from this phase's actual must_have scope (renaming the retired subagent and fixing `inject-ei-context.sh`'s fallback) — the sentence's *rename* is correct and its *presence* satisfies plan 03.1-01's own `must_haves.truths` #2 (which itself specified this exact wording), but the underlying claim's accuracy is a separate defect the reviewer correctly flagged. Judgment: this does not undermine the phase's stated must_haves.truths (which are about stale-name correction, not completeness-claim accuracy) and is left as a follow-up item — see `03.1-REVIEW.md` CR-01 for the fix recommendation. Not counted as a phase-goal gap in this verification.

### Human Verification Required

None. All findings in this report are grep/read-verifiable facts about static documentation text.

### Gaps Summary

No gaps remain. Gap-closure plan 03.1-03 corrected all 7 files identified in the prior `03.1-VERIFICATION.md` (`client/CLAUDE.md`, `.planning/PROJECT.md`, `.planning/STATE.md`, and 4 `.planning/codebase/*.md` files) with mechanical, scoped renames matching the same defect-fix pattern already applied by plans 03.1-01/03.1-02. A broadest-possible repo-wide grep (all file types, `.git/` excluded only) confirms every remaining occurrence of `client-project-scaffolder` in the repository is a legitimate historical record, self-referential planning artifact, or the correct `deprecated_files` manifest entry — none describe the retired architecture as currently live. All 6 previously-verified truths were spot-checked and show no regression. The phase goal — closing stale references to the retired subagent across all current-state documentation/prompts, and adding the dual-context CLAUDE.md fallback to `inject-ei-context.sh` — is fully achieved.

One informational item carries forward: `03.1-REVIEW.md` CR-01 (recepcionista-scaffolder.md Fase 5 unconditional completeness claim) is a real code-quality finding but is out of scope for this phase's must_haves — see Code Review Cross-Reference above.

---

*Verified: 2026-07-16T16:20:00Z*
*Verifier: Claude (gsd-verifier)*
