---
phase: 03.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/recepcionista-scaffolder.md
  - .claude/hooks/inject-ei-context.sh
autonomous: true
requirements: []

must_haves:
  truths:
    - "`.claude/agents/recepcionista-scaffolder.md` contains zero occurrences of the retired subagent name; the frontmatter description and body (lines 3, 16, 18) reference `client-scaffold-structure` as the folder-skeleton creator instead"
    - "`.claude/agents/recepcionista-scaffolder.md`'s Fase 5 confirmation text no longer claims a scaffolding subagent 'still needs to run' — it accurately states every specialty folder was already created and filled by the earlier structure->collect->fill loop before Recepcionista's own Fase 5 runs"
    - "`.claude/hooks/inject-ei-context.sh` prefers `$PROJECT_DIR/client/CLAUDE.md` when present and falls back to `$PROJECT_DIR/CLAUDE.md` otherwise, matching the dual-context pattern already used by `client-scaffold-fill.md`, `client-scaffold-collect.md`, `client-scaffold-structure.md`, and `ei-ajustes.md`"
    - "`inject-ei-context.sh` remains unregistered in `.claude/settings.json` — no SessionStart/UserPromptSubmit/PreToolUse hook is added; this fix is latent-risk-only, not an activation"
  artifacts: []
  key_links:
    - from: ".claude/agents/recepcionista-scaffolder.md"
      to: ".claude/commands/ei-cria-cliente.md"
      via: "Fase 5 confirmation text describes the actual Passo 4B.1(b)/(c) sequencing instead of a retired single-subagent flow"
      pattern: "client-scaffold-structure.*client-scaffold-collect.*client-scaffold-fill"
    - from: ".claude/hooks/inject-ei-context.sh"
      to: "client/CLAUDE.md"
      via: "file-existence check (dual-context fallback) mirroring client-scaffold-fill.md's Passo 0 pattern"
      pattern: "client/CLAUDE.md"
---

<objective>
Fix two documentation-drift/latent-bug targets identified in the v1.0 milestone audit (`.planning/v1.0-v1.0-MILESTONE-AUDIT.md`) and locked in `03.1-CONTEXT.md` Decisions 2 and 3: (1) every stale `client-project-scaffolder` reference in `.claude/agents/recepcionista-scaffolder.md`, including a factually-backwards Fase 5 confirmation line that claims a scaffolding step "still needs to run" when it has actually already completed; (2) `inject-ei-context.sh`'s missing dual-context (`client/CLAUDE.md`-first) fallback — the only CLAUDE.md consumer in the repo without it (CLMD-06 pattern).

Purpose: `client-project-scaffolder` was retired in Phase 2 (split into `client-scaffold-structure` -> `client-scaffold-collect` -> `client-scaffold-fill`) but `recepcionista-scaffolder.md`'s own prose — read by an LLM agent every time it runs — was never swept. A future invocation reading line 147 literally would tell the user a step "still needs to run" that has already happened, which is wrong in both the old and new architecture. `inject-ei-context.sh` is currently disabled (no `SessionStart`/`UserPromptSubmit`/`PreToolUse` registration in `.claude/settings.json`), so this is a latent fix, not an active bug — but it is one of this phase's two named targets and must be correct before any future re-enablement.
Output: `.claude/agents/recepcionista-scaffolder.md` and `.claude/hooks/inject-ei-context.sh` corrected; zero behavior change to any currently-active pipeline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-corrigir-refer-ncias-obsoletas-a-client-project-scaffolder-e/03.1-CONTEXT.md
@.claude/agents/recepcionista-scaffolder.md
@.claude/commands/ei-cria-cliente.md
@.claude/agents/client-scaffold-structure.md
@.claude/hooks/inject-ei-context.sh
@.claude/agents/client-scaffold-fill.md
@.claude/settings.json
</context>

<tasks>

<!-- planner-discipline-allow: client-project-scaffolder -->

<task type="auto">
  <name>Task 1: Fix stale client-project-scaffolder references in recepcionista-scaffolder.md (CONTEXT.md Decision 2)</name>
  <files>.claude/agents/recepcionista-scaffolder.md</files>
  <read_first>
    - `.claude/agents/recepcionista-scaffolder.md` (full file — lines 3, 16, 18, 147 are the 4 edit targets)
    - `.claude/commands/ei-cria-cliente.md` Passo 4B.1(b)/(c) — authoritative source for the Fase 5 rewrite: confirms every specialty runs its own structure->collect->gate->fill cycle BEFORE `recepcionista-scaffolder` is dispatched once, at the end
    - `.claude/agents/client-scaffold-structure.md` frontmatter `description` — confirms `client-scaffold-structure` is the subagent that creates each specialty folder (source of truth for the rename target)
  </read_first>
  <action>
Apply exactly these 4 edits, all mechanical renames except the 4th (which is a factual rewrite, not a rename):

1. Frontmatter `description` (line 3): within the sentence "NÃO cria as pastas das especialidades — isso é do `client-project-scaffolder`." replace the trailing agent name so it reads "...isso é do `client-scaffold-structure`." Then, in the same description, replace "chamado DEPOIS do `client-project-scaffolder` ter criado todas as especialidades, recebendo a lista delas" with "chamado DEPOIS que `/ei-cria-cliente` já rodou o ciclo `client-scaffold-structure` → `client-scaffold-collect` → `client-scaffold-fill` para todas as especialidades, recebendo a lista delas" — this reflects that filling (not just folder creation) is complete by the time this agent is dispatched in the full flow.

2. Line 16 (ESCOPO bullet): "- ❌ **NÃO** criar pastas das especialidades — isso é do `client-project-scaffolder`." becomes "- ❌ **NÃO** criar pastas das especialidades — isso é do `client-scaffold-structure`." — mechanical rename only.

3. Line 18 (Fluxo completo sub-bullet): "**Fluxo completo:** o `client-project-scaffolder` já criou a raiz ao montar as especialidades — `mkdir -p` é idempotente, segue tranquilo." becomes "**Fluxo completo:** o `client-scaffold-structure` já criou a raiz ao montar as especialidades — `mkdir -p` é idempotente, segue tranquilo." — mechanical rename only.

4. Line 147 (Fase 5: Confirmação) — this is NOT a rename, the current text is factually backwards. Replace the full line "- Confirme que o `client-project-scaffolder` ainda precisa rodar para criar as pastas das especialidades (o comando `/ei-cria-cliente` faz isso na sequência)." with: "- Confirme que todas as pastas das especialidades já foram criadas e preenchidas pelo ciclo `client-scaffold-structure` → `client-scaffold-collect` → `client-scaffold-fill` que o `/ei-cria-cliente` executou antes desta etapa (Passo 4B.1(b)) — não há nenhum passo de scaffolding pendente." The old line claims a scaffolding step still needs to run; by the time this Fase 5 executes (always last, per Passo 4B.1(c)) every specialty folder is already created AND filled — the replacement must not claim any scaffolding subagent still needs to run.

Do not touch any other line in the file (the REGRAS CRÍTICAS section's "NUNCA criar pastas das especialidades" bullet near the bottom of the file does not name any agent and needs no change).
  </action>
  <verify>
    <automated>[ "$(grep -c 'client-project-scaffolder' .claude/agents/recepcionista-scaffolder.md)" = "0" ] && [ "$(grep -c 'client-scaffold-structure' .claude/agents/recepcionista-scaffolder.md)" -ge 3 ] && grep -q 'client-scaffold-collect' .claude/agents/recepcionista-scaffolder.md && grep -q 'client-scaffold-fill' .claude/agents/recepcionista-scaffolder.md && grep -q 'não há nenhum passo de scaffolding pendente' .claude/agents/recepcionista-scaffolder.md && ! grep -q 'ainda precisa rodar' .claude/agents/recepcionista-scaffolder.md && echo PASS</automated>
  </verify>
  <done>recepcionista-scaffolder.md has zero remaining mentions of the retired subagent name; all 3 renamed mentions point to client-scaffold-structure; the Fase 5 confirmation line accurately states scaffolding is already complete instead of claiming a step still needs to run.</done>
  <acceptance_criteria>
    - `grep -c 'client-project-scaffolder' .claude/agents/recepcionista-scaffolder.md` returns 0
    - Lines 3, 16, 18 (frontmatter description + 2 body bullets) reference `client-scaffold-structure`
    - Line 147 no longer contains the phrase "ainda precisa rodar" and instead states the specialty folders were already created and filled
    - No other line in the file was modified
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add dual-context CLAUDE.md fallback to inject-ei-context.sh (CONTEXT.md Decision 3)</name>
  <files>.claude/hooks/inject-ei-context.sh</files>
  <read_first>
    - `.claude/hooks/inject-ei-context.sh` (full file — lines 12-13 define `CLAUDE_MD`, line 40 is the "not found" warning)
    - `.claude/agents/client-scaffold-fill.md` Passo 0 (lines ~17-19) — the exact dual-context precedence prose to mirror as a bash file-existence check: prefer `client/CLAUDE.md` if present, else `CLAUDE.md`
    - `.claude/settings.json` `_disabled_note` (line 3) — confirms no `SessionStart`/`UserPromptSubmit`/`PreToolUse` hook currently invokes this script; this task must not add or restore any such registration
  </read_first>
  <action>
Replace lines 12-13:
```
PROJECT_DIR="${CLAUDE_PROJECT_DIR:-$(pwd)}"
CLAUDE_MD="$PROJECT_DIR/CLAUDE.md"
```
with a dual-context fallback: define `CLIENT_CLAUDE_MD="$PROJECT_DIR/client/CLAUDE.md"` and `ROOT_CLAUDE_MD="$PROJECT_DIR/CLAUDE.md"`, then set `CLAUDE_MD` to `CLIENT_CLAUDE_MD` when that path exists (`[ -f "$CLIENT_CLAUDE_MD" ]`), otherwise to `ROOT_CLAUDE_MD` — mirroring the exact same client-first-else-root precedence already used by `client-scaffold-fill.md`, `client-scaffold-collect.md`, `client-scaffold-structure.md`, and `.claude/commands/ei-ajustes.md`, just expressed as a bash file-existence check instead of agent-instruction prose (this is a hook script, not an LLM prompt). All downstream uses of `$CLAUDE_MD` later in the file (the `if [ -f "$CLAUDE_MD" ]` block around line 34) continue to work unchanged since `CLAUDE_MD` still resolves to a single path.

Update the "not found" warning (line 40, inside the `else` branch of the `if [ -f "$CLAUDE_MD" ]` check): current text is "[aviso] CLAUDE.md não encontrado em $CLAUDE_MD" — since `$CLAUDE_MD` now only ever holds ONE of the two paths (whichever resolution picked), the warning must reflect that BOTH paths were checked, not just the one that lost. Change it to report both `$CLIENT_CLAUDE_MD` and `$ROOT_CLAUDE_MD` explicitly, e.g. "[aviso] CLAUDE.md não encontrado em $CLIENT_CLAUDE_MD nem em $ROOT_CLAUDE_MD".

Do NOT modify `.claude/settings.json` and do NOT add any `SessionStart`/`UserPromptSubmit`/`PreToolUse` hook registration anywhere — re-enabling this script is explicitly out of scope (CONTEXT.md Deferred). This task only fixes the fallback logic inside the still-dormant script.
  </action>
  <verify>
    <automated>bash -n .claude/hooks/inject-ei-context.sh && grep -q 'client/CLAUDE.md' .claude/hooks/inject-ei-context.sh && grep -q 'CLIENT_CLAUDE_MD' .claude/hooks/inject-ei-context.sh && ! grep -q '"SessionStart"' .claude/settings.json && ! grep -q '"UserPromptSubmit"' .claude/settings.json && ! grep -q '"PreToolUse"' .claude/settings.json && TESTDIR=$(mktemp -d) && mkdir -p "$TESTDIR/client" && echo CLIENT_VERSION > "$TESTDIR/client/CLAUDE.md" && echo ROOT_VERSION > "$TESTDIR/CLAUDE.md" && OUT1=$(CLAUDE_PROJECT_DIR="$TESTDIR" bash .claude/hooks/inject-ei-context.sh editor) && echo "$OUT1" | grep -q CLIENT_VERSION && ! echo "$OUT1" | grep -q ROOT_VERSION && rm "$TESTDIR/client/CLAUDE.md" && OUT2=$(CLAUDE_PROJECT_DIR="$TESTDIR" bash .claude/hooks/inject-ei-context.sh editor) && echo "$OUT2" | grep -q ROOT_VERSION && rm -rf "$TESTDIR" && echo PASS</automated>
  </verify>
  <done>inject-ei-context.sh prefers client/CLAUDE.md when present and falls back to root CLAUDE.md when absent, verified by a functional test with both fixture layouts; the "not found" warning names both checked paths; the script remains unregistered in .claude/settings.json.</done>
  <acceptance_criteria>
    - `bash -n .claude/hooks/inject-ei-context.sh` passes (no syntax errors)
    - Running the script with `CLAUDE_PROJECT_DIR` pointing at a fixture that has both `client/CLAUDE.md` and root `CLAUDE.md` outputs the client version's content, not the root version's
    - Running the script with only root `CLAUDE.md` present falls back and outputs the root version's content
    - `.claude/settings.json` has no `SessionStart`, `UserPromptSubmit`, or `PreToolUse` hook block (unchanged from before this task)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan touches no active runtime trust boundary. `recepcionista-scaffolder.md` is a Markdown prompt read by an LLM subagent (no code execution, no user input parsed at file-read time). `inject-ei-context.sh` is a bash script that is currently unregistered in `.claude/settings.json` (confirmed: only `Stop` and `SubagentStop` hooks exist; no `SessionStart`/`UserPromptSubmit`/`PreToolUse` block invokes it) — it reads local files via a `CLAUDE_PROJECT_DIR`/`pwd`-derived path and writes only to stdout/stderr; it does not accept untrusted external input.

| Boundary | Description |
|----------|--------------|
| None (new) | No new trust boundary is introduced by this plan; the edited files are either a documentation-only subagent prompt or a disabled, output-only hook script |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------------|------------------|
| T-03.1-01 | Tampering (doc-guided misbehavior) | `.claude/agents/recepcionista-scaffolder.md` Fase 5 prose | medium | mitigate | Task 1 is itself the mitigation: correcting the factually-backwards claim prevents a future LLM run from telling a human that a completed step "still needs to run," which could cause a duplicate/confused scaffolding attempt |
| T-03.1-02 | Tampering (wrong-context read) | `.claude/hooks/inject-ei-context.sh` path resolution, if ever re-enabled | low | mitigate | Task 2 applies the dual-context fallback now, before any future re-enablement, so the script cannot silently inject repo-internal (`client-scaffold-fill.md`-owner) content into a client project's context or vice versa; re-enablement itself remains explicitly out of scope this phase |
| T-03.1-03 | Information Disclosure | `manifest.json` `deprecated_files` entry, `CHANGELOG.md` historical mentions of the retired subagent | low | accept | Explicitly out of scope per CONTEXT.md: `manifest.json`'s entry is the correct, unrelated mechanism removing the dead file from client installs; `CHANGELOG.md` is a historical record and editing it would misrepresent project history |

</threat_model>

<verification>
- `grep -c 'client-project-scaffolder' .claude/agents/recepcionista-scaffolder.md` returns 0
- `.claude/agents/recepcionista-scaffolder.md` line 147 no longer claims a scaffolding subagent "still needs to run"
- `inject-ei-context.sh` functional test (both fixture layouts) confirms client-first-else-root precedence
- `.claude/settings.json` has zero `SessionStart`/`UserPromptSubmit`/`PreToolUse` hook blocks (unchanged)
</verification>

<success_criteria>
- `.claude/agents/recepcionista-scaffolder.md` accurately describes the current 3-subagent scaffolding chain, with no factually-backwards claims
- `.claude/hooks/inject-ei-context.sh` has the same dual-context CLAUDE.md fallback as every other consumer in the repo, verified functionally, with zero change to its (disabled) registration status
</success_criteria>

<artifacts_produced>
## Artifacts This Plan Produces

None — this plan modifies existing files only, creates no new files/functions/CLI flags/symbols.

**Modified:**
- `.claude/agents/recepcionista-scaffolder.md` — 4 stale-reference corrections (3 renames, 1 factual rewrite)
- `.claude/hooks/inject-ei-context.sh` — dual-context CLAUDE.md fallback added to path resolution and warning message
</artifacts_produced>

<output>
Create `.planning/phases/03.1-corrigir-refer-ncias-obsoletas-a-client-project-scaffolder-e/03.1-01-SUMMARY.md` when done
</output>
