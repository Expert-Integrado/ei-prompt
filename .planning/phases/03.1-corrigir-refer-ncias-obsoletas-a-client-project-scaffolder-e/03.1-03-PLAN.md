---
phase: 03.1
plan: 03
type: execute
wave: 2
depends_on: ["03.1-02"]
files_modified:
  - client/CLAUDE.md
  - .planning/PROJECT.md
  - .planning/STATE.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/CONVENTIONS.md
  - .planning/codebase/INTEGRATIONS.md
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "`client/CLAUDE.md` (the npm-distributed client payload) has zero occurrences of the retired `client-project-scaffolder` subagent name; the post-scaffolder-review.sh hook-trigger claim names `client-scaffold-fill` instead, matching the hook's actual case-statement trigger"
    - "`.planning/PROJECT.md`'s Core Value line and `.planning/STATE.md`'s 'Core value:' bullet both name `client-scaffold-fill` instead of the retired subagent, matching the wording already corrected in `.claude/CLAUDE.md` line 9 by plan 03.1-02"
    - "`.planning/codebase/{ARCHITECTURE,STRUCTURE,CONVENTIONS,INTEGRATIONS}.md` have zero occurrences of the retired subagent name; every table row, tree entry, and prose mention cites the correct current subagent(s) (`client-scaffold-structure`/`client-scaffold-collect`/`client-scaffold-fill`), matching the same defect-fix pattern already applied to `.claude/CLAUDE.md` and `COMANDOS.md`"
    - "Toda referência à arquitetura de scaffolding retirada (`client-project-scaffolder`) está corrigida para os 3 subagents atuais em toda a documentação e prompts CORRENTES do repositório (current-state docs, not historical records) — a scoped repo-wide grep excluding `.git/`, historical phase artifacts (`.planning/phases/01-*`, `.planning/phases/02-*`, `.planning/phases/03.1-*`), the milestone audit, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/codebase/CONCERNS.md`, `.planning/STATE.md`'s own phase-slug/history lines, and `CHANGELOG.md` returns empty"
  artifacts: []
  key_links:
    - from: "client/CLAUDE.md"
      to: ".claude/hooks/post-scaffolder-review.sh"
      via: "hook-trigger name in prose matches the hook's actual case-statement match on client-scaffold-fill"
      pattern: "client-scaffold-fill"
    - from: ".planning/PROJECT.md"
      to: ".claude/CLAUDE.md"
      via: "Core Value wording kept in sync between the project's canonical source and its already-corrected mirror"
      pattern: "client-scaffold-fill"
---

<objective>
Close the single remaining gap from `03.1-VERIFICATION.md` (truth #7, status FAILED): the phase's own ROADMAP goal — "toda referência à arquitetura de scaffolding retirada (`client-project-scaffolder`) seja corrigida para os 3 subagents atuais... em toda a documentação e prompts do repositório" — is not yet fully satisfied. Plans 03.1-01 and 03.1-02 correctly and completely fixed the 4 files named in `03.1-CONTEXT.md` Decision 1 (`.claude/agents/recepcionista-scaffolder.md`, `.claude/hooks/inject-ei-context.sh`, `.claude/CLAUDE.md`, `COMANDOS.md`), but `03.1-CONTEXT.md`'s own repo-wide grep excluded the entire `.planning/` tree and never re-scanned after `client/CLAUDE.md` was created — missing 7 more current-state files that still name the retired `client-project-scaffolder` subagent as if it were live: `client/CLAUDE.md` (highest priority — the actual npm-distributed client payload), `.planning/PROJECT.md`, `.planning/STATE.md`, and 4 GSD-generated codebase-mapping docs (`ARCHITECTURE.md`, `STRUCTURE.md`, `CONVENTIONS.md`, `INTEGRATIONS.md`).

Purpose: `client-project-scaffolder` was retired in Phase 2 (split into `client-scaffold-structure` -> `client-scaffold-collect` -> `client-scaffold-fill`). Every location that still names it as if it were the live architecture risks guiding a future human or LLM session (including a fresh Claude Code session reading `.claude/CLAUDE.md`'s "Project instructions" or a newly-scaffolded client reading its own `CLAUDE.md`) with instructions describing a flow that no longer exists. `client/CLAUDE.md` is the single highest-materiality miss: it is a real prompt shipped to every new client project via `manifest.json`'s `{"from": "client/CLAUDE.md", "to": "CLAUDE.md"}` entry, not an internal reference doc.
Output: `client/CLAUDE.md`, `.planning/PROJECT.md`, `.planning/STATE.md`, and `.planning/codebase/{ARCHITECTURE,STRUCTURE,CONVENTIONS,INTEGRATIONS}.md` corrected; zero behavior change (documentation-only, same as plans 03.1-01/03.1-02).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-corrigir-refer-ncias-obsoletas-a-client-project-scaffolder-e/03.1-CONTEXT.md
@.planning/phases/03.1-corrigir-refer-ncias-obsoletas-a-client-project-scaffolder-e/03.1-VERIFICATION.md
@client/CLAUDE.md
@.claude/CLAUDE.md
@.claude/hooks/post-scaffolder-review.sh
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/INTEGRATIONS.md
@.claude/agents/client-scaffold-structure.md
@.claude/agents/client-scaffold-collect.md
@.claude/agents/client-scaffold-fill.md
</context>

<tasks>

<!-- planner-discipline-allow: client-project-scaffolder -->

<task type="auto">
  <name>Task 1: Fix client/CLAUDE.md (npm-distributed payload, highest priority) + PROJECT.md + STATE.md Core Value/value lines</name>
  <files>client/CLAUDE.md, .planning/PROJECT.md, .planning/STATE.md</files>
  <read_first>
    - `client/CLAUDE.md` line 76 (the "Pipeline via hook (Stop event)" bullet under "Regras Básicas") — the edit target; this is the actual npm-distributed file, ships to every new client via `manifest.json`'s `{from: "client/CLAUDE.md", to: "CLAUDE.md"}` entry
    - `.claude/hooks/post-scaffolder-review.sh` lines 40-41 (`case "$LAST_SUBAGENT" in client-scaffold-fill)`) — ground truth confirming the hook's actual SubagentStop trigger is `client-scaffold-fill`, not the retired name
    - `.claude/CLAUDE.md` line 9 (Core Value, already corrected by plan 03.1-02) — exact wording to mirror verbatim in `.planning/PROJECT.md`
    - `.planning/PROJECT.md` line 9 (Core Value section) — edit target
    - `.planning/STATE.md` line 26 ("Core value:" bullet) — edit target
  </read_first>
  <action>
Apply exactly 3 edits, all mechanical renames — same defect class already fixed in `.claude/agents/recepcionista-scaffolder.md`, `.claude/CLAUDE.md`, and `COMANDOS.md` by plans 03.1-01/03.1-02, applied here to 3 more current-state files the original repo-wide grep missed (it excluded `.planning/` entirely and never re-scanned after `client/CLAUDE.md` was created):

1. `client/CLAUDE.md` line 76 — within the "Pipeline via hook (Stop event)" bullet, the clause "Hook antigo `.claude/hooks/post-scaffolder-review.sh` (SubagentStop, `client-project-scaffolder`) ganha guarda silenciosa que detecta o sentinela e sai exit 0 (D-11 — coexistência sem conflito)." names the retired subagent as the hook's SubagentStop trigger. Replace `client-project-scaffolder` with `client-scaffold-fill` — confirmed via `post-scaffolder-review.sh`'s own `case` statement, which matches on `client-scaffold-fill`, not the retired name. This is the highest-priority fix in this plan: `client/CLAUDE.md` is the file `manifest.json` ships as `CLAUDE.md` to every newly-scaffolded client project, so a stale claim here reaches real client repos, not just this maintainer repo. Rest of the sentence unchanged.

2. `.planning/PROJECT.md` line 9 (Core Value section) currently reads: "Um `docs-editor-conciso` ou `client-project-scaffolder` nunca deve conseguir gerar/deixar um arquivo de cliente com XML quebrado (casca removida, invertida ou malformada) sem que isso seja pego automaticamente — hoje isso só é "pego" se o `docs-reviewer` (LLM) lembrar de checar a checklist manual do `docs/regras-validacao.md`." Replace the entire line verbatim with `.claude/CLAUDE.md` line 9's already-corrected wording — the identical sentence with only `client-project-scaffolder` swapped for `client-scaffold-fill`, no other word changed — so both "Core Value" statements stay in sync. `.planning/PROJECT.md` is the canonical source `.claude/CLAUDE.md`'s Core Value line was itself derived from; the two must match.

3. `.planning/STATE.md` line 26 ("Core value:" bullet) currently reads: "**Core value:** Um `docs-editor-conciso` ou `client-project-scaffolder` nunca deve conseguir gerar/deixar um arquivo de cliente com XML quebrado sem que isso seja pego automaticamente por código." This is STATE.md's own condensed wording (shorter than PROJECT.md's full sentence) — do NOT replace it with PROJECT.md's longer text. Only rename the agent: replace `client-project-scaffolder` with `client-scaffold-fill`, keep the rest of STATE.md's own sentence exactly as-is (including its trailing "...sem que isso seja pego automaticamente por código.").

Do not touch any other occurrence of the retired name in these 3 files. `.planning/STATE.md` legitimately retains other mentions that must NOT be edited: its own phase-directory slug appears verbatim in lines 6, 27, and 31 (`corrigir-refer-ncias-obsoletas-a-client-project-scaffolder-e` is this phase's literal folder name, describing "the phase that fixes references to X" — not a live-architecture claim), and lines 84 and 106 narrate past-tense decision/roadmap-evolution history ("aposentado via `deprecated_files`", "Corrigir referências obsoletas a client-project-scaffolder em..." describing this very phase's insertion) — both are correct historical narration, not stale claims.
  </action>
  <verify>
    <automated>[ "$(grep -c 'client-project-scaffolder' client/CLAUDE.md)" = "0" ] && grep -q 'client-scaffold-fill' client/CLAUDE.md && [ "$(grep -c 'client-project-scaffolder' .planning/PROJECT.md)" = "0" ] && grep -q 'client-scaffold-fill' .planning/PROJECT.md && grep -qE 'Core value:.*client-scaffold-fill' .planning/STATE.md && ! grep -qE 'Core value:.*client-project-scaffolder' .planning/STATE.md && echo PASS</automated>
  </verify>
  <done>client/CLAUDE.md's hook-trigger claim, PROJECT.md's Core Value line, and STATE.md's Core value bullet all name client-scaffold-fill instead of the retired subagent; STATE.md's other legitimate (historical/slug) mentions of the retired name are left untouched.</done>
  <acceptance_criteria>
    - `grep -c 'client-project-scaffolder' client/CLAUDE.md` returns 0
    - `grep -c 'client-project-scaffolder' .planning/PROJECT.md` returns 0
    - `.planning/STATE.md` line 26 ("Core value:" bullet) contains `client-scaffold-fill`, not `client-project-scaffolder`
    - `.planning/STATE.md` lines 6, 27, 31, 84, 106 (phase slug + historical decision log) are unmodified
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Sweep .planning/codebase/{ARCHITECTURE,STRUCTURE,CONVENTIONS,INTEGRATIONS}.md for the same defect class</name>
  <files>.planning/codebase/ARCHITECTURE.md, .planning/codebase/STRUCTURE.md, .planning/codebase/CONVENTIONS.md, .planning/codebase/INTEGRATIONS.md</files>
  <read_first>
    - `.planning/codebase/ARCHITECTURE.md` (full file — lines 53, 58, 89, 110 are the 4 edit targets)
    - `.planning/codebase/STRUCTURE.md` (full file — lines 30, 122, 135 are the 3 edit targets)
    - `.planning/codebase/CONVENTIONS.md` (full file — line 18 is the 1 edit target)
    - `.planning/codebase/INTEGRATIONS.md` (full file — lines 79, 82 are the 2 edit targets)
    - `.claude/CLAUDE.md` lines 145-147, 150, 175 (the already-corrected Component Responsibilities 3-row split, post-scaffolder-review.sh row, and Layers "Used by" line) — exact target pattern to mirror in ARCHITECTURE.md
    - `.claude/agents/client-scaffold-structure.md`, `.claude/agents/client-scaffold-collect.md`, `.claude/agents/client-scaffold-fill.md` frontmatter (`description`, `model`) — source of truth for each subagent's real responsibility and declared model, needed for the STRUCTURE.md/ARCHITECTURE.md/INTEGRATIONS.md replacements (confirmed: client-scaffold-structure.md declares `model: sonnet`; client-scaffold-collect.md and client-scaffold-fill.md both declare `model: opus`)
  </read_first>
  <action>
Apply the same defect-fix pattern already used across all 4 previously-fixed files (`.claude/agents/recepcionista-scaffolder.md`, `.claude/hooks/inject-ei-context.sh`, `.claude/CLAUDE.md`, `COMANDOS.md` by plans 03.1-01/03.1-02) to these 4 GSD-generated codebase-mapping docs — every occurrence of the retired `client-project-scaffolder` becomes the correct current subagent(s) (`client-scaffold-structure`/`client-scaffold-collect`/`client-scaffold-fill`), matching each file's existing table/list/tree format. Do not regenerate or restructure any other part of these 4 files (this is a factual-correction sweep, not a `/gsd-docs-update` regeneration pass) — touch only the 10 lines/cells listed below.

ARCHITECTURE.md — 4 edits:
1. Line 53 (Component Responsibilities table): the single row `| client-project-scaffolder subagent | Generates a full single-agent client stack from modelo/ | .claude/agents/client-project-scaffolder.md |` describes a component that no longer exists as ONE subagent. Replace it with the same 3-row split already applied to `.claude/CLAUDE.md` lines 145-147, same table format (`| Component | Responsibility | File |`): a `client-scaffold-structure` subagent row ("Creates the client folder skeleton and copies modelo/*.md templates verbatim, asking no questions" / `.claude/agents/client-scaffold-structure.md`), a `client-scaffold-collect` subagent row ("Reads the copied templates, collects every required field (including media) conversationally, returns a structured <dados_coletados> block; read-only, never writes files" / `.claude/agents/client-scaffold-collect.md`), and a `client-scaffold-fill` subagent row ("Fills the copied templates' placeholders with the collected data, preserving {{variavel}} syntax and the pending marker for anything unanswered; non-interactive" / `.claude/agents/client-scaffold-fill.md`).
2. Line 58 (post-scaffolder-review.sh hook row): replace `client-project-scaffolder` with `client-scaffold-fill` — rest of the cell unchanged.
3. Line 89 (Layers section, Template layer "Used by"): "Used by: client-project-scaffolder, recepcionista-scaffolder subagents" becomes "Used by: client-scaffold-structure, recepcionista-scaffolder subagents" — client-scaffold-structure is the subagent that actually copies modelo/*.md verbatim today.
4. Line 110 (Data Flow, "Primary Path: New Client Creation", step 2): "Command determines single- vs multi-agent mode and dispatches client-project-scaffolder (per specialty, looped for multi-agent) (.claude/agents/client-project-scaffolder.md)" describes the retired single-dispatch flow. Replace with: "Command determines single- vs multi-agent mode, then for each specialty (or the single client, in single-agent mode) sequentially dispatches client-scaffold-structure (creates the folder + copies templates) then client-scaffold-collect (collects data) then a human confirmation gate (AskUserQuestion) then client-scaffold-fill (fills the templates) (.claude/agents/client-scaffold-structure.md, .claude/agents/client-scaffold-collect.md, .claude/agents/client-scaffold-fill.md)". Do not touch step 3 of this same Data Flow list — it does not name the retired subagent and is out of this task's scope.

STRUCTURE.md — 3 edits:
5. Line 30 (Directory Layout tree, under `.claude/agents/`): the single tree entry `client-project-scaffolder.md` describes a deleted file. Replace that one tree line with 3 tree lines, one per current scaffolding subagent, in this exact order immediately above the existing `recepcionista-scaffolder.md` line: `client-scaffold-structure.md`, `client-scaffold-collect.md`, `client-scaffold-fill.md`. Preserve the existing box-drawing tree prefix used by sibling lines in that block (the same `│   │   ├── ` prefix already used for `recepcionista-scaffolder.md`) for each of the 3 new lines, and keep every other line in the tree — including the final `└── docs-reviewer.md` — exactly as-is.
6. Line 122 (Naming Conventions, "Subagents:" bullet): "Subagents: kebab-case, descriptive of role — client-project-scaffolder.md, docs-analyzer.md, docs-editor-conciso.md, docs-reviewer.md" — replace the dead filename with `client-scaffold-fill.md`, keeping the rest of the list unchanged (mirrors the same list-example treatment already applied to `.claude/CLAUDE.md` line 73).
7. Line 135 ("Where to Add New Code", "New agent persona template" bullet): "Reference it from any relevant scaffolder subagent (.claude/agents/client-project-scaffolder.md or recepcionista-scaffolder.md)" — replace the dead path with `.claude/agents/client-scaffold-structure.md` (the subagent that actually copies modelo/*.md templates today), keeping "or recepcionista-scaffolder.md" unchanged.

CONVENTIONS.md — 1 edit:
8. Line 18 (Naming Patterns, "Claude subagents:" bullet): "...`.claude/agents/docs-reviewer.md`, `.claude/agents/client-project-scaffolder.md`, `.claude/agents/recepcionista-scaffolder.md`." — replace the dead path with `.claude/agents/client-scaffold-fill.md`, keeping the rest of the comma-separated list unchanged (identical treatment to `.claude/CLAUDE.md` line 73).

INTEGRATIONS.md — 2 edits:
9. Line 79 ("Claude Code Integration" bullet list): "`.claude/hooks/post-scaffolder-review.sh` — SubagentStop hook guarding against loops after the client-project-scaffolder subagent." — replace the retired agent name with `client-scaffold-fill`.
10. Line 82 ("Subagents with model: frontmatter" bullet): "Subagents with model: frontmatter (opus, etc.): .claude/agents/client-project-scaffolder.md, docs-editor-conciso.md, docs-reviewer.md, docs-analyzer.md, recepcionista-scaffolder.md — these run under the Claude API..." — replace the single dead entry with all 3 current scaffolding subagents, each of which does declare its own `model:` frontmatter field (client-scaffold-structure.md is `sonnet`; client-scaffold-collect.md and client-scaffold-fill.md are both `opus` — this also makes the existing "opus, etc." qualifier in the lead-in accurate, since the list now genuinely mixes sonnet and opus). Keep the same relative-path style already used for the other list items: `.claude/agents/client-scaffold-structure.md`, `.claude/agents/client-scaffold-collect.md`, `.claude/agents/client-scaffold-fill.md`, `docs-editor-conciso.md`, `docs-reviewer.md`, `docs-analyzer.md`, `recepcionista-scaffolder.md`.
  </action>
  <verify>
    <automated>for f in .planning/codebase/ARCHITECTURE.md .planning/codebase/STRUCTURE.md .planning/codebase/CONVENTIONS.md .planning/codebase/INTEGRATIONS.md; do [ "$(grep -c 'client-project-scaffolder' "$f")" = "0" ] || { echo "FAIL: $f"; exit 1; }; done && grep -q 'client-scaffold-structure' .planning/codebase/ARCHITECTURE.md && grep -q 'client-scaffold-collect' .planning/codebase/ARCHITECTURE.md && grep -q 'client-scaffold-fill' .planning/codebase/ARCHITECTURE.md && grep -q 'client-scaffold-structure.md' .planning/codebase/STRUCTURE.md && grep -q 'client-scaffold-collect.md' .planning/codebase/STRUCTURE.md && grep -q 'client-scaffold-fill.md' .planning/codebase/STRUCTURE.md && grep -q 'client-scaffold-fill.md' .planning/codebase/CONVENTIONS.md && grep -q 'client-scaffold-structure.md' .planning/codebase/INTEGRATIONS.md && grep -q 'client-scaffold-collect.md' .planning/codebase/INTEGRATIONS.md && grep -q 'client-scaffold-fill.md' .planning/codebase/INTEGRATIONS.md && echo PASS</automated>
  </verify>
  <done>All 4 GSD-generated codebase-mapping docs have zero remaining mentions of the retired subagent name; every table row, tree entry, naming-pattern example, and prose mention now cites the correct current subagent(s), matching the same defect-fix pattern already verified in .claude/CLAUDE.md, COMANDOS.md, and recepcionista-scaffolder.md.</done>
  <acceptance_criteria>
    - `grep -c 'client-project-scaffolder' .planning/codebase/ARCHITECTURE.md` returns 0; Component Responsibilities table has 3 rows for the current scaffolding subagents; the post-scaffolder-review.sh row, Layers "Used by" line, and Data Flow step 2 all cite current subagent names
    - `grep -c 'client-project-scaffolder' .planning/codebase/STRUCTURE.md` returns 0; the directory tree lists the 3 current scaffolding subagent files instead of the retired one; the Naming Conventions and "Where to Add New Code" mentions cite current subagents
    - `grep -c 'client-project-scaffolder' .planning/codebase/CONVENTIONS.md` returns 0
    - `grep -c 'client-project-scaffolder' .planning/codebase/INTEGRATIONS.md` returns 0; both the hook description and the model-frontmatter subagent list cite current subagents
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan touches no runtime trust boundary. All 7 edited files are static documentation/prompt text: `client/CLAUDE.md` autoloads as "Project instructions" for whoever's Claude Code session is rooted at a scaffolded client folder (no code execution, no user input parsed at file-read time); `.planning/PROJECT.md` and `.planning/STATE.md` are GSD planning artifacts read by future planning/context-gathering sessions; the 4 `.planning/codebase/*.md` files are GSD-generated codebase-mapping docs read the same way. None of the 7 files are fetched over a network boundary by this plan's edits, and none accept untrusted external input.

| Boundary | Description |
|----------|--------------|
| None (new) | No new trust boundary is introduced; every edited file is static prose read by a human or an LLM session, not executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------------|------------------|
| T-03.1-07 | Tampering (doc-guided misbehavior) | `client/CLAUDE.md`'s post-scaffolder-review.sh hook-trigger claim (npm-distributed to every new client project) | medium | mitigate | Task 1 corrects the claim to name `client-scaffold-fill`, preventing a future client-side Claude Code session (or a maintainer reading the distributed payload) from believing a retired subagent still exists as the hook's trigger |
| T-03.1-08 | Tampering (doc-guided misbehavior) | `.planning/PROJECT.md` and `.planning/STATE.md` Core Value statements | low | mitigate | Task 1 syncs both statements with the already-corrected `.claude/CLAUDE.md` wording, removing an internal inconsistency between the project's own source-of-truth documents |
| T-03.1-09 | Tampering (doc-guided misbehavior) | `.planning/codebase/{ARCHITECTURE,STRUCTURE,CONVENTIONS,INTEGRATIONS}.md` | low | mitigate | Task 2 corrects GSD-generated codebase-mapping docs that future planning/context-gathering passes read as ground truth; leaving them stale would keep misleading future sessions until an unplanned `/gsd-map-codebase` rerun |
| T-03.1-10 | Information Disclosure | `manifest.json` deprecated_files entry, `CHANGELOG.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/v1.0-v1.0-MILESTONE-AUDIT.md`, `.planning/codebase/CONCERNS.md`, and this phase's own planning artifacts (`03.1-CONTEXT.md`/`03.1-01-PLAN.md`/`03.1-01-SUMMARY.md`/`03.1-02-PLAN.md`/`03.1-02-SUMMARY.md`/`03.1-VERIFICATION.md`/this plan) | low | accept | Explicitly out of scope: `manifest.json`'s entry is the correct, unrelated mechanism removing the dead file from client installs; `CHANGELOG.md`, `MILESTONE-AUDIT.md`, `REQUIREMENTS.md`, and `codebase/CONCERNS.md` are historical records that would be revisionist to edit; `.planning/ROADMAP.md`'s Phase 03.1 title/goal legitimately names the defect this very phase fixes; this phase's own planning artifacts (CONTEXT/PLAN/SUMMARY/VERIFICATION) correctly narrate the retired name as the subject of the fix; `.planning/STATE.md`'s phase-slug (lines 6, 27, 31) and historical decision-log lines (84, 106) fall in the same accepted-legitimate-mention category |

</threat_model>

<verification>
Per-file checks (see each task's `<verify>`), plus a scoped repo-wide regression grep that closes the phase's own ROADMAP goal ("toda a documentação e prompts do repositório") without false-positiving on legitimate historical/self-referential content:

grep -rn "client-project-scaffolder" . --include="*.md" --include="*.sh" --exclude-dir=.git \
  | grep -v "CHANGELOG.md" \
  | grep -v ".planning/phases/01-" \
  | grep -v ".planning/phases/02-" \
  | grep -v ".planning/phases/03.1-" \
  | grep -v ".planning/v1.0-v1.0-MILESTONE-AUDIT.md" \
  | grep -v ".planning/REQUIREMENTS.md" \
  | grep -v ".planning/ROADMAP.md" \
  | grep -v ".planning/codebase/CONCERNS.md" \
  | grep -v ".planning/STATE.md"

Expected: empty output. The exclusions are principled, not scope reduction — every excluded path is either (a) this phase's own historical artifacts narrating the defect it fixes, (b) other pre-existing historical records (audit, changelog, past-tense requirement descriptions), (c) the ROADMAP's own phase-title/goal text naming the defect, or (d) `.planning/STATE.md`'s phase-directory slug and historical decision log — none of these are "current-state architecture" claims, so a naive un-scoped grep would report false gaps forever. `.planning/STATE.md` line 26 specifically is verified separately in Task 1's `<verify>` (region-scoped, not file-wide, since the rest of STATE.md legitimately keeps the string elsewhere).
</verification>

<success_criteria>
- `client/CLAUDE.md` (the actual npm-distributed client payload) no longer misnames the post-scaffolder-review.sh hook's trigger
- `.planning/PROJECT.md` and `.planning/STATE.md` Core Value statements are consistent with the already-corrected `.claude/CLAUDE.md` wording
- All 4 `.planning/codebase/*.md` GSD-generated docs accurately describe the current 3-subagent scaffolding chain
- The scoped repo-wide regression grep (see `<verification>`) returns empty, closing `03.1-VERIFICATION.md` truth #7 and the phase's ROADMAP goal
</success_criteria>

<artifacts_produced>
## Artifacts This Plan Produces

None — this plan only edits existing files, creates no new files/functions/CLI flags/symbols.

**Modified:**
- `client/CLAUDE.md` — 1 stale-reference correction (hook-trigger name)
- `.planning/PROJECT.md` — 1 stale-reference correction (Core Value line, synced with `.claude/CLAUDE.md`)
- `.planning/STATE.md` — 1 stale-reference correction (Core value bullet)
- `.planning/codebase/ARCHITECTURE.md` — 4 stale-reference corrections (1 table-row split into 3, 3 renames)
- `.planning/codebase/STRUCTURE.md` — 3 stale-reference corrections (1 tree-entry split into 3, 2 renames)
- `.planning/codebase/CONVENTIONS.md` — 1 stale-reference correction (rename)
- `.planning/codebase/INTEGRATIONS.md` — 2 stale-reference corrections (1 rename, 1 list-entry split into 3)
</artifacts_produced>

<output>
Create `.planning/phases/03.1-corrigir-refer-ncias-obsoletas-a-client-project-scaffolder-e/03.1-03-SUMMARY.md` when done
</output>
