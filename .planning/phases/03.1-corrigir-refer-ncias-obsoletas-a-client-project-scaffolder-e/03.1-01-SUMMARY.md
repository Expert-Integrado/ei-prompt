---
phase: 03.1-corrigir-refer-ncias-obsoletas-a-client-project-scaffolder-e
plan: 01
subsystem: docs
tags: [claude-code-agents, prompt-hygiene, bash-hooks, dual-context-fallback]

# Dependency graph
requires:
  - phase: 02-three-step-gated-client-scaffolding
    provides: retired the monolithic client-project-scaffolder subagent in favor of client-scaffold-structure -> client-scaffold-collect -> client-scaffold-fill
  - phase: 03-separar-claude-md-distribuido-do-interno
    provides: established the client/CLAUDE.md-first, root-CLAUDE.md-fallback dual-context pattern across client-scaffold-fill.md, client-scaffold-collect.md, client-scaffold-structure.md, and ei-ajustes.md
provides:
  - recepcionista-scaffolder.md with zero stale references to the retired client-project-scaffolder subagent
  - Factually-correct Fase 5 confirmation text describing the actual structure->collect->fill sequencing
  - inject-ei-context.sh dual-context CLAUDE.md fallback matching every other consumer in the repo
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Dual-context CLAUDE.md fallback expressed as a bash file-existence check ([ -f \"$CLIENT_CLAUDE_MD\" ]) rather than agent-instruction prose, since inject-ei-context.sh is a hook script, not an LLM prompt"

key-files:
  created: []
  modified:
    - .claude/agents/recepcionista-scaffolder.md
    - .claude/hooks/inject-ei-context.sh

key-decisions:
  - "Followed PLAN.md's 4 exact edit targets verbatim (3 mechanical renames + 1 factual rewrite of the Fase 5 confirmation line) — no additional lines touched"
  - "inject-ei-context.sh remains unregistered in .claude/settings.json — this is a latent-risk-only fix, not a re-enablement, per CONTEXT.md Deferred scope"

patterns-established: []

requirements-completed: []

coverage:
  - id: D1
    description: "recepcionista-scaffolder.md has zero occurrences of the retired client-project-scaffolder name; frontmatter description and body (lines 3, 16, 18) reference client-scaffold-structure instead"
    verification:
      - kind: other
        ref: "grep -c 'client-project-scaffolder' .claude/agents/recepcionista-scaffolder.md returns 0; grep -c 'client-scaffold-structure' returns >= 3"
        status: pass
    human_judgment: false
  - id: D2
    description: "recepcionista-scaffolder.md Fase 5 confirmation text no longer claims a scaffolding subagent 'still needs to run' — it accurately states every specialty folder was already created and filled before Fase 5 runs"
    verification:
      - kind: other
        ref: "grep -q 'não há nenhum passo de scaffolding pendente' passes; grep -q 'ainda precisa rodar' fails (absent)"
        status: pass
    human_judgment: false
  - id: D3
    description: "inject-ei-context.sh prefers client/CLAUDE.md when present and falls back to root CLAUDE.md when absent, mirroring client-scaffold-fill.md's dual-context pattern"
    verification:
      - kind: other
        ref: "bash -n inject-ei-context.sh syntax check; functional test with both fixture layouts (client/CLAUDE.md present -> outputs client content; client/CLAUDE.md absent -> falls back to root content)"
        status: pass
    human_judgment: false
  - id: D4
    description: "inject-ei-context.sh remains unregistered in .claude/settings.json — no SessionStart/UserPromptSubmit/PreToolUse hook added"
    verification:
      - kind: other
        ref: "grep -c '\"SessionStart\"'/'\"UserPromptSubmit\"'/'\"PreToolUse\"' .claude/settings.json all return 0"
        status: pass
    human_judgment: false

# Metrics
duration: 2min
completed: 2026-07-16
status: complete
---

# Phase 03.1 Plan 01: Fix stale client-project-scaffolder references and add CLAUDE.md dual-context fallback Summary

**Renamed 3 retired-subagent mentions + rewrote a factually-backwards confirmation line in recepcionista-scaffolder.md, and added the client/CLAUDE.md-first fallback to inject-ei-context.sh matching every other consumer in the repo**

## Performance

- **Duration:** 2 min
- **Started:** 2026-07-16T14:50:33Z
- **Completed:** 2026-07-16T14:51:59Z
- **Tasks:** 2 completed
- **Files modified:** 2

## Accomplishments
- `recepcionista-scaffolder.md` no longer references the Phase 2-retired `client-project-scaffolder` subagent anywhere (frontmatter description, ESCOPO bullet, Fluxo completo sub-bullet all now point to `client-scaffold-structure`)
- Fase 5 confirmation text rewritten from a factually-backwards claim ("scaffolding still needs to run") to an accurate statement that the structure->collect->fill cycle already completed for every specialty
- `inject-ei-context.sh` gained the same `client/CLAUDE.md`-first, root-`CLAUDE.md`-fallback precedence already used by `client-scaffold-fill.md`, `client-scaffold-collect.md`, `client-scaffold-structure.md`, and `ei-ajustes.md` — verified functionally against both fixture layouts
- Confirmed the hook remains unregistered in `.claude/settings.json` (no `SessionStart`/`UserPromptSubmit`/`PreToolUse` block added) — this closes a latent-risk gap without activating a currently-disabled pipeline

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix stale client-project-scaffolder references in recepcionista-scaffolder.md (CONTEXT.md Decision 2)** - `f6e48ad` (docs)
2. **Task 2: Add dual-context CLAUDE.md fallback to inject-ei-context.sh (CONTEXT.md Decision 3)** - `44e7ce1` (fix)

**Plan metadata:** pending (docs: complete plan)

## Files Created/Modified
- `.claude/agents/recepcionista-scaffolder.md` - 4 stale-reference corrections: 3 mechanical renames (client-project-scaffolder -> client-scaffold-structure) plus a factual rewrite of the Fase 5 confirmation line
- `.claude/hooks/inject-ei-context.sh` - Dual-context CLAUDE.md fallback (CLIENT_CLAUDE_MD checked first, falls back to ROOT_CLAUDE_MD) and an updated "not found" warning naming both checked paths

## Decisions Made
None - plan executed exactly as written. Both tasks followed CONTEXT.md Decisions 2 and 3 verbatim.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Both v1.0 milestone audit findings targeted by this phase (stale scaffolder references, missing CLAUDE.md dual-context fallback) are resolved
- `inject-ei-context.sh` re-enablement (adding a SessionStart/UserPromptSubmit/PreToolUse hook) remains explicitly out of scope per CONTEXT.md Deferred — no blocker, just a deliberately unaddressed future decision
- No other plan currently depends on this one's output; phase 03.1 has this as its only plan (Plan 1 of 2 per STATE.md pre-execution position — confirm phase completion status against ROADMAP.md after this SUMMARY)

---
*Phase: 03.1-corrigir-refer-ncias-obsoletas-a-client-project-scaffolder-e*
*Completed: 2026-07-16*

## Self-Check: PASSED

- FOUND: .claude/agents/recepcionista-scaffolder.md
- FOUND: .claude/hooks/inject-ei-context.sh
- FOUND: .planning/phases/03.1-corrigir-refer-ncias-obsoletas-a-client-project-scaffolder-e/03.1-01-SUMMARY.md
- FOUND commit: f6e48ad
- FOUND commit: 44e7ce1
