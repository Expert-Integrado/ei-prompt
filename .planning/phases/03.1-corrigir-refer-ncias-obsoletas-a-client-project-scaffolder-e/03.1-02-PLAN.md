---
phase: 03.1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/CLAUDE.md
  - COMANDOS.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "`.claude/CLAUDE.md` contains zero occurrences of the retired subagent name; Core Value, Naming Patterns, Component Responsibilities table, the post-scaffolder-review.sh row, and the Layers 'Used by' line all reference the current 3-subagent chain (client-scaffold-structure / client-scaffold-collect / client-scaffold-fill)"
    - "`.claude/CLAUDE.md`'s Component Responsibilities table has one row per current scaffolding subagent (3 rows), not one row describing a retired single subagent"
    - "`COMANDOS.md` contains zero occurrences of the retired subagent name; the `/ei-cria-cliente` Fluxo section describes the gated 3-step flow (structure -> collect -> Gate de Confirmação -> fill) for both single-agent and multi-agent modes, including the bypass path and the consolidated final summary with per-specialty status"
    - "`COMANDOS.md`'s 'Agentes envolvidos' table lists the 3 current scaffolding subagents instead of the retired one"
  artifacts: []
  key_links:
    - from: "COMANDOS.md"
      to: ".claude/commands/ei-cria-cliente.md"
      via: "Fluxo section describes the same Passo 3/4A/4B.1/4B.2 + Gate de Confirmação sequence as the actual command"
      pattern: "Gate de Confirmação"
    - from: ".claude/CLAUDE.md"
      to: ".claude/agents/client-scaffold-structure.md"
      via: "Component Responsibilities row cites this file as the folder-skeleton creator's source"
      pattern: "client-scaffold-structure.md"
---

<objective>
Fix the remaining two documentation-drift targets identified by a repo-wide grep during `03.1-CONTEXT.md` gathering (Decision 1: same defect class as Plan 01, found beyond the audit's one named file) and locked in Decisions 4 and 5: `.claude/CLAUDE.md`'s 5 stale mentions of the retired `client-project-scaffolder` subagent (Core Value, Naming Patterns, a Component Responsibilities table row that needs splitting into 3, the post-scaffolder-review.sh row, and the Layers "Used by" line), and `COMANDOS.md`'s 2 mechanical renames plus its `/ei-cria-cliente` "Fluxo" section, which still describes the fully-retired single-subagent, no-gate, 7-phase flow verbatim (stale since Phase 2 shipped 2026-07-06).

Purpose: Both files are read as project/reference documentation — `.claude/CLAUDE.md` autoloads as this repo's "Project instructions," and `COMANDOS.md` is the canonical internal reference for how the `/ei-*` commands work. Neither should describe an architecture (one monolithic scaffolder, no confirmation gate) that stopped existing when Phase 2 shipped the 3-step gated flow. This is a correctness/consistency cleanup — no runtime behavior changes.
Output: `.claude/CLAUDE.md` and `COMANDOS.md` corrected to describe the current `client-scaffold-structure` -> `client-scaffold-collect` -> Gate de Confirmação -> `client-scaffold-fill` architecture.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-corrigir-refer-ncias-obsoletas-a-client-project-scaffolder-e/03.1-CONTEXT.md
@.claude/CLAUDE.md
@COMANDOS.md
@.claude/commands/ei-cria-cliente.md
@.claude/agents/client-scaffold-structure.md
@.claude/agents/client-scaffold-collect.md
@.claude/agents/client-scaffold-fill.md
</context>

<tasks>

<!-- planner-discipline-allow: client-project-scaffolder -->

<task type="auto">
  <name>Task 1: Fix 5 stale client-project-scaffolder references in .claude/CLAUDE.md (CONTEXT.md Decision 4)</name>
  <files>.claude/CLAUDE.md</files>
  <read_first>
    - `.claude/CLAUDE.md` (full file — lines 9, 73, 145, 150, 175 are the 5 edit targets; all fall inside the pre-existing `<!-- GSD:project-start -->`/`<!-- GSD:conventions-start -->`/`<!-- GSD:architecture-start -->` managed blocks — edit content within them directly, do not remove or move the markers)
    - `.claude/agents/client-scaffold-structure.md`, `.claude/agents/client-scaffold-collect.md`, `.claude/agents/client-scaffold-fill.md` frontmatter `description` fields — source of truth for the 3-row split at line 145 (each subagent's real scope)
  </read_first>
  <action>
Apply exactly these 5 edits, all mechanical renames except the 3rd (a table-row split, not a straight rename):

1. Line 9 (Core Value): "Um `docs-editor-conciso` ou `client-project-scaffolder` nunca deve conseguir gerar/deixar um arquivo de cliente com XML quebrado..." — replace the retired agent name with `client-scaffold-fill` (the subagent that actually writes/customizes client file content today; `client-scaffold-structure` only copies verbatim templates before variable substitution). Rest of the sentence unchanged.

2. Line 73 (Naming Patterns example list): "...`.claude/agents/docs-reviewer.md`, `.claude/agents/client-project-scaffolder.md`, `.claude/agents/recepcionista-scaffolder.md`." — replace the dead path with `.claude/agents/client-scaffold-fill.md`, keeping the rest of the comma-separated list (`docs-editor-conciso.md`, `docs-analyzer.md`, `docs-reviewer.md`, `recepcionista-scaffolder.md`) unchanged.

3. Line 145 (Component Responsibilities table): the single row describing the retired subagent (`| \`client-project-scaffolder\` subagent | Generates a full single-agent client stack from \`modelo/\` | \`.claude/agents/client-project-scaffolder.md\` |`) describes a component that no longer exists as ONE subagent. Replace it with 3 rows, one per current subagent, same table format (`| Component | Responsibility | File |`):
   - `client-scaffold-structure` subagent | Creates the client folder skeleton and copies `modelo/*.md` templates verbatim, asking no questions | `.claude/agents/client-scaffold-structure.md`
   - `client-scaffold-collect` subagent | Reads the copied templates, collects every required field (including media) conversationally, returns a structured `<dados_coletados>` block; read-only, never writes files | `.claude/agents/client-scaffold-collect.md`
   - `client-scaffold-fill` subagent | Fills the copied templates' placeholders with the collected data, preserving `{{variavel}}` syntax and the pending marker for anything unanswered; non-interactive | `.claude/agents/client-scaffold-fill.md`

4. Line 150 (post-scaffolder-review.sh row): "`SubagentStop` hook that reacts after `client-project-scaffolder` runs; has an anti-loop sentinel guard" — replace the retired agent name with `client-scaffold-fill` (the hook's own `case` statement already matches on `client-scaffold-fill` — only this doc prose was stale). Rest of the cell unchanged.

5. Line 175 (Layers section, "Used by"): "Used by: `client-project-scaffolder`, `recepcionista-scaffolder` subagents" — replace the retired agent name with `client-scaffold-structure` (the subagent that actually copies `modelo/*.md` verbatim today). Rest of the line unchanged.

Do not regenerate or restructure any other part of this file (it is GSD-managed codebase documentation, but this task is a factual-correction task, not a `/gsd-docs-update` regeneration pass — touch only the 5 identified lines/table cells).
  </action>
  <verify>
    <automated>[ "$(grep -c 'client-project-scaffolder' .claude/CLAUDE.md)" = "0" ] && grep -q 'client-scaffold-fill' .claude/CLAUDE.md && grep -q 'client-scaffold-structure' .claude/CLAUDE.md && grep -q 'client-scaffold-collect' .claude/CLAUDE.md && [ "$(grep -c 'client-scaffold-structure.md\|client-scaffold-collect.md\|client-scaffold-fill.md' .claude/CLAUDE.md)" -ge 3 ] && echo PASS</automated>
  </verify>
  <done>.claude/CLAUDE.md has zero remaining mentions of the retired subagent name; Core Value, Naming Patterns, the post-scaffolder-review.sh row, and the Layers "Used by" line all cite a current subagent name; the Component Responsibilities table has 3 rows (one per current scaffolding subagent) instead of 1 row for the retired one.</done>
  <acceptance_criteria>
    - `grep -c 'client-project-scaffolder' .claude/CLAUDE.md` returns 0
    - Line 9 (Core Value) cites `client-scaffold-fill`
    - Line 73 (Naming Patterns) lists `.claude/agents/client-scaffold-fill.md` instead of the dead path
    - The Component Responsibilities table has 3 rows citing `client-scaffold-structure`, `client-scaffold-collect`, and `client-scaffold-fill`, each with its own file path
    - The post-scaffolder-review.sh row cites `client-scaffold-fill`
    - The Layers "Used by" line cites `client-scaffold-structure`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fix COMANDOS.md — 2 renames + Fluxo section rewrite (CONTEXT.md Decision 5)</name>
  <files>COMANDOS.md</files>
  <read_first>
    - `COMANDOS.md` (full file — lines 24-33, 74, 102 are the 3 edit targets)
    - `.claude/commands/ei-cria-cliente.md` Passo 3 (mode question), Passo 4A (single-agent), Passo 4B.1 (multi-agent full flow + loop), Passo 4B.2 (bypass), and the "Gate de Confirmação (Passo 2→3)" subsection — authoritative source for the Fluxo rewrite; every replacement description must match this file, not just rename tokens
    - `.claude/agents/client-scaffold-structure.md`, `.claude/agents/client-scaffold-collect.md`, `.claude/agents/client-scaffold-fill.md` frontmatter `description` fields — source for the "Agentes envolvidos" table row split
  </read_first>
  <action>
Apply exactly these 3 edits:

1. Line 74 (Hooks automáticos table, SubagentStop row): "`post-scaffolder-review.sh` revisa scaffolding do `client-project-scaffolder` automaticamente; guarda silenciosa (D-11) para coexistência com o pipeline `/ei-ajustes`" — replace the retired agent name with `client-scaffold-fill`, keeping the rest of the cell ("automaticamente; guarda silenciosa (D-11) para coexistência com o pipeline `/ei-ajustes`") unchanged.

2. Line 102 (Agentes envolvidos table, 2-column `| Agente | Papel |` format): the row `| \`client-project-scaffolder\` | Cria novos projetos de cliente (Fase 0 carrega contexto) |` describes a retired single subagent. Replace it with 3 rows, same 2-column format as the other rows in that table (`docs-analyzer`/`docs-editor-conciso`/`docs-reviewer` below it):
   - `client-scaffold-structure` | Passo 1 — cria a pasta do cliente e copia os templates de `modelo/` verbatim, sem perguntar nada
   - `client-scaffold-collect` | Passo 2 — lê os templates copiados e coleta todo campo obrigatório (incl. mídia) via conversa; read-only
   - `client-scaffold-fill` | Passo 3 — preenche os templates com os dados coletados, preservando `{{variavel}}` e o marcador `[PENDENTE]`; não-interativo

3. Lines 24-33 (the entire `/ei-cria-cliente` "Fluxo" list) — full rewrite, not a rename. The current list describes the fully-retired single-subagent, no-gate, 7-phase flow (dispara `client-project-scaffolder`, Fase 0 a Fase 5). Replace the whole `**Fluxo:**` numbered list plus the `**Saída:**` line with content covering, at minimum: (a) the mode question (single-agent vs. multi-agente) is asked first via `AskUserQuestion`; (b) the single-agent path: `client-scaffold-structure` (creates folder + copies templates) -> `client-scaffold-collect` (collects data conversationally) -> **Gate de Confirmação humano** (`AskUserQuestion`: "Aprovar e preencher" / "Cancelar" — this human gate is the single most important behavior change from the old flow) -> `client-scaffold-fill` (fills the templates); (c) the multi-agent path: the same structure->collect->gate->fill cycle repeats once per specialty (sequential, never parallel), then `recepcionista-scaffolder` runs once at the end to build `Recepcionista/`; (d) the multi-agent bypass: skips the specialty loop entirely, only runs `recepcionista-scaffolder`; (e) output: one consolidated final summary (not per-specialty) with per-specialty status (**preenchida** / **cancelada-e-não-preenchida**). Suggested concrete replacement (exact wording is discretionary, this meets all 5 points above):

```
**Fluxo:**
1. Pergunta o modo via `AskUserQuestion`: **single-agent** (1 frente) ou **multi-agente** (2+ frentes com fluxos distintos).
2. **Single-agent:**
   1. `client-scaffold-structure` cria a pasta `<cliente>/` e copia `modelo/*.md` verbatim.
   2. `client-scaffold-collect` coleta todos os campos obrigatórios (incl. mídia) via conversa e devolve `<dados_coletados>`.
   3. **Gate de Confirmação** (`AskUserQuestion`: "Aprovar e preencher" / "Cancelar") — só avança para o preenchimento com aprovação explícita.
   4. `client-scaffold-fill` preenche os templates com os dados aprovados.
3. **Multi-agente (fluxo completo):** repete o ciclo estrutura → coleta → gate → preenchimento (item 2 acima) uma vez por especialidade, sempre sequencial, nunca em paralelo. Depois que todas as especialidades foram criadas (preenchidas ou canceladas), dispara `recepcionista-scaffolder` uma única vez para montar `Recepcionista/`.
4. **Multi-agente (bypass):** pula o loop de especialidades inteiro — dispara só o `recepcionista-scaffolder`, que pede nome/descrição/gatilhos de cada especialidade já existente.
5. **Resumo final:** um único resumo consolidado ao fim de todo o fluxo (não por especialidade), com o status de cada ciclo (**preenchida** ou **cancelada-e-não-preenchida**) e os campos pendentes.

**Saída:** nova pasta `<cliente>/` (single-agent) ou `<cliente>/<especialidade>/` + `<cliente>/Recepcionista/` (multi-agente) com os agentes preenchidos (ou marcados como pendentes/cancelados).
```
  </action>
  <verify>
    <automated>[ "$(grep -c 'client-project-scaffolder' COMANDOS.md)" = "0" ] && grep -q 'client-scaffold-structure' COMANDOS.md && grep -q 'client-scaffold-collect' COMANDOS.md && grep -q 'client-scaffold-fill' COMANDOS.md && grep -q 'Gate de Confirmação' COMANDOS.md && grep -q 'cancelada-e-não-preenchida' COMANDOS.md && echo PASS</automated>
  </verify>
  <done>COMANDOS.md has zero remaining mentions of the retired subagent name; the Hooks automáticos and Agentes envolvidos tables cite current subagents; the Fluxo section describes the gated 3-step flow for both single-agent and multi-agent modes (including bypass and consolidated summary) instead of the retired 7-phase flow.</done>
  <acceptance_criteria>
    - `grep -c 'client-project-scaffolder' COMANDOS.md` returns 0
    - Line 74 (Hooks automáticos) cites `client-scaffold-fill`
    - The Agentes envolvidos table has 3 rows for `client-scaffold-structure`, `client-scaffold-collect`, `client-scaffold-fill`
    - The Fluxo section mentions the mode question, the 3-step chain, the "Gate de Confirmação" with its two exact option labels, the multi-agent loop + bypass paths, and the consolidated per-specialty summary (`cancelada-e-não-preenchida` present)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan touches no runtime trust boundary. `.claude/CLAUDE.md` autoloads as this repo's "Project instructions" (read by the main Claude agent at session start, not by any external input path). `COMANDOS.md` is explicitly repo-internal, non-published documentation (its own header states it is not distributed via `ei-prompt install`, confirmed by absence from `manifest.json`). Both edits are prose-only.

| Boundary | Description |
|----------|--------------|
| None (new) | No new trust boundary is introduced; both files are static documentation with no code execution path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------------|------------------|
| T-03.1-04 | Tampering (doc-guided misbehavior) | `.claude/CLAUDE.md` Component Responsibilities table + Core Value line | medium | mitigate | Task 1 corrects the table to describe the actual 3-subagent chain, preventing a future agent session (or human) from believing a single `client-project-scaffolder` component still exists or is the file-writer of client XML content |
| T-03.1-05 | Tampering (doc-guided misbehavior) | `COMANDOS.md` `/ei-cria-cliente` Fluxo section | medium | mitigate | Task 3's rewrite replaces a description of a flow with no confirmation gate with one that documents the Gate de Confirmação as the single most important behavior change — reduces risk of a human following stale internal docs and expecting client files to be filled without ever being asked to approve |
| T-03.1-06 | Information Disclosure | `manifest.json` `deprecated_files` entry, `CHANGELOG.md` historical mentions of the retired subagent | low | accept | Explicitly out of scope per CONTEXT.md: `manifest.json`'s entry is the correct, unrelated mechanism removing the dead file from client installs; `CHANGELOG.md` is a historical record and editing it would misrepresent project history |

</threat_model>

<verification>
- `grep -c 'client-project-scaffolder' .claude/CLAUDE.md` returns 0
- `grep -c 'client-project-scaffolder' COMANDOS.md` returns 0
- `.claude/CLAUDE.md`'s Component Responsibilities table has 3 rows for the current scaffolding subagents
- `COMANDOS.md`'s Fluxo section documents the Gate de Confirmação and the consolidated per-specialty summary
</verification>

<success_criteria>
- `.claude/CLAUDE.md` accurately describes the current 3-subagent scaffolding chain in every location that previously named the retired subagent
- `COMANDOS.md` accurately documents the gated 3-step `/ei-cria-cliente` flow (single-agent, multi-agent full flow, and bypass) instead of the retired 7-phase flow
</success_criteria>

<artifacts_produced>
## Artifacts This Plan Produces

None — this plan modifies existing files only, creates no new files/functions/CLI flags/symbols.

**Modified:**
- `.claude/CLAUDE.md` — 5 stale-reference corrections (4 renames, 1 table-row split)
- `COMANDOS.md` — 2 mechanical renames + 1 full section rewrite (Fluxo)
</artifacts_produced>

<output>
Create `.planning/phases/03.1-corrigir-refer-ncias-obsoletas-a-client-project-scaffolder-e/03.1-02-SUMMARY.md` when done
</output>
