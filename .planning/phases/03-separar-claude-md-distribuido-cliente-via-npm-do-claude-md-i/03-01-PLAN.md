---
phase: 03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/CLAUDE.md
  - manifest.json
autonomous: true
requirements: [CLMD-01, CLMD-02]

must_haves:
  truths:
    - "client/CLAUDE.md exists and contains every client-facing section from the pre-migration root CLAUDE.md (index banner + v1.8.9 note, Mapa de Regras, Arquitetura Padrão de Agentes, Arquitetura Multi-Agente, Slash Commands table, Regras Básicas) verbatim, except the Commits section (D-05/D-06)"
    - "manifest.json's files[] entry for CLAUDE.md is the object {from: 'client/CLAUDE.md', to: 'CLAUDE.md'}; every other files[] entry and every deprecated_files[] entry remains an unchanged plain string (D-03/D-04)"
  artifacts:
    - client/CLAUDE.md
  key_links:
    - "manifest.json's new {from,to} entry is consumed by bin/cli.js's normalizeEntry()/fetchFile()/writeFile() — wired in Plan 03-02 (this plan only lands the data shape, not the consuming code)"
---

<objective>
Create the new isolated `client/` folder with `client/CLAUDE.md` as the real, physically-separate source of the npm-distributed client payload (D-01/D-02), and point `manifest.json`'s CLAUDE.md entry at it via the new `{from, to}` object shape (D-03/D-04).

Purpose: This is step 1+3 of the phase's additive-first sequence (see 03-RESEARCH.md `<critical_sequencing_note>`/Architecture Patterns). Root `CLAUDE.md` is NOT touched or emptied in this plan — the repo must never pass through a state where no file holds a valid copy of the client payload. `client/CLAUDE.md` becomes the new source of truth; root `CLAUDE.md` keeps its current content, untouched, until Plan 03-04 (Wave 2) removes it after the consuming code (Plan 03-02) and the ~9 fallback reads (Plan 03-03) have landed.
Output: `client/CLAUDE.md` (new file, isolated folder per D-02) + `manifest.json`'s `files[]` CLAUDE.md entry converted to `{from, to}`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-separar-claude-md-distribuido-cliente-via-npm-do-claude-md-i/03-CONTEXT.md
@.planning/phases/03-separar-claude-md-distribuido-cliente-via-npm-do-claude-md-i/03-RESEARCH.md
@CLAUDE.md
@manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create client/CLAUDE.md verbatim minus the Commits section</name>
  <files>client/CLAUDE.md</files>
  <read_first>CLAUDE.md (root, current version — 83 lines, already loaded above in context)</read_first>
  <action>
Create the new folder `client/` (D-02: this folder must NEVER contain any other file, now or later — it exists solely to hold this one isolated source file, so that Claude Code's subpath CLAUDE.md autoload-on-file-read behavior never triggers on unrelated reads inside it, unlike `docs/` which is read constantly by other subagents).

Write `client/CLAUDE.md` with the EXACT same content as the current root `CLAUDE.md`, byte-for-byte, EXCEPT remove exactly this one section: the `## Commits` heading line, its single bullet line ("Não incluir assinatura..."), and the blank line that follows it, so the file flows directly from the `> ⚠️ **v1.8.9:** ...` banner block into `## Arquitetura Padrão de Agentes` (D-06 — the Commits rule is about how THIS repo's maintainer commits changes, not about client bot behavior, so it stays out of the client payload entirely).

Do not paraphrase, reformat, reorder, retitle, or "clean up" any other heading, table, list, or link. Do not alter the v1.8.9 warning banner (D-05 — it is legitimately client-facing: `inject-ei-context.sh` is itself distributed via `manifest.json`, so the "load manually" instruction applies to the client's own session too). The result must be a strict verbatim copy of root `CLAUDE.md` minus only the 3 lines identified above.
  </action>
  <verify>
    <automated>[ -f client/CLAUDE.md ] && [ "$(grep -c '^## Commits$' client/CLAUDE.md)" = "0" ] && [ "$(grep -c '^## Mapa de Regras$' client/CLAUDE.md)" = "1" ] && [ "$(grep -c '^## Arquitetura Padrão de Agentes$' client/CLAUDE.md)" = "1" ] && [ "$(grep -c '^## Arquitetura Multi-Agente' client/CLAUDE.md)" = "1" ] && [ "$(grep -c '^## Slash Commands$' client/CLAUDE.md)" = "1" ] && [ "$(grep -c '^## Regras Básicas$' client/CLAUDE.md)" = "1" ] && [ "$(grep -c 'v1.8.9' client/CLAUDE.md)" = "1" ] && echo PASS</automated>
  </verify>
  <done>client/CLAUDE.md exists as the sole file in a new isolated client/ folder, containing every current client-facing section of root CLAUDE.md byte-for-byte except the Commits section.</done>
  <acceptance_criteria>
    - `client/` folder exists and contains exactly one file (`CLAUDE.md`)
    - `client/CLAUDE.md` has zero occurrences of the `## Commits` heading
    - `client/CLAUDE.md` has exactly one occurrence each of the 5 migrated headings + the v1.8.9 banner note
    - No other line, table, or link in `client/CLAUDE.md` differs from the current root `CLAUDE.md`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Point manifest.json's CLAUDE.md entry at {from, to}</name>
  <files>manifest.json</files>
  <read_first>manifest.json</read_first>
  <action>
Edit `manifest.json`'s `files` array: replace the plain string `"CLAUDE.md"` entry with the object `{ "from": "client/CLAUDE.md", "to": "CLAUDE.md" }` (D-03). This is the ONLY entry in `files[]` that changes shape — leave every other one of the ~28 remaining `files[]` strings and all 4 `deprecated_files[]` strings exactly as plain strings, untouched (D-04 — `deprecated_files` never needs the `{from,to}` treatment since it only ever needs one path for local removal). Preserve the existing JSON formatting style (2-space indent, trailing commas per the project's existing style) — do not reformat the rest of the file.
  </action>
  <verify>
    <automated>node -e "const m=require('./manifest.json'); const obj=m.files.find(f=>typeof f==='object'); if(!obj||obj.from!=='client/CLAUDE.md'||obj.to!=='CLAUDE.md') throw new Error('CLAUDE.md entry not converted correctly: '+JSON.stringify(obj)); const strs=m.files.filter(f=>typeof f==='string'); if(strs.length!==28) throw new Error('expected 28 remaining string entries in files[], got '+strs.length); if(!Array.isArray(m.deprecated_files)||m.deprecated_files.length!==4||!m.deprecated_files.every(f=>typeof f==='string')) throw new Error('deprecated_files schema changed'); console.log('PASS')"</automated>
  </verify>
  <done>manifest.json's files[] array has exactly one object entry ({from:"client/CLAUDE.md", to:"CLAUDE.md"}) and 28 unchanged string entries; deprecated_files[] is untouched (4 plain strings).</done>
  <acceptance_criteria>
    - `require("./manifest.json").files` contains exactly one non-string element, and it is `{from: "client/CLAUDE.md", to: "CLAUDE.md"}`
    - `deprecated_files` array is byte-identical to its pre-task state (4 entries, all strings)
    - `node -e "require('./manifest.json')"` does not throw (valid JSON)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest.json (git-controlled, maintainer-only) → bin/cli.js | `manifest.json`'s `files[]` entries dictate what `bin/cli.js` fetches/writes; trusted input today (only the maintainer commits it), but this plan changes its schema for the first time |
| client/CLAUDE.md content → every installed client project | Whatever lands in `client/CLAUDE.md` is distributed verbatim to every `npx ei-prompt` user — an accidental leak of internal-only content here reaches every client |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | manifest.json files[] entry shape | medium | mitigate | Task 2's automated verify asserts the exact `{from,to}` shape and that no other entry's type changed, before this plan is considered done — malformed shapes are caught here, before Plan 03-02's `normalizeEntry()` ever has to defend against them at runtime |
| T-03-02 | Information Disclosure | client/CLAUDE.md content | medium | mitigate | Task 1's automated verify greps for zero `## Commits` occurrences — the one section that must NEVER reach client/CLAUDE.md (D-06), since it is a maintainer-only git-hygiene rule, not client bot behavior |
</threat_model>

<verification>
- `client/CLAUDE.md` exists, is the only file under `client/`, and matches root `CLAUDE.md` minus the Commits section (Task 1 automated verify)
- `manifest.json` is valid JSON with exactly one `{from,to}` entry and all other entries unchanged (Task 2 automated verify)
- `node bin/cli.js --help` still runs without throwing (bin/cli.js is not yet updated to consume `{from,to}` — that lands in Plan 03-02; this is a smoke check that the manifest.json change alone doesn't crash the CLI's *parsing* of the file, since `require("../manifest.json")` happens at module load)
</verification>

<success_criteria>
- `client/` folder exists containing only `client/CLAUDE.md`, verbatim copy of root CLAUDE.md minus Commits section
- `manifest.json`'s CLAUDE.md entry is `{from: "client/CLAUDE.md", to: "CLAUDE.md"}`; nothing else in manifest.json changed shape
</success_criteria>

<artifacts_produced>
## Artifacts This Plan Produces

- **New file:** `client/CLAUDE.md` — the real source of the npm-distributed client payload (D-02), verbatim copy of pre-migration root CLAUDE.md minus the Commits section
- **New folder:** `client/` — isolated, contains only the file above, never gains a second file (D-02 hard constraint)
- **Config change:** `manifest.json`'s `files[]` CLAUDE.md entry becomes `{from: "client/CLAUDE.md", to: "CLAUDE.md"}` — the only object-shaped entry in an otherwise all-string array
</artifacts_produced>

<output>
Create `.planning/phases/03-separar-claude-md-distribuido-cliente-via-npm-do-claude-md-i/03-01-SUMMARY.md` when done
</output>
