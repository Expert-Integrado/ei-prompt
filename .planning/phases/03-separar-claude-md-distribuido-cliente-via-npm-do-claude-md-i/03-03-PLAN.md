---
phase: 03
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/client-scaffold-structure.md
  - .claude/agents/client-scaffold-collect.md
  - .claude/agents/client-scaffold-fill.md
  - .claude/agents/docs-analyzer.md
  - .claude/agents/docs-editor-conciso.md
  - .claude/agents/docs-reviewer.md
  - .claude/agents/recepcionista-scaffolder.md
  - .claude/commands/ei-cria-cliente.md
  - .claude/commands/ei-ajustes.md
autonomous: true
requirements: [CLMD-06]

must_haves:
  truths:
    - "Every distributed subagent/command whose Fase 0 / Passo 0 (or an equivalent mid-flow file-read reference) previously said 'Read CLAUDE.md' now checks (via Glob) whether client/CLAUDE.md exists first, reads it when present, and falls back to CLAUDE.md when absent — so the same instruction text works correctly both inside this source repo (client/CLAUDE.md present) and inside an already-installed client project (client/CLAUDE.md absent, since client/ is never distributed)"
  artifacts: []
  key_links:
    - "Fase 0/Passo 0 Glob check → Read client/CLAUDE.md OR CLAUDE.md, in each of the 9 files listed in files_modified"
---

<objective>
Apply the dual-context fallback-read pattern (RESEARCH.md Pattern 3) to every distributed subagent/command whose Fase/Passo 0 (or an equivalent mid-flow reference) currently reads `CLAUDE.md` assuming it holds the client payload. This is RESEARCH.md's "achado crítico" Pitfall 2 — 9 files (one more than RESEARCH.md's own table found: `client-scaffold-fill.md:19` was missed by research and is included here per the scope-reduction-prohibition gap check) across 11 edit points.

Purpose: This is step 4 of the phase's additive-first sequence, and per RESEARCH.md's Architecture Patterns section it MUST land before Plan 03-04 removes root `CLAUDE.md` (Wave 2) — otherwise every one of these files silently breaks the maintainer's own live scaffolding tests (confirmed pattern of use in `02-05-SUMMARY.md`: "throwaway test client" created directly inside this repo). This plan does not touch `bin/cli.js`, `manifest.json`, or `client/CLAUDE.md`, so it runs safely in parallel with Plans 03-01 and 03-02 (no file overlap, no functional dependency — the Glob-check-then-read text works correctly whether or not `client/CLAUDE.md` physically exists yet).
Output: 9 edited markdown files (7 subagents, 2 commands), each now checking `client/CLAUDE.md` via Glob before falling back to `CLAUDE.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-separar-claude-md-distribuido-cliente-via-npm-do-claude-md-i/03-CONTEXT.md
@.planning/phases/03-separar-claude-md-distribuido-cliente-via-npm-do-claude-md-i/03-RESEARCH.md
@.planning/phases/03-separar-claude-md-distribuido-cliente-via-npm-do-claude-md-i/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply fallback-read pattern to the 4 client-scaffold-*/docs-analyzer subagents</name>
  <files>.claude/agents/client-scaffold-structure.md, .claude/agents/client-scaffold-collect.md, .claude/agents/client-scaffold-fill.md, .claude/agents/docs-analyzer.md</files>
  <read_first>.claude/agents/client-scaffold-structure.md lines 1-22; .claude/agents/client-scaffold-collect.md lines 1-24; .claude/agents/client-scaffold-fill.md lines 1-22; .claude/agents/docs-analyzer.md lines 1-24</read_first>
  <action>
Apply these 4 exact edits (each file's frontmatter `tools:` line must include `Glob` — check before editing, add it if missing, since the new instruction text below requires a Glob call):

1. `.claude/agents/client-scaffold-structure.md` (`tools:` already has `Glob` — no frontmatter change needed) — replace the Fase 0 numbered item "1. Leia `CLAUDE.md` integralmente via Read tool para internalizar as regras do projeto (arquitetura dos agentes, naming patterns, arquitetura multi-agente)." with: "1. Verifique se `client/CLAUDE.md` existe (Glob). Se existir (repo-fonte do ei-prompt), leia-o via Read — é a fonte real das regras do projeto (arquitetura dos agentes, naming patterns, arquitetura multi-agente). Se NÃO existir (projeto de cliente já instalado via npx), leia `CLAUDE.md` normalmente — ali está o conteúdo correto."

2. `.claude/agents/client-scaffold-collect.md` (`tools:` already has `Glob`) — inside "## Passo 0 — Carregar Regras (OBRIGATÓRIO)", replace the bullet "- `CLAUDE.md`" (first item under "Antes de qualquer outra ação, leia via `Read`:") with: "- `client/CLAUDE.md` se existir (Glob) — senão `CLAUDE.md` (fallback dual-contexto: repo-fonte do ei-prompt vs. projeto de cliente instalado)"

3. `.claude/agents/client-scaffold-fill.md` — its `tools:` frontmatter line currently reads `tools: Read, Edit, Write` and does NOT include `Glob`; change it to `tools: Read, Glob, Edit, Write`. Then, inside "## Passo 0 — Carregar Contexto", replace the bullet "- `CLAUDE.md`" (first item under "Antes de qualquer ação, leia via `Read`:") with the same fallback bullet text as item 2 above.

4. `.claude/agents/docs-analyzer.md` (`tools:` already has `Glob`) — inside "## ⚠️ PASSO 0 — CARREGAR REGRAS (OBRIGATÓRIO)", replace the bullet "- `CLAUDE.md`" (first item under "Antes de qualquer outra ação, leia via `Read`:") with the same fallback bullet text as item 2 above.

Do not alter any other line in any of the 4 files (no content revision — this is a structural pointer fix only, per CONTEXT.md's phase boundary).
  </action>
  <verify>
    <automated>for f in .claude/agents/client-scaffold-structure.md .claude/agents/client-scaffold-collect.md .claude/agents/client-scaffold-fill.md .claude/agents/docs-analyzer.md; do grep -q "client/CLAUDE.md" "$f" || { echo "MISSING fallback in $f"; exit 1; }; done; grep -q "^tools:.*Glob" .claude/agents/client-scaffold-fill.md || { echo "client-scaffold-fill.md missing Glob tool"; exit 1; }; echo PASS</automated>
  </verify>
  <done>All 4 files' Fase/Passo 0 file-read step checks client/CLAUDE.md via Glob first, falling back to CLAUDE.md; client-scaffold-fill.md's frontmatter tools list now includes Glob.</done>
  <acceptance_criteria>
    - Each of the 4 files contains the string `client/CLAUDE.md` in its Fase/Passo 0 section
    - `client-scaffold-fill.md`'s `tools:` frontmatter line includes `Glob`
    - No other content in any of the 4 files changed
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Apply fallback-read pattern to docs-editor-conciso.md, docs-reviewer.md, recepcionista-scaffolder.md</name>
  <files>.claude/agents/docs-editor-conciso.md, .claude/agents/docs-reviewer.md, .claude/agents/recepcionista-scaffolder.md</files>
  <read_first>.claude/agents/docs-editor-conciso.md lines 1-20 and lines 85-99; .claude/agents/docs-reviewer.md lines 1-30; .claude/agents/recepcionista-scaffolder.md lines 1-42</read_first>
  <action>
Apply these edits (all 3 files already have `Glob` in their frontmatter `tools:` — no frontmatter changes needed):

1. `.claude/agents/docs-editor-conciso.md`, TWO edit points in this one file:
   (a) Inside "## ⚠️ PASSO 0 — CARREGAR REGRAS DE EDIÇÃO (OBRIGATÓRIO)", replace the bullet "- `CLAUDE.md`" (first item under "**Antes de qualquer outra ação**, leia via `Read`:") with: "- `client/CLAUDE.md` se existir (Glob) — senão `CLAUDE.md` (fallback dual-contexto: repo-fonte do ei-prompt vs. projeto de cliente instalado)"
   (b) In the "## VERIFICAÇÃO DE ESCOPO" section, replace "**ANTES de qualquer edição:** Consultar a seção "Limites do Ajuste de Prompts" no CLAUDE.md para verificar se o pedido está dentro do escopo." with: "**ANTES de qualquer edição:** Consultar a seção "Limites do Ajuste de Prompts" no arquivo carregado no PASSO 0 (`client/CLAUDE.md` ou `CLAUDE.md`, conforme o Glob determinou) para verificar se o pedido está dentro do escopo." — keep the rest of that sentence ("Se estiver fora, informar ao usuário onde resolver e NÃO prosseguir com edição.") unchanged.

2. `.claude/agents/docs-reviewer.md` — inside "## Passo 0 — OBRIGATÓRIO antes de qualquer auditoria", replace the indented bullet "   - `CLAUDE.md`" (first item under "1. **Ler via `Read`**...") with: "   - `client/CLAUDE.md` se existir (Glob) — senão `CLAUDE.md` (fallback dual-contexto)"

3. `.claude/agents/recepcionista-scaffolder.md` — inside "### Fase 0: Carregar Contexto", replace numbered item "1. Leia `CLAUDE.md` (raiz do projeto) — internalize a seção "Arquitetura Multi-Agente (opcional — Recepcionista)"." with: "1. Verifique se `client/CLAUDE.md` existe (Glob). Se existir, leia-o via Read — internalize a seção "Arquitetura Multi-Agente (opcional — Recepcionista)". Se NÃO existir, leia `CLAUDE.md` (raiz do projeto) normalmente."

Do not alter any other line in any of the 3 files.
  </action>
  <verify>
    <automated>for f in .claude/agents/docs-editor-conciso.md .claude/agents/docs-reviewer.md .claude/agents/recepcionista-scaffolder.md; do grep -q "client/CLAUDE.md" "$f" || { echo "MISSING fallback in $f"; exit 1; }; done; [ "$(grep -c 'client/CLAUDE.md' .claude/agents/docs-editor-conciso.md)" -ge 2 ] || { echo "docs-editor-conciso.md missing one of its 2 edit points"; exit 1; }; echo PASS</automated>
  </verify>
  <done>All 3 files' file-read reference(s) to CLAUDE.md now check client/CLAUDE.md via Glob first; docs-editor-conciso.md has both its Passo 0 bullet AND its mid-flow "Limites do Ajuste de Prompts" reference updated.</done>
  <acceptance_criteria>
    - `docs-editor-conciso.md` contains at least 2 occurrences of `client/CLAUDE.md`
    - `docs-reviewer.md` and `recepcionista-scaffolder.md` each contain at least 1 occurrence of `client/CLAUDE.md`
    - No other content in any of the 3 files changed
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Apply fallback-read pattern to ei-cria-cliente.md and ei-ajustes.md commands</name>
  <files>.claude/commands/ei-cria-cliente.md, .claude/commands/ei-ajustes.md</files>
  <read_first>.claude/commands/ei-cria-cliente.md lines 1-22; .claude/commands/ei-ajustes.md lines 258-270 and lines 550-560</read_first>
  <action>
Slash commands run in the main agent's context (no `tools:` frontmatter restriction to check — Glob is always available), so only the instruction text changes:

1. `.claude/commands/ei-cria-cliente.md` — in "### Passo 1 — Carregar contexto", replace numbered item "1. Leia `CLAUDE.md` integralmente — atenção à seção "Arquitetura Multi-Agente (opcional — Recepcionista)"." with: "1. Verifique se `client/CLAUDE.md` existe (Glob). Se existir, leia-o via Read — atenção à seção "Arquitetura Multi-Agente (opcional — Recepcionista)". Se NÃO existir, leia `CLAUDE.md` integralmente."

2. `.claude/commands/ei-ajustes.md`, TWO edit points in this one file:
   (a) In "### Passo 4: Carregar contexto", replace the bullet "- `CLAUDE.md`" (first item under "Leia via Read (se ainda não leu nesta sessão):") with: "- `client/CLAUDE.md` se existir (Glob) — senão `CLAUDE.md` (fallback dual-contexto)"
   (b) In the reviewer fan-out prompt template (the "REGRAS A AUDITAR" checklist), replace the bullet "- CLAUDE.md + docs/regras-validacao.md + docs/proibido-fazer.md (re-ler se ainda não leu nesta sessão)" with: "- `client/CLAUDE.md` (ou `CLAUDE.md`, conforme o Passo 4 determinou) + docs/regras-validacao.md + docs/proibido-fazer.md (re-ler se ainda não leu nesta sessão)"

Do not alter any other line in either file.
  </action>
  <verify>
    <automated>grep -q "client/CLAUDE.md" .claude/commands/ei-cria-cliente.md || { echo "MISSING fallback in ei-cria-cliente.md"; exit 1; }; [ "$(grep -c 'client/CLAUDE.md' .claude/commands/ei-ajustes.md)" -ge 2 ] || { echo "ei-ajustes.md missing one of its 2 edit points"; exit 1; }; echo PASS</automated>
  </verify>
  <done>Both command files' file-read references to CLAUDE.md now check client/CLAUDE.md via Glob first; ei-ajustes.md has both its Passo 4 bullet AND its reviewer fan-out checklist bullet updated.</done>
  <acceptance_criteria>
    - `ei-cria-cliente.md` contains at least 1 occurrence of `client/CLAUDE.md`
    - `ei-ajustes.md` contains at least 2 occurrences of `client/CLAUDE.md`
    - No other content in either file changed
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fase/Passo 0 instruction text → subagent's runtime behavior | The instruction text edited here directly controls which file each subagent treats as ground truth for agent-architecture rules; a mistake here silently changes agent behavior, not a crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Tampering (workflow integrity) | 9 distributed subagents/commands reading CLAUDE.md | high | mitigate | Every one of the 11 edit points is applied and automated-verified (grep for `client/CLAUDE.md`) BEFORE Plan 03-04 (Wave 2, `depends_on` includes this plan) is allowed to remove root `CLAUDE.md` — this ordering is the core mitigation for RESEARCH.md's Pitfall 2 (maintainer's own live scaffolding tests silently breaking) |
| T-03-07 | Information Disclosure | client-scaffold-fill.md missing Glob tool (found during this plan, not in RESEARCH.md's original table) | low | mitigate | Task 1 explicitly adds `Glob` to `client-scaffold-fill.md`'s frontmatter `tools:` list and verifies it via grep — without this the file's new fallback-check instruction would be unusable (the subagent would have no tool to perform the existence check with) |
</threat_model>

<verification>
- All 9 files contain `client/CLAUDE.md` in their fallback-check instruction text (per-task automated greps)
- `client-scaffold-fill.md`'s `tools:` frontmatter includes `Glob`
- No content outside the identified 11 edit points changed in any of the 9 files (spot-check via `git diff` line count matching expected edits)
</verification>

<success_criteria>
- 9 files edited: 7 subagents (`.claude/agents/client-scaffold-structure.md`, `client-scaffold-collect.md`, `client-scaffold-fill.md`, `docs-analyzer.md`, `docs-editor-conciso.md`, `docs-reviewer.md`, `recepcionista-scaffolder.md`) + 2 commands (`.claude/commands/ei-cria-cliente.md`, `ei-ajustes.md`)
- 11 total edit points applied (docs-editor-conciso.md and ei-ajustes.md each have 2)
- `client-scaffold-fill.md` gained `Glob` in its tools list
</success_criteria>

<artifacts_produced>
## Artifacts This Plan Produces

- **Modified (content):** `.claude/agents/client-scaffold-structure.md`, `.claude/agents/client-scaffold-collect.md`, `.claude/agents/client-scaffold-fill.md`, `.claude/agents/docs-analyzer.md`, `.claude/agents/docs-editor-conciso.md`, `.claude/agents/docs-reviewer.md`, `.claude/agents/recepcionista-scaffolder.md`, `.claude/commands/ei-cria-cliente.md`, `.claude/commands/ei-ajustes.md`
- **Modified (frontmatter):** `.claude/agents/client-scaffold-fill.md` — `tools:` gains `Glob`
</artifacts_produced>

<output>
Create `.planning/phases/03-separar-claude-md-distribuido-cliente-via-npm-do-claude-md-i/03-03-SUMMARY.md` when done
</output>