---
phase: 03
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - CLAUDE.md
  - .claude/CLAUDE.md
autonomous: true
requirements: [CLMD-04, CLMD-05]

must_haves:
  truths:
    - "Root CLAUDE.md no longer exists in this repo (D-07 full-removal recommendation, per RESEARCH.md State of the Art citing official Claude Code docs confirming removal is a supported, common configuration)"
    - ".claude/CLAUDE.md is now the sole functional 'Project instructions' file for this repo, and contains none of the 5 migrated client-facing headings"
    - ".claude/CLAUDE.md's Agent-Editing Conventions commits-rule line no longer cites root CLAUDE.md as a source, since the rule no longer lives there (D-08)"
  artifacts: []
  key_links: []
---

<objective>
Remove root `CLAUDE.md` entirely (D-07) now that `client/CLAUDE.md` is the verified real source (Plan 03-01) and every distributed subagent/command that used to read root `CLAUDE.md` for client content has the fallback pattern in place (Plan 03-03). Fix the one line in `.claude/CLAUDE.md` that cites root `CLAUDE.md` as the source of the commits rule (D-08), since that rule left root `CLAUDE.md` for good in Plan 03-01.

Purpose: This is step 5+6 of the phase's additive-first sequence — the FIRST point in the whole phase where root `CLAUDE.md` is actually touched. It is gated (via `depends_on`) on all three Wave 1 plans because removing it earlier would strand the ~9 fallback-reading files with neither a valid `client/CLAUDE.md` reference in their own text (03-03) nor a functioning consumer of the new manifest schema (03-01/03-02) — this exact hazard is RESEARCH.md's Pitfall 2.
Output: root `CLAUDE.md` deleted; `.claude/CLAUDE.md`'s one stale cross-reference corrected.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-separar-claude-md-distribuido-cliente-via-npm-do-claude-md-i/03-CONTEXT.md
@.planning/phases/03-separar-claude-md-distribuido-cliente-via-npm-do-claude-md-i/03-RESEARCH.md
@CLAUDE.md
@.claude/CLAUDE.md
@client/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove root CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <read_first>client/CLAUDE.md (confirm it is present and complete — this is the safety precondition for this task); the 9 files edited in Plan 03-03 (spot-check at least 2 of them contain "client/CLAUDE.md")</read_first>
  <action>
Before deleting anything, confirm the two preconditions this task depends on are actually true in the current working tree (do not just trust the plan graph — verify directly, since this is the irreversible step of the phase): (1) `client/CLAUDE.md` exists and is non-empty; (2) at least the following files contain the string `client/CLAUDE.md`: `.claude/agents/docs-analyzer.md`, `.claude/agents/docs-editor-conciso.md`, `.claude/commands/ei-cria-cliente.md`. If either precondition fails, STOP and report which one — do not proceed with deletion.

Once confirmed, remove root `CLAUDE.md` from the repository entirely (`git rm CLAUDE.md` or equivalent — full removal per D-07's discretion call, following RESEARCH.md's State of the Art citation of the official Claude Code docs: removing root `CLAUDE.md` entirely, leaving `.claude/CLAUDE.md` as the sole "Project instructions" location, is an explicitly supported and common configuration, not a workaround). Do not replace it with a stub/pointer file — D-07's resolution is full removal, not a minimal pointer.
  </action>
  <verify>
    <automated>[ -f client/CLAUDE.md ] && [ -s client/CLAUDE.md ] && grep -q "client/CLAUDE.md" .claude/agents/docs-analyzer.md && grep -q "client/CLAUDE.md" .claude/agents/docs-editor-conciso.md && grep -q "client/CLAUDE.md" .claude/commands/ei-cria-cliente.md && [ ! -f CLAUDE.md ] && echo PASS</automated>
  </verify>
  <done>Root CLAUDE.md no longer exists; client/CLAUDE.md (verified present) is the sole source of the client payload going forward.</done>
  <acceptance_criteria>
    - `CLAUDE.md` does not exist at the repo root
    - `client/CLAUDE.md` exists and is non-empty
    - The three spot-checked fallback-reading files still contain their `client/CLAUDE.md` references (Plan 03-03 dependency held)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fix .claude/CLAUDE.md's commits-rule cross-reference and confirm no migrated headings leaked in</name>
  <files>.claude/CLAUDE.md</files>
  <read_first>.claude/CLAUDE.md line 121 (Agent-Editing Conventions section)</read_first>
  <action>
In `.claude/CLAUDE.md`'s "Agent-Editing Conventions" section, replace the line: "- **Commits never include "Generated with Claude Code" or "Co-Authored-By"** signatures (explicit project rule in `CLAUDE.md` and `docs/proibido-fazer.md`)." with: "- **Commits never include "Generated with Claude Code" or "Co-Authored-By"** signatures (explicit project rule — see `docs/proibido-fazer.md`)." — this is the ONLY line D-08 identifies as needing a fix (it is the sole place `.claude/CLAUDE.md` cites root `CLAUDE.md` as a rule SOURCE). Do not touch the other pre-existing mentions of "CLAUDE.md" elsewhere in this file's auto-generated `<!-- GSD:architecture-start -->...<!-- GSD:architecture-end -->` block (e.g. the "Rule docs" row in Component Responsibilities, or the "Layers" section's "root CLAUDE.md" mentions) — those are GSD-managed codebase-documentation prose describing what USED to exist, not a rule citation, and are out of scope per D-08's explicit "só ajustar essa linha existente" wording and the phase's domain boundary (CONTEXT.md: "não é uma revisão do que cada regra diz").
  </action>
  <verify>
    <automated>[ "$(grep -c 'explicit project rule in .CLAUDE\.md' .claude/CLAUDE.md)" = "0" ] && grep -q 'explicit project rule — see .docs/proibido-fazer\.md' .claude/CLAUDE.md && [ ! -f CLAUDE.md ] && ! grep -E '^## (Mapa de Regras|Arquitetura Padrão de Agentes|Arquitetura Multi-Agente|Slash Commands|Regras Básicas)' .claude/CLAUDE.md && echo PASS</automated>
  </verify>
  <done>.claude/CLAUDE.md's commits-rule line no longer cites root CLAUDE.md as a source; the full CLMD-04 regression check (no migrated heading in either CLAUDE.md or .claude/CLAUDE.md, and root CLAUDE.md is absent) passes.</done>
  <acceptance_criteria>
    - `.claude/CLAUDE.md`'s commits-rule line reads "explicit project rule — see `docs/proibido-fazer.md`" (no longer citing `CLAUDE.md`)
    - `.claude/CLAUDE.md` contains zero occurrences of any of the 5 migrated H2 headings
    - Root `CLAUDE.md` remains absent
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Irreversible file deletion (root CLAUDE.md) | Once removed and committed, restoring requires git history — this task is the phase's single point of no return, hence the explicit precondition re-verification in Task 1 rather than blind trust in `depends_on` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08 | Tampering (workflow integrity) | Root CLAUDE.md removal timing | high | mitigate | Task 1 re-verifies (not just trusts) that `client/CLAUDE.md` exists and that representative Plan 03-03 fallback edits are present in the CURRENT working tree, immediately before deleting — catches a misordered or partially-failed Wave 1 before the irreversible step, rather than relying solely on the plan graph's `depends_on` |
| T-03-09 | Information Disclosure | .claude/CLAUDE.md stale cross-references outside the D-08 line | low | accept | RESEARCH.md/D-08 explicitly scope the required fix to the single Agent-Editing Conventions commits line. The auto-generated Architecture-block mentions of "root CLAUDE.md" (Component Responsibilities table, Layers section) become descriptively stale but are not client-facing rule leakage (CLMD-04's grep gate only checks for the 5 migrated H2 headings, which none of those lines contain) — accepted as out-of-scope content revision per CONTEXT.md's explicit phase boundary, not a security-relevant leak |
</threat_model>

<verification>
- `[ ! -f CLAUDE.md ]` — root CLAUDE.md is gone
- `! grep -E '^## (Mapa de Regras|Arquitetura Padrão de Agentes|Arquitetura Multi-Agente|Slash Commands|Regras Básicas)' .claude/CLAUDE.md` — CLMD-04's exact regression grep from VALIDATION.md, adjusted for root CLAUDE.md no longer existing to grep
- `.claude/CLAUDE.md`'s commits line no longer references `CLAUDE.md` as a source
</verification>

<success_criteria>
- Root `CLAUDE.md` removed from the repository
- `.claude/CLAUDE.md` is the only functional "Project instructions" file for this repo, contains zero migrated headings, and its commits-rule line cites only `docs/proibido-fazer.md`
</success_criteria>

<artifacts_produced>
## Artifacts This Plan Produces

- **Removed:** root `CLAUDE.md`
- **Modified:** `.claude/CLAUDE.md` — one line in "Agent-Editing Conventions" no longer cross-references the now-removed root `CLAUDE.md`
</artifacts_produced>

<output>
Create `.planning/phases/03-separar-claude-md-distribuido-cliente-via-npm-do-claude-md-i/03-04-SUMMARY.md` when done
</output>
