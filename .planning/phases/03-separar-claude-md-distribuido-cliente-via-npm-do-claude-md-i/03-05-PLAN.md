---
phase: 03
plan: 05
type: execute
wave: 3
depends_on: ["03-04"]
files_modified:
  - .claude/hooks/check-claude-md-audience.sh
  - .claude/hooks/check-claude-md-audience.test.js
  - .claude/settings.local.json
  - .gitignore
autonomous: false
requirements: [CLMD-07, CLMD-08]

must_haves:
  truths:
    - "Reintroducing any of the 5 migrated H2 headings into CLAUDE.md or .claude/CLAUDE.md is automatically blocked (decision:block) by check-claude-md-audience.sh on the next Stop/SubagentStop event, without any manual review step"
    - "The guard is registered ONLY in .claude/settings.local.json — never in .claude/settings.json, and the script itself is never listed in manifest.json's files[] — so no client project ever receives a reference to it"
    - "End-to-end distribution (bin/cli.js against the updated manifest.json) still produces a CLAUDE.md in an installed project identical to the pre-migration payload"
  artifacts:
    - .claude/hooks/check-claude-md-audience.sh
    - .claude/hooks/check-claude-md-audience.test.js
  key_links:
    - ".claude/settings.local.json hooks.Stop / hooks.SubagentStop → \"$CLAUDE_PROJECT_DIR\"/.claude/hooks/check-claude-md-audience.sh"
---

<objective>
Build the repo-local-only regression guard (D-09/D-10): a deterministic `Stop`/`SubagentStop` hook that blocks any future edit reintroducing client-facing content into `CLAUDE.md` or `.claude/CLAUDE.md`, wired ONLY into `.claude/settings.local.json` (never distributed). Close the phase with the one unavoidably-manual verification step (CLMD-08): confirming end-to-end distribution still works.

Purpose: This is step 7+8 of the phase's additive-first sequence, the final wave. It depends on Plan 03-04 because the guard's purpose — catching regressions in `CLAUDE.md`/`.claude/CLAUDE.md` — is only meaningful once those two files are in their final post-migration state.
Output: `.claude/hooks/check-claude-md-audience.sh` (new, bash, never in `manifest.json`), `.claude/hooks/check-claude-md-audience.test.js` (new, node:test), `.claude/settings.local.json` (new `hooks` block, existing `permissions` block untouched), `.gitignore` (new entry for `.claude/settings.local.json`), plus a closing checkpoint verifying the full pipeline end-to-end.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-separar-claude-md-distribuido-cliente-via-npm-do-claude-md-i/03-CONTEXT.md
@.planning/phases/03-separar-claude-md-distribuido-cliente-via-npm-do-claude-md-i/03-RESEARCH.md
@.claude/hooks/validate-xml-casca.sh
@.claude/hooks/validate-xml-casca.test.js
@.claude/settings.json
@.claude/settings.local.json
@manifest.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create check-claude-md-audience.sh + check-claude-md-audience.test.js</name>
  <files>.claude/hooks/check-claude-md-audience.sh, .claude/hooks/check-claude-md-audience.test.js</files>
  <read_first>.claude/hooks/validate-xml-casca.sh (full file, 62 lines — header comment style, stdin/transcript-extraction idiom, exit-0-always contract); .claude/hooks/validate-xml-casca.test.js lines 1-40 and lines 440-459 (makeTempDir/jsonlLine helpers + the CLI-level execFileSync test pattern to mirror)</read_first>
  <behavior>
    - Given a transcript where an `Edit`/`Write` tool_use touched an absolute path ending in `CLAUDE.md` (not containing `/client/`), and that file's current on-disk content contains a banned heading (e.g. `## Slash Commands`), the script emits `{"decision":"block","reason":"..."}` on stdout naming that file
    - Given the same touched file but its content contains NO banned heading, the script emits nothing (empty stdout)
    - Given a touched path containing `/client/CLAUDE.md` (the real client payload source) whose content DOES contain a banned heading, the script emits nothing — `client/CLAUDE.md` is explicitly excluded, never flagged
    - Given a transcript with no CLAUDE.md-named file touched at all, the script emits nothing
    - Given a missing/unreadable `transcript_path`, the script exits 0 with no output (same guard-clause idiom as `validate-xml-casca.sh`)
  </behavior>
  <action>
Create `.claude/hooks/check-claude-md-audience.sh` (bash, `chmod +x`). Structure, mirroring `.claude/hooks/validate-xml-casca.sh`'s established idiom exactly:

1. Header comment: purpose (repo-local-only regression guard for D-09, blocking client-facing content from leaking into `CLAUDE.md`/`.claude/CLAUDE.md`), an explicit note that this script is NEVER added to `manifest.json` and NEVER registered in `.claude/settings.json` (only `.claude/settings.local.json` — Pitfall 3), and a "DESIGN DELIBERADO" comment block stating there is intentionally NO `stop_hook_active` guard anywhere in this script (mirror the exact rationale from `validate-xml-casca.sh` lines 18-26: this hook reevaluates a static, re-checkable fact — "is this file's content client-facing right now?" — on every invocation, and must never be silently skipped on a retry).
2. `set -uo pipefail` (not `set -e`, matching project convention: `grep` returning no-match exit code 1 is an expected case handled explicitly).
3. `INPUT=$(cat)`, then extract `transcript_path` using the EXACT same grep+sed idiom as `validate-xml-casca.sh` lines 43-47 (no `jq`), followed by the same 3 guard-clause exits (empty, not-a-file, not-readable).
4. Discover touched CLAUDE.md files: `tail -n 400 "$TRANSCRIPT"`, grep for the pattern `"file_path"[[:space:]]*:[[:space:]]*"[^"]*CLAUDE\.md"` (matches any Edit/Write `tool_use.input.file_path` value ending in `CLAUDE.md`), extract the value with the same sed capture-group idiom, `sort -u` to dedupe, then `grep -v '/client/'` to exclude any path containing `/client/` (so `client/CLAUDE.md` itself — expected to legitimately contain the migrated headings — is never flagged). Store into a bash array via `mapfile -t TOUCHED < <( ... )`. If the array is empty, exit 0 with no output.
5. Define `BANNED_HEADINGS='^## Mapa de Regras$|^## Arquitetura Padrão de Agentes$|^## Arquitetura Multi-Agente|^## Slash Commands$|^## Regras Básicas$'` — reuse this EXACT regex (RESEARCH.md Exemplo 3 / Pitfall 4: high-precision match on the 5 migrated H2 headings only, never generic keywords like "modelo/" or "Recepcionista", which would false-positive on `.claude/CLAUDE.md`'s own legitimate "Component Responsibilities" table).
6. Loop over `TOUCHED`: skip (continue) if the file no longer exists on disk; if `grep -Eq "$BANNED_HEADINGS" "$f"` matches, `printf` a single line of JSON to stdout: `{"decision":"block","reason":"<f> contém um cabeçalho migrado para client/CLAUDE.md (D-05) — conteúdo de cliente vazando para doc interno."}` (interpolate the actual matched path), then `exit 0` immediately (Claude Code's block contract is presence of `decision:block` JSON on stdout — `exit 0` is still required, same as `validate-xml-casca.sh`).
7. If the loop completes with no match, fall through to `exit 0` with no output.

Create `.claude/hooks/check-claude-md-audience.test.js` using `node:test` + `node:assert` + `child_process.execFileSync` (mirror the CLI-level test at the end of `validate-xml-casca.test.js`, which shells out and asserts on stdout — but this hook reads stdin, not an argv flag, so invoke it via `execFileSync("bash", [HOOK_PATH], { input: JSON.stringify({ transcript_path: transcriptPath }), encoding: "utf8" })`). Reimplement small local `makeTempDir()`/`jsonlLine()` helpers in this new test file (do not cross-import private helpers from `validate-xml-casca.test.js`). Write one `test(...)` per bullet in the `<behavior>` list above (5 cases): build a temp dir, write a temp `CLAUDE.md` (or, for the exclusion case, a `client/CLAUDE.md` inside a `client` subdirectory of the temp dir) with the relevant content, write a synthetic JSONL transcript containing an `assistant` message with a `tool_use` `Edit` entry whose `input.file_path` is that temp file's absolute path, run the script, and assert on stdout.
  </action>
  <verify>
    <automated>bash -n .claude/hooks/check-claude-md-audience.sh && [ -x .claude/hooks/check-claude-md-audience.sh ] && node --test .claude/hooks/check-claude-md-audience.test.js 2>&1 | tail -15</automated>
  </verify>
  <done>check-claude-md-audience.sh exists, is executable, passes bash syntax check, and all 5 behavior cases in check-claude-md-audience.test.js pass.</done>
  <acceptance_criteria>
    - `bash -n .claude/hooks/check-claude-md-audience.sh` reports no syntax errors
    - Script has execute permission
    - All 5 `<behavior>` cases pass under `node --test`
    - Script never references `stop_hook_active` anywhere (grep confirms zero occurrences)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Register the guard in .claude/settings.local.json only, add .gitignore defense-in-depth</name>
  <files>.claude/settings.local.json, .gitignore</files>
  <read_first>.claude/settings.local.json (full file — currently only a `permissions` block); .claude/settings.json (hooks.Stop/hooks.SubagentStop array shape to mirror); .gitignore (current 4 lines)</read_first>
  <action>
Edit `.claude/settings.local.json`: add a new top-level `hooks` key, a sibling of the existing `permissions` key — preserve every existing key and every entry in `permissions.allow`/`permissions.additionalDirectories` byte-for-byte untouched. The new `hooks` key has this exact shape (mirroring `.claude/settings.json`'s existing `hooks.Stop`/`hooks.SubagentStop` array-of-`{hooks:[{type,command}]}` shape): `hooks.Stop` = `[{ "hooks": [ { "type": "command", "command": "\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/check-claude-md-audience.sh" } ] }]`, and `hooks.SubagentStop` = the identical single-entry array pointing at the same command string.

Do NOT touch `.claude/settings.json` or `manifest.json` in this task under any circumstance — the entire point of D-09 is that this registration must be invisible to every distributed client (RESEARCH.md Pitfall 3: registering it in the distributed `settings.json` would ship every client a dangling hook reference to a script that never arrives via `manifest.json`).

Append `.claude/settings.local.json` as a new line to the repo root `.gitignore` (defense-in-depth per RESEARCH.md Pitfall 3 — today this file is only protected by this environment's global git ignore config, not by anything committed in-repo, so a different clone/CI would have no protection against accidentally committing it).
  </action>
  <verify>
    <automated>node -e "const s=require('./.claude/settings.local.json'); if(!s.hooks||!s.hooks.Stop||!s.hooks.SubagentStop) throw new Error('missing hooks block'); if(!s.permissions||!Array.isArray(s.permissions.allow)) throw new Error('permissions clobbered'); const cmd=s.hooks.Stop[0].hooks[0].command; if(!/check-claude-md-audience\.sh/.test(cmd)) throw new Error('wrong command'); console.log('PASS')" && ! grep -q check-claude-md-audience .claude/settings.json && ! grep -q check-claude-md-audience manifest.json && grep -qx '.claude/settings.local.json' .gitignore && echo PASS2</automated>
  </verify>
  <done>.claude/settings.local.json has a new hooks.Stop/hooks.SubagentStop block pointing at check-claude-md-audience.sh, its existing permissions block is untouched, .claude/settings.json and manifest.json contain zero references to the new hook, and .gitignore lists .claude/settings.local.json.</done>
  <acceptance_criteria>
    - `.claude/settings.local.json` parses as valid JSON with both `hooks` and `permissions` top-level keys
    - `permissions.allow` array length is unchanged from before this task
    - `.claude/settings.json` contains zero occurrences of `check-claude-md-audience`
    - `manifest.json` contains zero occurrences of `check-claude-md-audience`
    - `.gitignore` contains the exact line `.claude/settings.local.json`
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the full separation pipeline and end-to-end distribution (CLMD-08)</name>
  <files>client/CLAUDE.md, manifest.json, bin/cli.js, .claude/settings.local.json, .claude/hooks/check-claude-md-audience.sh</files>
  <read_first>03-VALIDATION.md Manual-Only Verifications table (CLMD-08 row and CLMD-06 row)</read_first>
  <what-built>
The complete CLAUDE.md client/internal separation: `client/CLAUDE.md` as the real distributed source, `manifest.json`/`bin/cli.js` handling the new `{from,to}` schema, 9 distributed subagents/commands preferring `client/CLAUDE.md` with fallback, root `CLAUDE.md` removed, `.claude/CLAUDE.md` as the sole internal doc, and a deterministic regression guard wired into `.claude/settings.local.json` only.
  </what-built>
  <action>
No further automation to write for CLMD-08 — RESEARCH.md/03-VALIDATION.md explicitly mark end-to-end distribution as manual in this zero-dependency project (no network-fetch mock exists or should be built for it). This task is a human confirmation pass over the already-automated work from Tasks 1-2 of this plan plus Plans 03-01 through 03-04. Follow the how-to-verify steps below exactly.
  </action>
  <how-to-verify>
    1. Run the full automated regression: `npm test` (or `node --test bin/*.test.js .claude/hooks/*.test.js`) and confirm everything is green, including Phase 1's 27/27 `validate-xml-casca.test.js` count.
    2. Run `node bin/cli.js --help` and visually confirm the CLAUDE.md line reads `- CLAUDE.md` (never `[object Object]`).
    3. CLMD-08 end-to-end distribution check: in a scratch directory outside this repo, run `node <path-to-this-repo>/bin/cli.js install` (exercises the real `fetchFile`/`writeFile` logic — point `manifest.branch` at this phase's branch temporarily if it isn't merged to `main` yet), OR simply diff `client/CLAUDE.md` in this repo directly against a saved pre-phase copy of the old root `CLAUDE.md` with its `## Commits` section manually removed — either check confirms the payload content is unchanged. Clean up the scratch directory afterward.
    4. (Recommended, not blocking — reinforces CLMD-06) Run `/ei-cria-cliente` once inside this repo as a throwaway test client and confirm the scaffolder still correctly reflects multi-agente/Recepcionista rules despite root `CLAUDE.md` being absent — delete the throwaway client folder afterward.
    5. Sanity-check the guard live: temporarily add a line `## Slash Commands` to `.claude/CLAUDE.md`, trigger a `Stop` event (e.g. end a turn), confirm the session is blocked with a reason mentioning `check-claude-md-audience.sh`'s message, then revert the temporary line.
  </how-to-verify>
  <resume-signal>Type "approved" once all checks pass, or describe exactly which check failed and what you observed.</resume-signal>
  <verify>
    <human-check>Follow the 5 steps in how-to-verify above; CLMD-08 has no automated command in this zero-dependency project (no network-fetch mock) — a human must run the real install/diff comparison.</human-check>
  </verify>
  <acceptance_criteria>
    - Full automated test suite green (Phase 1 regression + Plan 03-02 + Plan 03-05 new tests)
    - `--help` output shows no `[object Object]`
    - End-to-end (or content-diff) check confirms the distributed `CLAUDE.md` payload is unchanged from pre-migration
    - Guard live sanity check blocks the temporary banned-heading insertion and the revert leaves the repo clean
  </acceptance_criteria>
  <done>Human has typed "approved" confirming all how-to-verify steps passed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .claude/settings.local.json (gitignored, maintainer-only) vs .claude/settings.json (distributed) | The exact boundary this task must never cross in the wrong direction — registering the guard in the wrong file ships a dangling hook reference to every client |
| check-claude-md-audience.sh's touched-file discovery vs transcript content | The script trusts `tool_use.input.file_path` values found in the local JSONL transcript — a local-only, non-network trust boundary (same as `validate-xml-casca.sh`'s existing discovery logic) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-10 | Information Disclosure / Tampering | Guard hook registration location | high | mitigate | Task 2's automated verify greps `.claude/settings.json` AND `manifest.json` for zero occurrences of `check-claude-md-audience`, confirming the registration lives exclusively in `.claude/settings.local.json` (RESEARCH.md Pitfall 3 — this is the single highest-severity threat in the whole phase, since getting it wrong silently breaks every client's Stop/SubagentStop event) |
| T-03-11 | Tampering (false positive) | BANNED_HEADINGS regex over-matching | medium | mitigate | Regex matches only the 5 exact migrated H2 headings (RESEARCH.md Pitfall 4), never generic keywords — Task 1's negative behavior case (no banned heading present) proves the script does not fire on `.claude/CLAUDE.md`'s own legitimate architecture-table content |
| T-03-12 | Tampering (design gap) | stop_hook_active suppression | medium | mitigate | The script intentionally never checks `stop_hook_active` (Pattern 4 in RESEARCH.md) — Task 1's verify explicitly greps for zero occurrences of that string, so a future "fix" cannot silently reintroduce a retry-suppression bug that would let a broken-content retry slip through unblocked |
</threat_model>

<verification>
- `check-claude-md-audience.sh` passes `bash -n` and all 5 behavior cases in its test file
- `.claude/settings.local.json` has the new `hooks` block; `.claude/settings.json`/`manifest.json` have zero references to the new hook
- `.gitignore` lists `.claude/settings.local.json`
- Human checkpoint confirms full regression suite green, `--help` sane, and end-to-end distribution content-correct
</verification>

<success_criteria>
- Reintroducing a migrated heading into `CLAUDE.md`/`.claude/CLAUDE.md` is automatically blocked
- The guard is invisible to every distributed client (absent from `manifest.json` and `.claude/settings.json`)
- Full test suite green (Phase 1 regression + Plan 03-02's new tests + this plan's new tests)
- Human confirms end-to-end distribution still produces the correct `CLAUDE.md` content
</success_criteria>

<artifacts_produced>
## Artifacts This Plan Produces

- **New file:** `.claude/hooks/check-claude-md-audience.sh` — repo-local-only Stop/SubagentStop guard, never listed in `manifest.json`
- **New file:** `.claude/hooks/check-claude-md-audience.test.js` — node:test suite, 5 behavior cases
- **Modified:** `.claude/settings.local.json` — new `hooks.Stop`/`hooks.SubagentStop` block (permissions block preserved)
- **Modified:** `.gitignore` — new entry `.claude/settings.local.json`
</artifacts_produced>

<output>
Create `.planning/phases/03-separar-claude-md-distribuido-cliente-via-npm-do-claude-md-i/03-05-SUMMARY.md` when done
</output>
